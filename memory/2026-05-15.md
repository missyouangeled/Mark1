## 2026-05-15

- OpenClaw 已升级到 2026.5.12；更新脚本里的 restart 异常根因不是升级失败，而是 18789 端口已有 gateway 在运行，当前 gateway 实际可用。
- 为兼容 2026.5.12，本地已修两处正式维护入口：`scripts/apply-openclaw-control-ui-branding.py` 适配新版 Control UI 前端 asset 结构；`scripts/openclaw-post-upgrade-self-check.py` 不再写死旧的 `index-*.js` asset 名。
- 升级后已重新通过关键自检：gateway reachable / service running / post-upgrade self-check PASS / broker 与 frontstage recovery watcher 正常。
- broker 当前已确认完整可用：snapshot-first 正常，`jarvis-frontstage-snapshot.json` 为正式主入口，`jarvis-frontstage-status.json` 仍保留兼容别名；视图 `frontstage` / `health` / `tasks` / `recovery` / `snapshot` / `overview` 均在。
- 2026-05-15 11:00 已完成一次前台真烟测：broker 成功把消息投递到当前 dashboard `agent:main:dashboard:0e238e2b-0e3c-4e63-9c1d-790e3f2794cf`，并已写入 transcript；验证“broker -> 当前前台”核心链路可用。
- 2026-05-15 11:08 左右又收掉了 supervisor 这条最后一处前台绑定偏差：`scripts/openclaw-supervisor-subagent.py` 现在在 owner=`agent:main:main` 且存在 dashboard 时，会优先把通知解析到最新 dashboard；`scripts/openclaw-supervisor-status.py` 也补了 broker 返回 `messageId` 的兼容提取。真烟测已确认 `[监工] frontstage-tail-continue-no-stop 已完成。` 实际落进当前 dashboard transcript，`notify_sent ... target=agent:main:dashboard:0e238e2b-0e3c-4e63-9c1d-790e3f2794cf messageId=ab0baee2-9cc5-4301-9cee-bcd554d10942`。
