# 2026-05-11

- 用户进一步明确了歌名判断的证据权重规则：当用户本人无法确认歌名、需要我来判断时，若失真严重的低质量模型输出不能给出稳定、靠谱的答案，则应优先采用高度相似的语义线索作为最终回答；低质量模型结果不能反向推翻更可信的高相似语义判断。
- 用户再次确认：在“第一次在酒吧认识千千、点她唱歌并录下”的那段视频里，她唱的是《安和桥》的一部分；后续优化识别结果也支持“我知道那些夏天就像你一样回不来”这一句附近。
- 当前未完成任务：用户希望我在抖音网页版搜索“海棠lolm”，并下载其主页显示的第一条视频；实际执行时被网页版滑块验证码拦住，当前最稳的继续路径是让用户提供主页链接或该条视频链接后再继续下载。
- 用户把“前端会话始终通畅、后台进度要及时反馈”再次明确为硬要求，而不是普通偏好；如果后台工作已开始却长时间无可见结果，应主动汇报当前在做什么、卡在哪、下一步是什么，不能让前端体感像没反应。
- 用户提出了一个长期改进方向：当检测到需要工作的长任务时，可考虑自动附带一个轻量“监工”后台分身/协调层，用来跟踪后台任务状态并把关键进度主动反馈回前台；更合适的落地方向是事件驱动、按任务临时创建，而不是永远常驻高频轮询。
- 用户授权：之后在 Skill 这类安装任务里，只要先完成安全审查且确认硬盘容量没问题，就可以直接安装，不必再次单独询问；安装前必须先做容量评估。
- 本轮 Skill 审查结论：已安装 1 个相对克制的抖音类 Skill `xiaoyiv/douyin-skill`；验证码类候选与较重的 `yzfly/douyin-mcp-server` 因涉及外部 API key、外部连接/服务、持久浏览器会话或权限面过宽，未通过“安全前提下直接安装”的标准，因此没有安装。
- 当前抖音 Skill 运行层仍有一项待续：`xiaoyiv/douyin-skill` 依赖 `nodriver-kit`，Skill 本体已落地，但该运行依赖尚未继续做独立供应链审查与安装。
- 本日已把“去抖音/其他平台搜索并下载视频”的可运行流程整理成正式文档 `docs/通用-视频平台下载工作流.md`，并同步在 `TOOLS.md` / `MEMORY.md` 留入口；以后默认先试平台内直达，若被验证码、登录墙、作品流异常或前端懒加载拦住，就立刻切到“外搜定位公开页面 → 反查作者主页 / 公开视频 → 提取真实媒体地址 → 下载并校验”的备用链路。
- 本次抖音验证链路的关键现实限制也已记录：站内搜索会进验证码中间页，作者主页作品流可能返回“服务异常，重新刷新拉取数据”且 API 响应体为空；当前仍缺稳定登录态、可接受的验证码方案、以及 `nodriver-kit` 的可审计公开来源。
- 用户同意继续把平台视频下载链路做成可复用入口；当前已新增最小脚本 `scripts/download-platform-video.py`，可直接处理“抖音公开视频页 URL → 提取真实 mp4 → 下载 → ffprobe 校验”，未来再在此基础上继续补“关键词搜索 / 作者主页 → 第一条作品”的自动化。
- 用户进一步强调：像“第一条作品”这种位置只是这次测试的临时位点，以后很可能会变成随机、最后一条、指定序号或指定视频 ID；因此视频下载链路里的“目标位”不能写死，必须做成变量。当前 `scripts/download-platform-video.py` 已补上 `--pick first|last|random|index:N|video:<id>`。
- 在继续补视频下载脚本时，已把一个关键兜底固定下来：当输入抖音作者主页 URL 且作品流本身返回“服务异常”时，脚本会明确停止自动选取，避免把页面里的热点/推荐视频误判成作者作品；此时应改走“公开视频页 / 外搜候选”链路。
- 继续补平台视频下载链路时，又确认了一个当前边界：在这台环境里，把“关键词 → 候选列表”直接脚本化还不稳，因为公开搜索引擎会分别遇到百度安全验证、搜狗反爬页、DuckDuckGo 不稳定、Bing 中文相关性偏弱；因此这一步暂时仍应保留给 agent 的外部搜索工具 + 规则判断，不要在脚本里假装已经可靠自动化。
- 继续补平台视频下载脚本时，又新增了一层对上游输入的兼容：`scripts/download-platform-video.py` 现在不只接受纯 URL，也可直接接受混合文本 / 搜索片段 / 分享文案，自动从文本中提取受支持的抖音视频页 / 作者主页 URL，再结合 `--pick` 选择目标位。
- 继续补平台视频下载链路时，已把“候选整理器”直接并进 `scripts/download-platform-video.py`：现在在不下载的情况下，也可用 `--list-only` 把混合文本 / 多 URL 输入整理成标准化候选 JSON（含 `selected`、`selected_from`、`candidates[index/platform/id/url]`），便于后续快速接力下载。
- 候选整理器又补了一步：`scripts/download-platform-video.py --list-only` 现在支持同时导出候选 URL 文本文件（`--candidates-out`）和结构化 JSON 报告（`--report-out`），这样我后续从搜索工具拿到候选后，可以更快地把结果固定成可复用的中间产物，再接力下载。
- 用户再次直接纠正：即使是在 Git 整理、提交、推送这类连续长操作里，也不能让前台长时间无反馈；进入“整理范围 / 暂存 / 提交 / 推送”等阶段前，应先用一句简短中文说明当前在做什么，必要时中途补进度，避免前台体感像掉线。
- 本日上午相关改动已整理并推送到 GitHub：提交 `ec402ca`，提交信息为 `Sync morning work: video workflow, vision pipeline and memory updates`。
- 用户提出一个新的长期方向：把“文生图 / 图生图 / 换图片生成模型”收成一个固定 skill 入口，之后只需自然语言下达生成或切模型指令，就能更快触发，不要每次临时手写脚本。
- 当前已在 workspace 内落地本地 skill 原型 `skills/nvidia-build-image/`，主脚本为 `skills/nvidia-build-image/scripts/nvidia_build_image.py`；其目标是把 NVIDIA Build 的文生图与模型切换收成固定入口，并默认复用 OpenClaw 现有的 `nvidia:default` 认证。
- 这条 NVIDIA Build 图像 skill 当前已验证可用的模型包括：`flux-dev`（主线质量文生图）、`flux-schnell`（快速 smoke test）、`flux-klein`（另一条已探活的官方文生图模型）；其中 `flux-klein` 的最小探活已成功。
- 这轮继续排查后确认了一个重要边界：在当前这台机器所走的 NVIDIA Build hosted 路线下，任意本地图片的 base64 直传图生图并未打通；不只是 `flux.1-dev` 的 `canny/depth`，连 `flux.1-kontext-dev` 也会报 `Expected: example_id, got: base64`。
- 同时也已确认：当前认证与入口下，NVIDIA 文档提到的 OpenAI-compatible `/v1/images/generations` 与 `/v1/images/edits` 根路由在现有 hosted 路线上并未直接开放，至少本轮访问结果为 404；因此当前 skill 不应假装“本地图生图已可用”，而应把它明确标记为 hosted 能力边界。
- 当前这套 `nvidia-build-image` skill 已通过打包校验，并产出 `tmp/skill-dist/nvidia-build-image.skill`；后续继续使用时，应把“文生图 + 换模型”视为已可用能力，把“本地图片直传图生图”视为待后续寻找 hosted 编辑入口或 self-hosted NIM 的未完成项。
- 用户刚把 NVIDIA 视频模型排查范围再次收窄并明确：当前只关心“云端 API key / hosted 路线能不能直接调用”，不要把判断岔到“下载模型后本地部署”。
- 本轮 NVIDIA 视频模型调查新增的较稳结论是：`stable-video-diffusion` 的官方 reference 页面确实存在，但当前 key 实测返回 `Function ... not found for account`；再结合 NVIDIA 官方论坛里已出现“模型页面可见、但 API / 视频上传功能因安全原因被禁用”的员工回复，当前更应把这些视频模型判断为“接口/页面存在，但 hosted 调用权限未对当前账号开放或被官方关闭”，而不是简单归因为 API key 填错。
- 用户在结束前又明确提出一条新的短期目标：基于当前设备评估“语音对话做成流式 / 至少把语音生成更快一些”是否可行；最终已把一份偏保守、以 `ChatTTS daemon 热机 + 首句分块先播 + 伪流式可打断` 为核心的《当前设备语音提速最小实施方案》写入 `PLANS.md`，作为明天继续推进的直接起点。
