# 2026-05-21

## 今日记录

- **08:31** 用户确认：本机（公司 Linux VM）默认拥有最高权限，无需逐次确认；只有涉及连接到其他设备操作时才询问权限。已写入 `TOOLS.md`。
- **08:32** 用户查询当前使用模型 → DeepSeek V4 Pro（`deepseek/deepseek-v4-pro`），默认模型 `nvidia/z-ai/glm-5.1`。
- **08:33-08:39** 用户报告 Control UI 模型下拉列表无法切换。
  - 排查过程：确认模型切换函数 `bR` 调用 `sessions.patch` 逻辑正确；怀疑 health dock（`position:fixed, z-index:80`）拦截了下拉菜单点击事件。
  - 修复方案：在 `scripts/apply-openclaw-control-ui-branding.py` 的 health dock CSS 中加入 `pointer-events: none`（容器）、`pointer-events: auto`（FAB 按钮和展开面板），防止 dock 容器拦截底层交互。
  - 已重新应用品牌补丁，生成新的 `jarvis-branding-override.js`。用户待刷新页面验证修复效果。
- **08:36** 用户再次确认当前模型 → 同上，DeepSeek V4 Pro。
- 对话中途触发了 compaction，进度上下文已通过 summary 保留。

## 待处理

- 等待用户刷新 Control UI 后验证模型下拉列表修复是否生效。
- 若仍无效，需要进一步排查：浏览器控制台错误、是否有 JS 异常影响事件绑定。

## 技术备忘

- Control UI 模型切换机制：`i.request('sessions.patch', {key: sessionKey, model: modelId||null})` 通过 WebSocket RPC 调用，非 HTTP REST API。
- Health dock 定位：`position: fixed; right: 18px; top: 148px; z-index: 80`，由 `positionHealthDock()` 动态调整以避开顶栏交互元素。
# 2026-05-21 每日记录

## Control UI 模型下拉列表修复

**问题：** 点点报告模型选择器"不管用了"——下拉列表点击无反应，或切换不生效。

**根因：** `jarvis-branding-override.js` 中有一段"模型选择器安全网"代码（约59行），在 document 上注册了 capture 阶段的 change/input 事件监听器，用自己的 getSessionKey() 和 patchModel() 重复发送模型切换请求，与 Control UI 自带的 <select @change> 处理逻辑冲突。

**修复：**
1. 从 `dist/control-ui/jarvis-branding-override.js` 删除第883-941行劫持代码
2. 从 `scripts/apply-openclaw-control-ui-branding.py` 同步删除第1102-1161行（生成模板中的劫持代码）
3. 验证通过：下拉展开/收起正常，模型切换生效，JS/Python语法完整

**持久化：** 品牌覆盖脚本已同步更新，后续 gateway 重启或升级后重新应用品牌覆盖不会再带这段劫持代码。

## 安装 Summarize Skill

- 搜索到 `steipete/clawdis@summarize`（16K安装量）
- 安装命令：`npx skills add steipete/clawdis@summarize -g -y`
- CLI 工具通过 npm 安装：`sudo npm install -g @steipete/summarize` → v0.14.1
- 默认配置 `~/.summarize/config.json` 设为 `openclaw/main`，走本地网关无需外部 API key
- 测试通过：`summarize "https://example.com" --length short`

## 后台分身教训

修复模型下拉列表时，后台分身（`fix_model_selector_patch`）使用了默认模型 `glm-5.1`，推进极慢（36分钟仅找到劫持代码的边界行号），最终前台直接完成修复并关闭该分身。后续给后台分身分配复杂任务时应优先指定更好的模型。

## 监工服务

- 任务期间开启 `force_on`
- 修复完成后恢复
