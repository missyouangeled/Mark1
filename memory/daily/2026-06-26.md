# 2026-06-26 日常记录

## 今日摘要

上午完成 OpenClaw 6.9→6.10 升级（详见 `docs/通用-OpenClaw-升级记录.md`），下午按用户要求"AI 模型都换 MiniMax-M3"集中处理 doctor 报告的 6 项优化。系统当前状态：Gateway 60215 健康 + doctor Errors 0，5 个 cron 任务 100% 走 MiniMax-M3。

## 重要事项

- **AI 模型全局统一**：`agents.defaults.model/imageModel` 全部改为 `minimax/MiniMax-M3`，minimax provider 加 `timeoutSeconds: 600` 替代 6.10 已废弃的 `agents.defaults.llm` 路径
- **5 个 cron 任务全部 MiniMax-M3**：2 个 enabled（早安+午餐）保持 ok 状态，3 个 disabled 也统一了 model
- **mark42-3day-checkpoint 修复 + 禁用**：原 `timeoutSeconds: 120` + 缺 `idleTimeoutSeconds` 导致连续 8 次 `model-call-started` 失败，已 `timeoutSeconds: 900` + 加 `toolsAllow=[process,read,write]` + 禁用。任务设计目的（3 天守护 v2.3 稳定性观察）已达成
- **OpenClaw 6.10 schema 调整**：① `agents.defaults.llm` 已 legacy，要用 `models.providers.<id>.timeoutSeconds`；② `agents.defaults.cron` 不存在，全局 model 自动被 cron 继承
- **doctor 误报**：6.10 用 sqlite 存 cron（`state/openclaw.sqlite`），但 doctor 仍读 `jobs.json.migrated` 旧备份，**报"5 jobs override"是滞后 bug**，实际 5/5 是 minimax

## 后续待办

- [ ] **Security 明文 API key 迁移**（用户 15:25 暂不要求）：openclaw.json 有 7 处明文（gateway.auth.token + 4 个 provider apiKey + 其他），用 `openclaw secrets configure` 迁到 SecretRefs
- [ ] 观察 MiniMax-M3 全局化后未来 1-2 周的稳定性（task-watch / health-watch cycle 增长、错误率）

## 15:33 收尾

- 联网核实确认：原判定"doctor 读 .migrated 误报"是错的——查 `cron-BPwqOmBa.js` 源码后发现 `legacyStoreDetected = false` 时 warning 来自 sqlite `rawJobs` 本身
- `openclaw cron edit --clear-model` × 5：清掉冗余 payload.model，让 job 继承 `agents.defaults.model = minimax/MiniMax-M3`，"5 jobs override" warning 消失
- `openclaw cron remove a6903701-...`：删除 mark42-3day-checkpoint（任务目标已达成 + 6.10 isolated agent 设计限制）,"manual command conversion" warning 消失
- doctor Errors 保持 0,cron 段完全消失
