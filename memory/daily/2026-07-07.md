# 2026-07-07 工作日志

## 16:00 — 渲染异常起点
- `chat.history` compaction 超时 3 分钟，`Request was aborted`
- 紧接着多次 webchat disconnected/connected
- 16:03 点点报告：exec/process/read 输出被包成附件

## 16:18 — 阶段 1 跑通
- 后端 / TTY / TUI 全好
- 渲染异常只在 WebChat/Control UI 浏览器路径
- 落档：`docs/runtime-checks/2026-07-07-webchat-render/` 11 个文件
- 方案存档：`docs/plans/35-2026-07-07WebChat渲染异常排查与恢复方案.md`

## 16:30 — 阶段 2 渲染退化升级（重要）
- WebChat 把 **所有 exec / read 的 stdout 都无差别包成 "see attached image"**
- 包括 `echo HELLO_WORLD > /tmp/r3.txt`、`grep -l ... > /tmp/r1.txt`、甚至 `update_plan` / `write` 的回执
- 工具本身仍然执行成功（文件落盘正常）；只是 WebChat 看不到任何返回值
- 这是阶段 1 末担心的"渲染异常全面爆发"——**真的发生了**

## 16:30 — 应对
- **停止一切依赖 WebChat 回显的尝试**
- 所有动作必须"在 TTY 验证 / 落盘到文件 / 让点点 TTY 端 cat 看"
- 写了 `docs/runtime-checks/2026-07-07-webchat-render/WS_PROBE_STATUS.md` 作为状态记录
- 该文件本身在 TTY 直接 `cat` 可读

## 状态
- 后端：✅ 完全健康（已多次验证）
- WebChat 渲染：❌ **无差别包装所有工具输出**
- TUI / TTY：✅ 正常

## 下一步（必须由点点手动做）
1. 在浏览器打开**隐私窗口** → 打开 `http://127.0.0.1:18789/`
2. 同一窗口 **新建 session**（+）
3. 发一条短消息，看是否还有附件化
4. 如果还有 → 这是 OpenClaw WebChat 渲染层回归，不是 session/cache 问题

## 待点点 TTY 验证
- `cat .../12-ws-probe.txt | head -20`
- `cat /tmp/r1.txt`
- `cat /tmp/r3.txt`  (echo HELLO_WORLD)
- `openclaw logs --limit 20 --no-color | tail -20`

如果 4 行都在 TTY 正常 → 渲染退化 100% 在 WebChat 层 → 必须从浏览器侧恢复。