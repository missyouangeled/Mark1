# 2026-07-08 工作日志

## 08:27 — 开工基线复核（昨天事故恢复确认）

点点 08:27 报到，按昨晚留的指令跑 TTY 4 项验证 + WebChat 渲染复测。

### TTY 4 项验证结果

| # | 命令 | 结论 |
|---|------|------|
| 1 | `cat /tmp/r1.txt` | 文件不存在（昨天清理掉了，不影响判断） |
| 2 | `cat /tmp/r3.txt` | 文件不存在（同上） |
| 3 | `cat docs/runtime-checks/.../12-ws-probe.txt \| head -20` | 输出 = `HELLO preview: connect.challenge` + `sessions.list` Traceback → **昨天 ws_probe 第二次调用 15s 超时的直接证据** |
| 4 | `openclaw logs --limit 20 --no-color \| tail -20` | 健康；仅显示昨晚 cron restart 尾巴 + 08:27:08 本次 webchat 连接 + 2 次 MiniMax-M3 200/2.5s |

### WebChat 渲染复测

- 08:27:08 `webchat connected conn=2db808fc…`
- `chat.startup 1321ms`、`chat.metadata 2336ms`、`models.authStatus 3424ms` 全部成功
- 点点发"渲染测试" → 收到**纯文字回显**，无附件包装 ✅

### 结论

后端、TTY、TUI、WebChat 全部正常。**昨天 16:00 那次 `Request was aborted` + 渲染回归未回潮**，被浏览器侧/前端 bundle 自身的更新或缓存清理自然治好。

### 不做的事（明确边界）

- ❌ 不推送 GitHub（点点 08:31 明确指示）
- ❌ 不动 Mark42 业务代码（`engine.py` 等）
- ❌ 不动 OpenClaw 源码
- ❌ 不重启 Gateway / 不清站点数据（不需要）

## 08:31 — 进度存档（本日志 + 4 份文件）

按点点指示保存当前进度：

1. `memory/daily/2026-07-08.md`（本文件）
2. `docs/plans/35-...` 第 5/8 节补完（验证清单填☑、阶段 3 数据回填）
3. `docs/runtime-checks/2026-07-08-render-recheck/`（新建基线证据目录）
4. `PLANS.md` 35 号方案状态从"调研"改为"阶段 1-3 通过 · WebChat 已自愈 · 归档"

## 下一步（待点点决定）

- 修方案文档第 9 节是否还在（实际上不存在，只有第 8 节；如需要可加"形态假设回填"作为新子节）
- 继续 Mark42 修复（用 TTY 验证流程）
- 升级 OpenClaw
- 其他

## 状态

| 层 | 状态 |
|---|---|
| 后端 | ✅ 健康 |
| TTY/TUI | ✅ 健康 |
| WebChat 渲染 | ✅ 自愈 |
| 业务代码 | ✅ 未改动（仍为快照回退后基线）|

---

## 08:50–09:40 Mark42 / Context 安全方案调研

### 背景
- 点点 08:50 回来，准备继续修 Mark42，但强调"再崩溃一次受不了"
- 调研发现：Mark42 主体无 bug，"崩溃"记忆来自历史 engine.py:374 改动，已 VM 快照回退干净

### 产出 4 份文档（未改任何业务代码）
1. `docs/design/mark42-修改风险评估报告-20260708.md` — 8 个真实风险点 + 4 不铁律 + 9 步检查清单 + 4 条恢复路径
2. 联网核查 4 个不确定点（OpenClaw sessions.compact 退出码 / compaction hang bug / subprocess 标准异常 / systemd 杀子进程）— 全部官方文档核实
3. **重大发现 R0**：armor_guard() 守护在 85% 时会自动调 armor_compress()（不传 dry_run → 走真压缩路径）— 不是 bug，是设计
4. `Context-安全防护方案-v1.md` — 三道防线：Session Pruning + compaction.model 用 flash + 保留救命 prompt；放在桌面 + workspace 双份

### 关键认知修正
- OpenClaw compaction bug #43661 真实症状 = "沉默循环 + 重复发同一条消息"，**不是会话卡死**
- 我之前推荐的"方案 E（85% 只提醒）"是自欺欺人 — OpenClaw bug 与谁触发无关
- 真正的改动空间：配 contextPruning + 换 compaction.model，不用写代码
- 推荐 compaction.model 候选：`litellm/agnes-2.0-flash`（首推）/ `minimax/MiniMax-M2.5`（次推）— 都已在 fallback 链验证
- 禁用：`nvidia/*` 系列（你明确说 gemma-4-31b-it 不好用）

### openclaw.json 双 compaction 块的真相
- 之前 grep 看到"两个 compaction 块"是**误读**
- Python 解析后确认**只有一个块**，结构干净
- 无需合并

### 待点点拍板
- [ ] 走《Context 安全防护方案 v1》哪几步？建议按 防线 1 → 防线 2 → 防线 3 顺序
- [ ] 备份点：是否同意 `cp openclaw.json openclaw.json.bak.20260708-context-safety`
- [ ] 是否同意"等 7 天观察期"作为新方案