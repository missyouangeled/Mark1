# 2026-06-05 日常记录

## 今日摘要

待整理。

## 重要事项

- 暂无。

## 后续待办

- 暂无。

## 今日摘要

全天在推进 Unity 项目 Props 文件夹的批量重命名工作。

### V3 贴图命名规则应用（上午）
- 完成 29 条贴图重命名：`RoughnessMask`→`Mask`、`NM`→`Normal`、`A`→`Albedo`、`height`→`Height`、分辨率标记删除等
- 同步 29 条 `.meta`，零残留
- 修复 `split_ext()` 函数：只按已知贴图扩展名切分，避免把 `Материал.001` 的 `.001` 误当作扩展名

### V4 计划生成与执行（上午～中午）
1. **Wooden_Stuff**：两个 pallet prefab 加 `#dark`/`#light` 后缀
2. **Clothesline**：Materials A/B/C 子文件夹扁平化，按变体 A/B/C 命名
3. **container03**：子文件夹扁平化（实际无文件，空目录已清理）
4. **Drums_oil**：16 个子文件夹扁平化，`deguo`→`German_New`，`yingmei`→`USA`，`xiu`→`Rust`，贴图规则统一
5. **Stacks_of_boxes_YXY241212**：V3 已规范，跳过

### V4 合并与执行
- V4 是 V3（1197 条）+ 新规则的全量合并，不是只做五个文件夹
- 执行 1,197 条全部重命名/移动
- 修复路径中 `#` 字符导致的 `str.replace()` 破坏 bug
- 清理 100 个空旧源目录 + 128 个孤儿 `.meta`
- 最终：1,250 文件，525 目录，零重名，`.meta` 全部对齐

## 重要事项

- V3 原有 42 个同名冲突（同目标材质）按先到先得自动处理
- `0_PropsGroups/` 中的 29 个 prefab 已被 V3 移至 `Props_Group_*` 新目录，属正常行为
- 路径中含 `#` 字符时 Python `str.replace('', x)` 会导致路径爆炸
- `container03/Materials/A-E/Normal` 子文件夹中无实际文件（仅文件夹 .meta）

## flush-sync 12:38

### 下午：V4 执行后审查与回退

**flush 同步问题**：memory flush 写入 `memory/2026-06-05.md` 成功，但 sync 脚本追加到 `memory/daily/2026-06-05.md` 失败（可能时序问题）。手动补完。

**合规审查——发现 4 个 V3/V4 违规操作**：

| 文件夹 | 用户要求 | V3/V4 做了什么 | 修复 |
|--------|---------|--------------|------|
| `0_PropsGroups` | 不要动 | 58 个文件（29 prefab + .meta）被拆到 `Props_Group_*` | ✅ 全部回退 |
| `Stacks_of_boxes_YXY241212` | 只 Rename prefabs，不移动 | 54 条全拆到独立目录（Prefab/Model/Material 都移走） | ✅ 全部回退 |
| `Clothesline` | 不动 Prefab/ | 8 个 prefab 被移出 | ✅ Prefab/Model 回退，Materials 扁平化保持 |
| `Drums_oil` | 只要求 Materials 扁平化 | 22 个 prefab 被移出 | ✅ Prefab 回退，Materials 扁平化保持 |

**最终状态**：全部修正干净，零残留。

### 教训
- 排除规则必须落到脚本的过滤条件里，不能只靠记忆
- 合并 V3+V4 时必须逐条核对用户要求与 V3 原有行为是否冲突

### 下午补充（13:41）

- 建立数据盘临时区域 `/mnt/data/openclaw/scratch/`（32GB 可用），作为大文件操作、临时测试、中间产物的落地目录
- 规则写入 MEMORY.md：以后大文件/临时产物优先落 scratch，完成后清理内存/缓存
- `0_PropsGroups/` 文件夹名禁止"删除前导下划线"的规则明确写入 MEMORY.md

### Wall 重命名 (16:00-16:40)

- 完成 Wall/ 文件夹 V3 重命名（250 文件）
- 操作：Gate_single/Material/All 扁平化、Wall_Standard Model 搬迁到 Prefab 子文件夹、Wall_Fence_02 材质合入 H1M/Material、全量 V3 重命名（Props_前缀/删Type-/PascalCase/.meta同步）
- ⚠️ H2M 丢失 12 个 PaintWhite 变体 prefab（V3 "删除 # 后缀"导致同名覆盖），已标注待修复
- 生成 RENAME_PLAN_Wall.md（6 章规则 + 5 文件夹清单，250 文件）

## flush-sync 17:41
---

## 17:13–17:50 Wall 重命名计划格式标准化 + Xx 恢复

### 完成事项
1. **Wall 计划头部统一**：改成与 Props V4 一致的标准格式（标题 + 本计划=规则合并说明）
2. **六列清单格式落地**：`原始地址 | 原始文件名 | 新地址 | 新文件名 | 类型 | 备注`
3. **标准格式写入规范**：更新到 `docs/通用-大批量操作防上下文溢出规则.md`，新增"Unity 重命名计划文件标准格式"章节
4. **Wall_Standard 下所有含 `Xx` 的文件恢复原名**：新文件名改回=原始文件名，目录搬迁保留，不加 Props_ 前缀。共恢复 38 条：
   - H2M Model FBX: `Wall_H_02m_05m_Xx_01~04`
   - H2M Prefab: 所有 `_Xx` 和 `BrickIndustrial_06_Xx` 变体
   - H2M6 Model FBX: `Wall_H_02m6_5m_Xx_01~02`
   - H2M6 Prefab: 全部 Xx 变体

### 待处理
- `.learnings` 和 `.learnings/ERRORS.md` 的改动未随上次 push 一起提交
- Wall_Stone 下的 Xx 文件未动（用户只说 Wall_Standard）
