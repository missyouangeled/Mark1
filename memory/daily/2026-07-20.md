# 2026-07-20 日常记录

## 今日摘要

待整理。

## 重要事项

### 08:18-08:42 OpenClaw v2026.7.1 -> v2026.7.1-2 升级 + 系统全面检查

**升级过程：**
- 点点 8:18 到公司，看到系统提示可升级
- 按规矩先查安装注册表 + 当前版本 + npm 最新版本
- `openclaw update --dry-run` 确认目标版本 `2026.7.1-2`
- 通过 `systemd-run --user --collect --wait openclaw update --yes` 执行升级
- 升级成功：`2026.7.1 (2d2ddc4)` -> `2026.7.1-2 (0790d9f)`

**发现并修复的问题：**

1. **Branding 脚本失败（ExecStartPre exit 1）**
   - 根因：v2026.7.1-2 前端改用 Vite 代码分割，聊天逻辑从 `index-*.js` 拆到独立 `chat-page-*.js` chunk；所有函数名全变，6 个历史版本适配都匹配不上
   - 修复：在 `scripts/apply-openclaw-control-ui-branding.py` 中新增 v2026.7.1 版本适配分支
     - 文件扫描新增 `chat-page-*.js` glob
     - 函数映射：Bl=isHidden, ql=mergeHistory, O=roleNormalize, G=textExtract, ps=stripPrefix, Lb=unwrap, Pi=shouldSkip, Rb=isSending
     - 注入 Jarvis helper 函数 + yielded history replay + reading indicator
     - "无效重载" bug 已被新版本原生修复，不再需要补丁
   - 修复后 ExecStartPre 退出 0 ✅

2. **Gateway 重启卡在 deactivating**
   - 原因：升级时 gateway 内运行中的会话/任务阻止优雅退出
   - 处理：`systemctl --user kill --signal=SIGKILL` 强制终止后正常重启

3. **memory_search 超时**
   - 原因：embed-sidecar 在升级前已启动，与升级后的 gateway 连接异常
   - 修复：重启 `openclaw-embed-sidecar.service` 后恢复正常
   - 验证：memory_search 搜索 3.5s 返回 6 条结果 ✅

4. **MiniMax catalog schema 错误**
   - 原因：`openclaw.json` 中 MiniMax-M3 的 `input` 字段包含 `"video"`，新版本 schema 不允许
   - 修复：从 `openclaw.json` 的 minimax provider 配置中移除 `"video"`，删除旧 catalog.json 让 doctor 重新生成
   - 验证：doctor 不再报 schema 错误 ✅

**系统全面检查结果：**

| 检查项 | 状态 |
|---|---|
| Gateway 服务 | active (running) ✅ |
| Gateway 版本 | v2026.7.1-2 (0790d9f) ✅ |
| ExecStartPre branding | exit 0 ✅ |
| embed-sidecar | active, search 正常 ✅ |
| infos-handle | active, port 18788 → 200 ✅ |
| unified proxy | active, port 18789 → 200 ✅ |
| systemd timers (7个) | 全部正常运行 ✅ |
| cron jobs (3个) | 全部 enabled, last ok ✅ |
| Skills (60) | Eligible ✅ |
| Plugins (10 loaded) | 0 errors ✅ |
| Control UI branding | index.html + jarvis-branding-override.js + chat-page 补丁 ✅ |
| memory_search | 正常 (3.5s, 6 hits) ✅ |
| session backup | 最近 08:37 正常备份 ✅ |
| lifecycle maintainer | 正常运行 ✅ |
| session size watcher | 正常运行（清理了 12.6MB） ✅ |
| frontstage guardian | 正常运行 ✅ |
| doctor | 0 errors ✅ |

### 13:25 最终验证

- mcporter daemon: `qmd: connected` ✅
- memory_search "点点生日": 3.4s 返回 6 条结果 ✅
- memory_search "测试搜索": 2.5s 返回 6 条结果 ✅
- cgroup: `memory.max=3221225472` (3G), `memory.high=2147483648` (2G) ✅
- OpenClaw 自动拉起 mcporter daemon 验证：手动 stop daemon 后 memory_search 仍正常（OpenClaw 自动 start）✅

**结论：memory_search vsearch 超时问题彻底解决。**

---

## 后续待办

- 暂无。升级后全面检查通过，所有服务正常运行。

---

### 10:31-12:10 Codex 集成调研 + Trae Agent 确认 + memory_search 深度修复

**Codex 集成调研：**
- 点点问 OpenClaw 新版本的 Codex 集成是怎么回事
- 查了官方文档：OpenClaw 的 `codex` 插件可以把 Codex app-server 作为 agent runtime，让 OpenAI GPT-5.6 原生执行编程任务
- 与我们当前链路无关（我们用 doubao-seed-2.0-pro，不走 OpenAI）

**Trae Agent 确认：**
- 点点记得装过一个"代码版的 trae"，让我查
- 在 `~/trae-agent/` 找到：字节跳动的 `bytedance/trae-agent` v0.1.0
- 当前配置：通过 OpenRouter 走 `gpt-oss-120b:free`，$0 费用
- 有配套的 `trae-agent-engineering` skill（贾维斯工程任务默认加载）
- 调用方式：`~/trae-agent/jarvis-trae.sh "任务"`

**memory_search 深度修复（核心问题）：**

上午 08:42 记录"memory_search 已修复"是重启 embed-sidecar 后的临时恢复，但根本问题没解决。

**根因分析：**
1. `memory.backend = "qmd"` -> OpenClaw 用 qmd 做记忆搜索
2. OpenClaw 有自己的 qmd home：`~/.openclaw/agents/main/qmd/xdg-cache/qmd/`
3. 之前误以为 qmd 用的是 `~/.cache/qmd/`（用户 home），花了很多时间在错误的地方建索引
4. OpenClaw 的 qmd 索引：120 文件已索引，但向量嵌入基本没做（只有 384/1500+）
5. `searchMode = "vsearch"` 要求向量搜索，但向量不够 -> 超时
6. 两个 qmd embed 进程同时跑，各吃 ~500MB 内存 -> 系统内存不够 -> Gateway 被 cgroup OOM kill
7. Gateway 反复重启，每次都把正在进行的会话中断

**修复措施：**
1. 将 `searchMode` 从 `"vsearch"` 改为 `"search"`（BM25 模式），不需要向量也能搜索
2. 杀掉所有 qmd embed 进程释放内存
3. 用 `systemd-run` 独立单元跑 embed，限制 `MemoryMax=450M`、`Nice=10`，不影响 Gateway
4. memory_search 恢复正常：2.6s 返回结果 ✅

**经验教训：**
- OpenClaw 的 qmd home 在 `~/.openclaw/agents/main/qmd/`，不是 `~/.cache/qmd/`
- 以后查 qmd 状态必须用 `XDG_CACHE_HOME` 指向 OpenClaw 的路径
- qmd embed 是内存密集型操作，不能在 Gateway cgroup 内直接跑
- BM25 模式（search）不需要向量，可以立即工作；vsearch 需要完整嵌入才能正常工作
- embed 进程必须用 systemd-run 独立跑 + 内存限制

**待完成（已全部解决 ✅）：**
- [x] OpenClaw qmd home 的向量嵌入完成（2670/2670 vectors）
- [x] `searchMode` 已改回 `"vsearch"` ✅
- [x] embed-sidecar 已禁用释放 1GB 内存 ✅

### 12:30-13:25 vsearch 超时根因终局修复

**问题：** 上午把 searchMode 改回 vsearch 后，memory_search 再次 15s 超时。

**完整根因链：**
1. `node-llama-cpp` 在本平台没有预编译二进制，首次 vsearch 要从源码编译 llama.cpp（耗时 6 分钟）
2. 编译完成后，vsearch 单次运行需要 ~10s 加载 300M 嵌入模型，刚好卡在 15s 硬超时边界
3. OpenClaw memory_search 的 15s 超时是**硬编码**（`MEMORY_SEARCH_TOOL_TIMEOUT_MS = 15e3`），不可配置
4. Gateway cgroup MemoryMax=1.5G 不够（vsearch 需 ~1.3G + Gateway 自身 ~200M）
5. **最关键**：mcporter daemon 未启用，每次 memory_search 都 spawn 新 qmd 进程冷启动

**最终修复：**
1. 用独立 systemd-run 单元完成 `node-llama-cpp` 编译并缓存二进制（~6 分钟）
2. 停用并 disable `openclaw-embed-sidecar.service`，释放 ~1GB 内存
3. Gateway cgroup 内存限制从 1.5G 调到 3G（`MemoryHigh=2G, MemoryMax=3G`）
4. **启用 mcporter daemon**：配置 `memory.qmd.mcporter = { enabled: true, serverName: "qmd", startDaemon: true }`
5. 创建 mcporter 配置文件 `config/mcporter.json`，设置 qmd server 为 `lifecycle: keep-alive`
6. searchMode 恢复为 `vsearch`

**效果：**
- memory_search 从 15s 超时 -> **2.5s 稳定返回**（6 条结果）
- qmd 通过 mcporter daemon 常驻，不再每次冷启动
- Gateway 重启后 OpenClaw 自动拉起 mcporter daemon
- 内存使用正常：Gateway 596M/3G，系统总用 2.5G/7.7G

**关键文件变更：**
- `/home/missyouangeled/.openclaw/openclaw.json`：新增 `memory.qmd.mcporter` 配置
- `/home/missyouangeled/.openclaw/workspace/config/mcporter.json`：新建，mcporter qmd server 配置
- `/home/missyouangeled/.openclaw/workspace/config/systemd/openclaw-gateway-resource-limits.conf`：MemoryHigh 2G, MemoryMax 3G
- `/home/missyouangeled/.config/systemd/user/openclaw-gateway.service.d/resource-limits.conf`：同步覆盖

**经验教训（长期记住）：**
- OpenClaw memory_search 15s 超时是硬编码，不可配置 -> 解法是让 qmd 常驻而非加超时
- mcporter daemon 是 OpenClaw 推荐的 qmd 常驻方案，文档在 `memory-config.md` 的 mcporter integration 章节
- `node-llama-cpp` 首次运行要编译 llama.cpp，必须用独立进程完成并缓存
- qmd vsearch 需要完整向量嵌入才能正常工作，BM25 模式（search）不需要
