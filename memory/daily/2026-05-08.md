# 2026-05-08

## 会话摘要

- 用户让我检查并清理 Control UI 里的无用会话。
- 我先核对了 `sessions.json`、当前会话树与本地 session 文件, 确认“关掉会话”在当前实现里本质上是移除会话索引并处理对应会话文件。
- 这次清理按“保留当前会话树 + 主会话”的原则执行: 先删旧 dashboard、旧直聊、已结束/失败/超时或僵尸 running 的 subagent, 然后继续清理 `trajectory` / `checkpoint` / `bak` / `reset` / `.deleted` 残留。
- 随后又做了一轮大扫除, 删除了 `sessions` 目录里所有不属于当前 3 个保留会话的旧文件, 并清掉老备份目录 `~/.openclaw/agents/main/sessions.before-2026.4.5.bak`。
- 本次会话清理累计释放约 309 MB 空间, 最终只保留当前会话树、主会话及其必要文件。

## 新规则 / 偏好

- 用户明确要求: 以后当他说“清会话”“再清一下会话”这类指令时, 默认沿用本次验证过的分层会话清理方案, 不再每次临时决定。
- 默认策略: 先保留当前正在使用的会话树与主会话, 先处理索引和主 `jsonl`, 再清理残留与旧归档, 默认不碰当前活跃会话自己的核心文件, 以稳为先。
