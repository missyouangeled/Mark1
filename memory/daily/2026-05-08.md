# 2026-05-08

## 会话摘要

- 用户让我检查并清理 Control UI 里的无用会话。
- 我先核对了 `sessions.json`、当前会话树与本地 session 文件, 确认“关掉会话”在当前实现里本质上是移除会话索引并处理对应会话文件。
- 这次清理按“保留当前会话树 + 主会话”的原则执行: 先删旧 dashboard、旧直聊、已结束/失败/超时或僵尸 running 的 subagent, 然后继续清理 `trajectory` / `checkpoint` / `bak` / `reset` / `.deleted` 残留。
- 随后又做了一轮大扫除, 删除了 `sessions` 目录里所有不属于当前 3 个保留会话的旧文件, 并清掉老备份目录 `~/.openclaw/agents/main/sessions.before-2026.4.5.bak`。
- 本次会话清理累计释放约 309 MB 空间, 最终只保留当前会话树、主会话及其必要文件。

## 新规则 / 偏好

- 用户明确要求: 以后当他说“清会话”“再清一下会话”这类指令时, 默认沿用本次验证过的分层会话清理方案, 不再每次临时决定。
- 默认策略: 先保留当前正在使用的会话树与主会话, 先处理索引和主 `jsonl`, 再清理残留与旧归档, 默认不碰当前活跃会话自己的核心文件, 以稳为先。
- 用户随后再次确认: 掌机必须明确按 `掌机（Windows）` 理解, 不能让后续更新流程读偏到 Linux。
- 用户进一步明确纠正: 他的主目标不是让他自己在掌机上手动点更新脚本, 而是让掌机上的 OpenClaw 在拉取 GitHub 更新并重启后, 自己识别为 `掌机（Windows）`, 自己继续读对应说明, 并尽量自主完成本机所需的下载 / 安装 / 修复 / 验证; 因此已把掌机维护文档和多机器规则改成“自动自更新是主流程, `.cmd/.ps1` 只是手动兜底入口”的表达。
- `scripts/update-openclaw.ps1/.cmd` 仍保留, 但定位已降级为掌机上的手动兜底入口; 同时它们也被做成更适合本机直接双击/直接看的提示版: 脚本开头会明确提示这是掌机 Windows 更新入口, 结束时给出“更新结果摘要”, 并自动跑一次 `openclaw gateway status` 快查；`.cmd` 在双击运行场景下会停住窗口, 方便用户看完结果再关。
- 用户随后又明确加了一条更硬的语音规则: 以后只要是语音回复场景, 在没有明确替代指示前, 默认都使用上午确认过的 `中文混合模板版本` (`tmp/voice-replies/zh-hybrid-default-template.mp3`), 不要为了技术验证临时切到其他声音。
- 下午继续排查后, 用户明确放弃继续折腾 NVIDIA 免费近似版, 当前调音主线改为 `characteristic-voice` 的第二条定向方案（Noiz 路线 + 上午模板作参考音频 + 定向 emo/speed）, 先沿这条继续逼近上午“完美版”; 但在用户再次听感确认前, 暂不把它擅自写死成新的长期默认模板。
- 下午为了把 Kokoro 模型从宿主机浏览器拷到当前机器, 临时起了一个带随机 token 的浏览器上传页, 用户已成功通过该页面上传 `voices-v1.0.bin` 与 `kokoro-v1.0.int8.onnx`; 用户随后明确要求把这套“宿主机浏览器 → 当前机器临时上传页”的方式记成以后默认文件拷贝方法之一。
- 随后已用用户手动上传的 `kokoro-v1.0.int8.onnx` 与 `voices-v1.0.bin` 真正跑通 Kokoro 中文离线样本; 但用户当场反馈差距很大, 几乎听不出来说的是中文。结论是: Kokoro 这条线目前属于“技术已跑通, 但中文体验不达标”, 暂不能作为上午那版完美语音的可替代方案。
- 继续深挖后, ChatTTS 在当前公司 Linux 机器上也被真正跑通: 通过“v0.1.1 权重 + v0.2.x 运行时 + 运行时补丁”的 hybrid 方案, 成功产出 `tmp/voice-replies/chattts-hybrid-infer.wav` 中文样本。
- 用户试听后明确反馈: “效果相当真实。可以。” 这说明 ChatTTS 这条线的听感已达到可接受级别; 但由于当前实现仍依赖素材混拼、跳过校验与 monkey-patch, 暂更适合作为已验证可用的候选方案, 还不应在未再确认前直接顶替现有默认中文语音回复链路。
- 傍晚继续追问后, 我又直接检查了 `tmp/voice-replies/chattts-hybrid/asset/DVAE_full.pt` 的键分布, 确认它名字虽叫 `DVAE_full.pt`, 实际并不完整: `encoder.* = 0`、`downsample_conv.* = 0`、`preprocessor_mel.* = 0`; 虽然还能看到少量 `vq_layer.*`, 但整体仍缺失编码侧关键权重, 只有大量 `decoder.*`; 这意味着当前本地 ChatTTS 仍只能证明“能出中文、听感可用”, 还不能证明 encoder/参考音频编码链路完整恢复。
- 用户随后询问 encoder 是否可以补全, 我的判断是: 理论上有机会, 前提是拿到与当前运行时匹配的官方完整 DVAE 资产; 不能靠补脚本凭空造出 encoder。考虑到剩余工作至少包括“定位官方完整资产 → 验证权重键完整性 → 跑兼容加载测试 → 再测参考音频/zero-shot 路径”, 到 18:00 前完成的把握偏低, 因此已按用户要求提前把当前结论和明天的续做路径写入 `HANDOFF.md`, 明天继续更稳。
- 随后用户又要求我用剩余时间正式设计一套可靠方案, 先核清“到底缺什么、如何补全、网上是否能证明确实可行”, 再写入方案存档。我已额外完成以下收口: (1) 本地源码层面确认 `sample_audio_speaker()` 依赖 `dvae.sample_audio()` 的 encode 路径, 而 encode 路径至少要求 `encoder + downsample_conv + preprocessor_mel + vq_layer`; 当前本地 `DVAE_full.pt` 缺失其中关键编码侧权重, 只能继续走 decoder 路; (2) 外部资料层面核到官方 README 摘要公开写过 `Open-source DVAE encoder and zero shot inferring code`, Hugging Face 资产树公开存在 `DVAE.safetensors / Decoder.safetensors`, 且有 `feat: add DVAE with encoder (#26)` 提交痕迹, 说明这条路在官方公开世界里确实存在; (3) 现实层面又补测到当前机器 shell 直连 GitHub / Hugging Face 分别报 `Connection reset by peer` / `Network is unreachable`, 所以后续可靠主路径不能默认依赖本机直连下载, 应优先走“宿主机浏览器下载官方资产 → 临时上传页传入当前机器”; (4) 已把完整方案、风险、执行顺序和成功标准写入 `PLANS.md` 的 `2026-05-08：ChatTTS encoder 补全与 zero-shot 恢复方案`, 并额外准备了资产验货脚本 `tmp/voice-replies/chattts-inspect-asset.py`, 供明天/后天拿到官方模型后先做键分布检查。
- 用户随后又追问当前 VMware 虚拟机是否也能使用宿主机 GPU, 要求核实可行性并并入方案。我已本地直接检查当前客体, 看到的显卡是 `VMware SVGA II Adapter [15ad:0405]`, 驱动是 `vmwgfx`, `nvidia-smi` 不存在, `/sys/class/drm/card0/device/vendor` 也是 VMware 的 `0x15ad`, 说明当前这台 VM 只有 VMware 虚拟显卡, 不能视为已经拿到可供 CUDA / PyTorch 使用的真实宿主 GPU。外部资料进一步核到: Broadcom 社区公开讨论明确区分了 Workstation 的 3D 图形加速与真正 host GPU support; VMware / Broadcom / NVIDIA 官方文档则确认在 `vSphere/ESXi` 里可以通过 `VMDirectPath I/O` 或 `NVIDIA vGPU` 让 VM 使用 GPU 做 AI / ML 负载。因此最终结论已补入 `PLANS.md`: **当前这台 VMware 桌面虚拟机不能作为现成 GPU ChatTTS 方案, 但如果未来改走 vSphere / ESXi + passthrough / vGPU, 那条路在官方层面是可行的。**
- 用户随后又追问自己这张 `RX 6900 XT` A 卡是不是“真的不能用”。我进一步核到更准确的表述应是: **不是卡本身不能用, 而是当前这台 VMware 客体没有真正拿到它。** 公开检索里能看到 `RX 6900 XT` / `gfx1030` 出现在 ROCm 相关文档与条目中, 但也能看到 `gfx1030` 存在“只有部分 operator 支持”这类限制; 因此更稳的结论是: 这张卡在宿主机 / 裸 Linux + ROCm 路线上有现实可用性, 只是兼容性和算子覆盖不如更新一代卡那么省心, 不能简单理解为“这张 6900 XT 废了”。
- 17:55 后用户已按方案 A 从宿主机浏览器下载并通过临时上传页把 `DVAE.safetensors` 与 `Decoder.safetensors` 传到当前机器。我立即本地验货, 结果非常关键: `tmp/upload-drop/chattts-20260508/DVAE.safetensors` 含有完整编码侧关键键--`encoder.* = 113`、`decoder.* = 113`、`downsample_conv.* = 4`、`preprocessor_mel.* = 2`、`vq_layer.* = 8`; 而 `Decoder.safetensors` 仍是 decoder-only。也就是说, **官方完整 DVAE 候选资产已经到手并通过第一轮键级验货**, 明天不需要再花时间找模型文件, 可以直接进入兼容加载测试与 sample audio / zero-shot 验证。上传完成后, 我已关闭临时上传服务收口。
