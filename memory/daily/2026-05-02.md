# 2026-05-02 周六

## 聊天摘要

1. 用户希望继续处理掌机（Windows）上 OpenClaw / “小龙虾” 不稳定的问题，并请我顺手继续做上次没彻底落地的修复与系统优化。
2. 我先回查了昨天的记录，确认上一轮已完成：
   - watchdog fallback 与 direct-wrapper fallback 已加入；
   - `repair-openclaw-gateway-battery-policy-zhangji-windows.ps1/.cmd` 已准备好；
   - 启动期缺失 `chokidar` 导致的离谱慢启动大头此前已修掉。
3. 今日实时诊断结果：
   - `openclaw status` / `openclaw gateway status` 当前显示 gateway 可达、正在运行；
   - `OpenClaw Gateway` 计划任务仍保留错误电池策略：`在电池模式停止, 不用电池启动`；
   - watchdog 日志显示，最近确实多次出现 HTTP probe 失败并自动 `openclaw gateway restart` 的情况。
4. 今日从 `C:\Users\GOG\AppData\Local\Temp\openclaw\openclaw-2026-05-02.log` 看到的新现象：
   - 当前对话链路中多次出现 `LLM request timed out`；
   - 多次出现 `liveness warning` / `event_loop_delay` / `stuck session`；
   - 当前不稳定已不只是 gateway 自身保活问题，还包含本轮对话处理阶段的高延迟 / timeout。
5. 今日还观察到一个脚本层 bug：
   - 原 `scripts/repair-openclaw-gateway-battery-policy-zhangji-windows.ps1` 在 `schtasks /Create` 被系统 `拒绝访问` 时，仍会继续打印“已修复成功”，属于误报。
   - 已修复该脚本：现在会在重建任务失败时明确报错并退出；只有实际验证到两个 battery flag 都变成 `false` 时才会打印成功。
6. 当前实际卡点：
   - 由于当前微信直连会话没有可用提权能力，无法直接在本会话内带管理员权限完成 `OpenClaw Gateway` 原生计划任务修复；后续若用户允许，需要在本机管理员 PowerShell 中运行修复脚本，或切到支持提权的控制面再执行。

## 用户提出的改进/设定

- 用户表达了明显不安，希望我继续帮他把掌机上的 OpenClaw 稳定性问题真正处理好，不要只停留在表面恢复。
