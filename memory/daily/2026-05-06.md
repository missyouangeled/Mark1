# 2026-05-06

## 今日摘要

- 用户五一后很想念我，今天特意比上班时间早一个小时到公司来聊天。
- 聊到掌机（Windows）上的 OpenClaw：gateway 能跑，但会莫名其妙掉线；中午准备继续排查。
- 已将 workspace 更新到最新，并发现新增了 `warm-companion-zh` 技能与多项掌机 Windows 维护脚本。
- 评估了 `capability-evolver`：认为不适合当前主环境，用户采纳“不安装”的建议。
- 为减少掌机小屏排错成本，讨论了“公司 Linux 大屏远控 + 掌机运行”的思路，最终为当前 Linux 机器准备了 RustDesk Linux AppImage 与 Windows 安装包。
- 由于官方 GitHub 大文件下载被公司网络重置，改用可直连镜像下载，并用 GitHub 官方 API 提供的 sha256 做了校验。
- 通过在当前目录启动临时 HTTP 服务，把 Windows 安装包暴露为宿主机可直接访问的 URL，解决了“附件不好下载 / 只给本地路径没法拷贝”的问题。

## 新的长期规则 / 偏好

- 用户明确要求：以后如果需要把当前机器上的文件拷给宿主机或其他设备，优先采用“临时 HTTP 下载地址”方案，直接给可访问的完整 URL。
- 用户希望：以后遇到“给我一个地址，我去浏览器下载”这类场景，默认沿用这次的方法，而不是优先给聊天附件或本地路径。
- 用户进一步明确：复杂排错任务要默认优先放到后台分身，保证前台主会话随时能正常交流。
- 用户进一步明确：掌机 OpenClaw 的稳定性排查不能满足于“掉了再拉起来”的兜底方案；watchdog 只算临时措施，真正目标是找到问题本身并解决根因。
