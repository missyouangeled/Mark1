# 2026-07-22 日常记录

## 今日摘要

ChatTTS 本地引擎废弃，正式切换到豆包 Seed TTS 2.0 作为语音回复标准引擎。

## 重要事项

- **ChatTTS 清理完毕**：模型文件已丢失（Vocos.pt 缺失），删除了 ChatTTS 技能目录、on-demand daemon、hybrid 资产、以及 6.1G 的 Python 虚拟环境（`openclaw-voice-venv311`）
- **豆包 TTS 正式上线**：使用 `byted-ark-tts-skill`，默认音色「魅力苏菲 2.0」，输出 wav 到 `media/tts/`
- **语音回复标准更新**：
  - 禁止在 `--text` 中使用 `#...#` 情绪指令前缀（用户明确要求）
  - 语速通过 `--speed` 参数控制，默认 1.0
  - 音频文件必须存到 `media/tts/`，不能存 `tmp/`（会导致前端无法播放）
  - 已更新 `memory/rules/chat-prefs.md` 中的语音偏好规则
- 清理了旧语音文件 `tmp/voice-replies/` 下的残留文件

## Mark42 v3 核心模块验收

- **R1 可插拔模型层验收完成**：15 个测试全通过，覆盖 v3 §2.4 的 5 条硬标准（换 runtime / model / API 只改配置 / stub 兜底 / fallback 链路 / advisor 开关）
- **R3 战甲主动交流验收完成**：19 个测试全通过，覆盖 4 类对话场景（A/B/C/D）+ advisor verdict 解析（approve/reject/modify）+ 低置信度降级 + API 失败兜底 + markdown 包裹 JSON 解析
- **R13-D FAILURE.md 降级契约完成**：`failure_contract.py` 实现 9 字段契约 + 8 核预定义配置 + Markdown 渲染 + `core_registry.py` 集成自动生成/清理 FAILURE.md。27 个测试全通过
- **R10+R14 集群思维完成**：`cluster_manager.py` 实现 8 集群目录结构 + 3 指标健康检查 + 重启 3 次上限 + 一键替换 + CLI 子命令（`mark42 cluster list/health/restart/replace`）。46 个测试全通过
- **全量测试结果**：934 通过 / 0 失败 / 2 跳过，覆盖率 82.9%
- **修复 conftest 隔离冲突**：R1 测试因 conftest reload 模块导致 isinstance 失败，改用 `type().__name__` 字符串比较解决
- **新文件同步**：`failure_contract.py` / `cluster_manager.py` / `log_setup.py` 从 `mark42-pkg/` 同步到 `scripts/mark42_modules/`；测试文件同步到 `scripts/tests/unit/`

## Mark42 v3 核心模块验收（第二轮）

- **R11 混沌工程模块完成**：`chaos_engine.py` 实现 7 个实验（kill_engine/armor、fill_disk、network_latency、high_context、circuit_breaker_trip、consciousness_degraded），四阶段框架 setup->execute->verify->cleanup，dry_run 默认安全模式。37 个测试全通过，覆盖率 77.7%
- **FAILURE.md 降级契约落地完成**：`core_registry.py` 的 probe_all/quarantine/restore 三个方法已集成 failure_contract，核心降级自动生成 FAILURE.md，恢复自动清理。8 个测试全通过
- **成本追踪模块完成**：`cost_tracker.py` 实现 CostRecord/CostTracker，支持 doubao/glm/agnes 三种模型定价，记录到 costs.jsonl，支持日报/月报/top callers/CSV 导出。llm_provider.py 已集成旁路记录（失败不阻塞主流程）。18 个测试全通过
- **cli.py Phase 3 测试补齐**：补充 archive/consciousness/cores/breaker/context-safety/logs 子命令测试。34 个新测试全通过
- **全量测试结果**：1052 通过 / 0 失败 / 2 跳过，覆盖率 83.4%
- **分身失败处理**：3 个分身中 2 个失败（R11 chaos + failure/cost），但分身在失败前已部分完成代码（core_registry 集成、llm_provider 成本记录代码），我接手补完了剩余部分

- **后续整改完成**：
  1. 删除旧 governance.py，拆出 module_health.py，CLI 全部迁移到新模块
  2. mark42-pkg 双目录同步：30 个文件同步 + 3 个新模块添加 + 导入路径修复 + CLI 烟测通过
  3. R6 dry-run 确认：所有执行入口（consciousness/heavy/chaos_engine）默认 dry-run/execute_now=False
  4. 修复 consciousness.py C1 自检里的 18792 端口探测 -> 改成 QMD 命令+索引检查
  5. v3-5 子 Loop 整合：context-guard 和 model-fallback 接入 Consciousness.handle_issue 链路（L4->advisor, L5->档案）
- **最终状态**：1061 测试 / 0 失败 / 83.2% 覆盖率 / 8 核全绿 / systemd 4 服务正常

## 后续待办

- v3-6 本地小模型接入（可选，需要 ollama）
- v4 用户感知层 HUD（前置条件：v3 跑稳 30 天）
- mark42-pkg 和 scripts 之间的 mark42_modules/mark42 导入路径差异是正常的（测试用前者，生产用后者）
