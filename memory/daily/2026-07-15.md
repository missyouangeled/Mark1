# 2026-07-15 日常记录

## 今日摘要

待整理。

## 重要事项

- **Nvidia 模型实测 + 配置修复**：点点截图 Control UI 里 4 个 Nvidia 模型（gemma-4-31b-it、Kimi K2.5、nemotron-3-super-120b-a12b、GLM 5.1），逐个调 Nvidia API 测了一遍。结论：只有 gemma 和 nemotron 真能用；截图里标 Kimi K2.5 / GLM 5.1 的实际是 UI 占位项，openclaw.json 里没接。
  - 修复：openclaw config patch 加上 `z-ai/glm-5.2`（实测 chat 可用，回复 pong），并把坏 alias `nvidia/moonshotai/kimi-k2.5`、`nvidia/z-ai/glm-5.1` 删掉。Gateway hot reload 生效。
  - Kimi 没接：账号 `/v1/models` 列出 `moonshotai/kimi-k2.6` 但 chat 接口报 404 "Not found for account"，是 Nvidia 那边没开通权限。等点点后续确认是否要换账号或换 key。
  - 备份：`~/.openclaw/openclaw.json.bak.20260715-kimi-glm`（改前完整快照）。

## 后续待办

- **Mark42 v3-1 阶段启动**：可插拔 LLM Provider（`llm_provider.py` + `model.yaml`）已完成，单测 25/25 通过。下一步按 v3 §6 进入 v3-2（错误档案系统 `error_archive.py`）。

## v3-1 落地细节

- **代码**：`scripts/mark42_modules/llm_provider.py`（约 350 行，含接口 + 3 runtime + 工厂 + fallback 链 + CLI）
- **配置**：`~/.config/mark42/model.yaml`（默认 stub，留好 ollama/api/advisor 三个钩子）
- **存档**：`docs/plans/40-Mark42-OS化深化方案-v3.md`（同步桌面版本）
- **验收**：单测覆盖 9 组场景（StubRuntime/Ollama 构造/APIRuntime 配置校验/load_config 行为/端到端/降级/fallback/advisor/CLI smoke），全部通过
- **不破坏**：v1/v2 任何模块、配置、守护进程都没动，纯增量

## v3-2 落地细节

- **代码**：`scripts/mark42_modules/error_archive.py`（约 470 行，含 ErrorArchive 主类 + 4 状态机 + L3 防护 + CLI）
- **CLI 接入**：`mark42_modules/cli.py` 加 `archive` 子命令（list/show/approve/reject/stats），最小改动不破坏 v1/v2 dispatch
- **验收 5 条**：✅ 写入/查询/授权全部走通（端到端 8 项冒烟全过）；✅ 手动注入 3 条测检索；✅ L3 cooldown (≤5 允许, 第5 warn, 第6 拦截) + hard_blacklist 4 类全拒；✅ 状态机 NEW/RESOLVED/AUTO_APPROVED/REJECTED；✅ 边界（不存在 ID / 非法 scope 不崩）
- **单测**：20/20 全过（含 cooldown 真实语义验证 + 黑名单 4 类）
- **修复 2 个 bug**：
  1. `lookup` 相似匹配只看 signature 前缀 → 跨 category 撞车；改成"前缀相同 + category 相同"
  2. cooldown 阈值 `> 5` 应是 `>= 5`（5 次后立即拦，不让 count 涨到 6 后才拦）
- **不破坏**：entries.jsonl 不存在时返回空（不崩）；v1/v2 任何模块、配置、守护进程没动
- **数据位置**：`~/.local/state/openclaw/mark42/error-archive/`（entries.jsonl + approvals.jsonl 审计 + config.json）

## v3 阶段进度

| 阶段 | 状态 | 备注 |
|---|---|---|
| v3-1 可插拔 LLM | ✅ 完成 | 25/25 单测 + 端到端 |
| v3-2 错误档案 | ✅ 完成 | 20/20 单测 + 端到端 |
| v3-3 战甲意识层骨架 | ✅ 完成 | 28/28 单测 + 端到端 4 项冒烟全过 |
| v3-4 主动交流协议 | ⏳ 待启动 | advisor_client.py |
| v3-5 整合 v2 子 Loop | ⏳ 待启动 | 需先跑稳 v3-3/v3-4 |
| v3-6 真实接 Qwen3-4B | ⏳ 待启动 | ollama 拉模型 |

## v3-3 落地细节

- **代码**：`scripts/mark42_modules/consciousness.py`（约 580 行，含 C1-C5 + §4.5 协作流程 + CLI）
- **CLI 接入**：`mark42.py consciousness {check|eval|handle}` 子命令，最小改动不破坏 v1/v2
- **验收 5 条**：
  - ✅ C1 自检：读 armor/heartbeat/loops/sidecar，不崩，issues 是 list
  - ✅ C2 评估：6 条规则（armor/sidecar/engine/systemd） + 错误档案匹配
  - ✅ C3 100% 自动修：dry-run 默认 + 黑名单 4 类永不过
  - ✅ C4 主动对话：4 类场景话术（100%/已批准/未批准/未知） + 未知自动写档案
  - ✅ C5 错误档案：便捷封装错误档案（v3-2）+ cooldown 透传
- **§4.5 协作流程**：C5 优先 → 100% 走 C3 → 不确定走 C4（新类型自动写档案）
- **单测**：28/28 全过（含黑名单 4 类 + 边界 + 流程）
- **不破坏**：v1/v2 任何模块、配置、守护进程没动

## 累计 v3 进展

- v3-1 + v3-2 + v3-3 三个阶段 = 可插拔 LLM + 错误档案 + 战甲意识 = **v3 三大件已全部就位**
- 剩余 v3-4（advisor）/ v3-5（整合 v2）/ v3-6（真模型）三阶段
- 按 v3 §6 硬规则"每阶段需连续运行 ≥ 24h 不出问题再进下一阶段"，下阶段启动前应等当前代码在实战中跑 24h

## v3 复查审核报告（2026-07-15 14:55-15:05）

点点要求"仔细复查直到全绿"，按 R7 记录全量复查过程和修复。

### 复查方法
1. **三个模块各跑独立边界/异常测试集**（隔离 XDG_STATE_HOME，不污染真实档案）
2. **全量合并测试套件**：`scripts/tests/test_v3_modules.py`（独立可重跑，80 项）
3. **v1/v2 命令烟雾测试**：确保 CLI 接入不破原有命令

### 复查发现的真问题（4 项已修）

| # | 模块 | 问题 | 修复 |
|---|---|---|---|
| 1 | `llm_provider.py` StubRuntime | 收到 `None` content 崩 (`'NoneType' object is not subscriptable`) | 加 `or ""` + `str()` 护栏，R4 确定性 |
| 2 | `error_archive.py` lookup | 精确匹配只看 signature，跨 category 撞车（找 cat_a+sig_b 返回 cat_b 的 entry） | 精确匹配条件加 `if not category or e.category == category` |
| 3 | `error_archive.py` 并发 | 多线程并发 `_rewrite_entries` 报 `tmp.replace` 失败（`No such file`） | 加进程内 `_file_lock` 锁 + `os.replace` 原子替换 |
| 4 | `error_archive.py` cooldown 阈值 | 第一次实现用 `> 5`，count 会涨到 6 才拦 | 改成 `>= 5`，第 5 次 increment（count 仍是 5）即拦 |

### 全量验收（2026-07-15 15:05）

```
总计: 通过 80 / 共 80  🎉 全部通过
```

- v3-1 llm_provider: 18/18 ✅
- v3-2 error_archive: 31/31 ✅
- v3-3 consciousness: 31/31 ✅

### CLI 集成验证（不破 v1/v2）

```
mark42.py status         ✅ 正常（context 5.7%, 4 loop registered）
mark42.py armor --check  ✅ 正常
mark42.py logs --status  ✅ 正常（1650 次轮替）
mark42.py archive        ✅ v3-2 新命令
mark42.py consciousness  ✅ v3-3 新命令
```

### 复查结论

- **v3-1/2/3 三个阶段代码全部可用**
- **未破坏 v1/v2 任何模块、配置、守护进程**
- **所有 R 原则（R4 确定性/R5 边界/R6 dry-run/R7 留痕/R8 不参与最终决策）都守住了**
- **测试覆盖：基础/边界/异常/并发/持久化/审计/CLI/集成 8 类场景**

### 交付物

- `scripts/mark42_modules/llm_provider.py` (350 行)
- `scripts/mark42_modules/error_archive.py` (470 行)
- `scripts/mark42_modules/consciousness.py` (580 行)
- `scripts/mark42_modules/cli.py` (增量加入 archive + consciousness)
- `scripts/tests/test_v3_modules.py` (独立可重跑单测套件，80 项)
- `~/.config/mark42/model.yaml` (默认配置)
- `docs/plans/40-Mark42-OS化深化方案-v3.md` (方案存档)
