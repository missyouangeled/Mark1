# 2026-05-01 周五

## 聊天摘要

1. 用户反馈掌机上的 OpenClaw gateway 只有在接通电源时才会启动，希望改成插电和电池模式下都能正常使用。
2. 已检查掌机（Windows）当前任务配置，确认原生 `OpenClaw Gateway` 计划任务 XML 带有：
   - `<DisallowStartIfOnBatteries>true</DisallowStartIfOnBatteries>`
   - `<StopIfGoingOnBatteries>true</StopIfGoingOnBatteries>`
   这正是“只在接通电源时启动 / 切到电池就停掉”的根因。
3. 由于当前会话没有可用提权能力，暂时无法直接覆盖该原生计划任务；因此先完成了可用性修复：
   - `scripts/openclaw-gateway-watchdog.ps1` 已加入电池限制检测与 direct-wrapper fallback；当 `openclaw gateway restart` 仍无法恢复时，会直接调用 `C:\Users\GOG\.openclaw\gateway.cmd` 拉起网关。
   - `scripts/start-openclaw-gateway-zhangji-windows.ps1` 已加入同样的 wrapper fallback。
4. 已新增掌机（Windows）专用修复脚本：
   - `scripts/repair-openclaw-gateway-battery-policy-zhangji-windows.ps1`
   - `scripts/repair-openclaw-gateway-battery-policy-zhangji-windows.cmd`
   用于后续在可提权环境下，把原生 `OpenClaw Gateway` 计划任务改成电池模式也允许启动、也不会切电池自动停止。
5. 已更新维护说明与工具备注：
   - `docs/掌机-Windows-OpenClaw-维护说明.md`
   - `TOOLS.md`
6. 已验证：脚本语法检查通过，`openclaw.cmd gateway status` 显示当前网关处于 running 状态。
7. Git 进度：
   - 本地提交成功：`40d6fa9` `Fix Windows gateway recovery on battery power`
   - 本地提交成功：`4e82771` `Add Windows gateway battery policy repair helper`
   - 两次 `git push origin master` 均失败，错误为 GitHub SSH 握手中断：`kex_exchange_identification: read: Software caused connection abort`。
8. 用户随后继续反馈：掌机上 OpenClaw 每次启动特别慢。已排查启动日志并确认，先前一大块启动耗时来自 gateway 启动前反复补装 bundled runtime dep：`memory-core` 依赖的 `chokidar@^5.0.0`。原始日志曾出现：
   - `[plugins] staging bundled runtime deps before gateway startup (1 missing, 14 install specs): chokidar@^5.0.0`
   - 单次补装耗时曾达到约 10~18 秒，且整次 `http server listening` 达到约 36.9 秒。
9. 已在本机运行时依赖目录 `C:\Users\GOG\.openclaw\plugin-runtime-deps\openclaw-2026.4.27-58515cad6bdd` 补装 `chokidar@5.0.0`，并完成多次重启复测。修复后日志里不再出现上述 staging / installed bundled runtime deps before gateway startup 记录；`http server listening` 已降到约：
   - 13.0s
   - 12.0s
   后续一次约 19.7s 的重启夹杂了主会话中断恢复与 stale lock 清理，不视为稳定基线。
10. 目前判断：最大的离谱慢启动元凶已经处理掉；剩余基线启动时间大致仍在 12~13 秒，主要落在 4 个插件（`acpx` / `browser` / `memory-core` / `openclaw-weixin`）的初始化链路，其中 `openclaw-weixin` 在 `starting...` 到 `starting HTTP server...` 之间占了明显时长。`startup model warmup timed out after 5000ms` 出现在 `gateway ready` 之后，不是 HTTP server listening 之前的主阻塞项。
11. 用户随后要求执行 `OpenClaw doctor` 做整体验证与自动修复。已实际运行：`openclaw.cmd doctor --fix --yes --deep`。
12. doctor 结果：
   - 完成 plugin registry refresh，显示 `6/118 enabled plugins indexed`
   - 当前 gateway 运行正常，`openclaw.cmd gateway status` 为 reachable / running
   - `openclaw.cmd status` 已可正常输出总览，不再像之前那样直接报 plugin runtime loader 错误
   - doctor 输出中未发现新的致命故障；主要仍是提醒类项目：
     - `No command owner is configured`
     - `AGENTS.md` 注入长度超限，会被截断
     - `Skills status` 中大量 skill 依赖缺失（属于环境完整性提示，不是当前微信通道致命问题）
     - 检测到 1 个 session lock 文件，但对应 pid 仍存活，非 stale
13. 当前结论：OpenClaw 整体链路处于“可用且已恢复到正常诊断状态”，当前剩余的主要优化方向已从“修坏链路”转成“继续压启动耗时 / 按需补 command owner 等治理项”。

## 用户提出的改进/设定

- 用户明确要求：掌机上的网关不能只在接通电源时工作，插电或电池都必须能正常使用。
