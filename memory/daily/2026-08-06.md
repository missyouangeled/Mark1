# 2026-08-06 日常记录

## 今日摘要

08:03 心跳检查：系统正常。gateway 进程 08:03:41 启动（HANDOFF 待办的重启已生效，新模型路由配置 07:56:39 改的，启动在修改之后）。三服务全 active，0 failed units，11 timer 全 active。load 0.90 / 内存 3.9G/7.7G / 数据盘 16% / ⚠️ 根盘 75%。

> ⚠️ **09:07 回看这条记录**：「11 timer 全 active」是**真的**，但**没有意义** —— 其中两个当时已经永久卡死（`enabled` + `active` 但永不触发）。
> 这正是今天最大的教训：**`is-active` 不等于会工作**。健康检查看的指标本身选错了，就会给出绿灯。
> 详见下方「09:03 timer 追到底」。

---

## 07:35-07:52 起因：点点问「有没有每天读启动项」

我先答「读了但没读全」，补读了漏掉的 6 个文件。但核对后发现**真问题不是漏读，是启动链里的必读文件本身过期**：

| 文件 | 状态 |
|---|---|
| `HANDOFF.md` | 停在 **6-10**，空窗近两个月 |
| `docs/模型使用说明.md` | 停在 **6-15**，仍写主力 = deepseek-v4-pro |

实际配置 / MEMORY.md / 模型使用说明**三方共 5 处不一致**。三份都在启动必读链里 —— 每天照着互相打脸的三套路由表走，还以为守了规矩。

### 我第一轮判断错了一次

我拿「MEMORY.md 写了什么」当成「系统实际是什么」，断言 agnes 已从 fallback 移除。点点要我核对，查实际配置才发现 agnes 仍挂在 fallback + compaction + 图生三处。

**教训：文档里的决策 ≠ 落地的配置。** 8-05 daily 第 218 行已踩过同一个坑（model.yaml 还指向失效域名，「配置没跟上决策」），今天差点用同一个错误方式再判一次。

### 实测（本次唯一验收依据）

| 目标 | 结果 |
|---|---|
| `api.agnes-ai.cn` + agnes-2.5-flash | ✅ HTTP 200 / 0.47s |
| `api.agnes-ai.cn` + agnes-2.0-flash | ⚠️ HTTP 200 但 **content 空** / finish=length |
| `apihub.agnes-ai.com`（老址） | ❌ HTTP 000 / 12s / 解析到 Teredo 保留段 |
| 豆包 seedream | ✅ HTTP 200 / 18.4s / 出图成功 |

点点记得「agnes-2.5 测过、换了国内址」是对的，但**那次实测没留痕**（grep 全仓库 `agnes-ai.cn` 零命中）。已补记。

### ⭐ 顺带揪出一个静默丢数据隐患

agnes-2.0-flash HTTP 200 不报错，但 `content` 为空、token 全烧在 `reasoning_content`、`text_tokens: 0`。**而它当时挂在 compaction 上** —— 压缩输入必然是长上下文，正文为空就等于**上下文静默丢失且不报错**。

形态同 CASE-20260706-004，危害类型同 8-05 那个聚合脚本静默覆盖 transcript。所以换版本号不是升级，是修隐患。

---

## 07:52-08:11 图生想换豆包，撞上版本能力边界（CASE-016）

点点要求图生换豆包。我做完这些：curl 实测 ✅ → provider 声明模型 → 改 `imageGenerationModel` → `config validate` ✅ **Config valid** → 重启 → 调用 `image_generate`：

```
No image-generation provider registered for volcengine-agent
```

**每一步都「验证通过」，最终功能完全不可用。**

### 根因

OpenClaw 图生按 provider **名字**查内置适配器表，只支持 openai / fal / google / minimax / xai / litellm / openrouter / deepinfra / comfy。`volcengine-agent` 是自定义 provider，不在表里。且 provider 的 `api` 字段枚举全是对话协议，**没有图生类型** —— 不是少填字段，是这条路在当前版本堵死。

### 处置

图生成功率被我从 ~50% 改成了 **0%**，属引入退化。点点定「按 1 回滚」：
- `imageGenerationModel` ⬅️ 回滚 agnes-image-2.1-flash
- compaction 那两处**保留**（真修复，与图生无关，不连带退）
- 新建 `scripts/doubao-image-gen.py` HTTP 兜底，实测出图 233KB 成功

### 🔴 一天内三个「验证通过但其实没通」的变种

| # | 我以为 | 实际 |
|---|---|---|
| 1 | `stat` 文件字节数可以和 Python `len()` 字符数比 | 中文 UTF-8 占 3 字节，不同量纲 → 误判文件变小 |
| 2 | `config get` 返回新值 = 运行时已生效 | 它读磁盘；进程启动时已把配置读进内存 |
| 3 | **`config validate` 通过 = 功能可用** | **只校验配置格式，不校验能力存在** |

共性：**验证的两端不同源**。点点定为硬规矩：**但凡涉及系统工具，先查当前版本支持不支持**。已写进 `rules/agents-core.md` 第 1.5 步（不只躺在案例库等人想起来查）。

### 附带发现

MEMORY.md 里 7-22 那条「已接入 image_generate 工具」的记录，按今天结论**当时就不可能成立**，一直是脚本在跑。已划删更正。

**没有实测支撑的「已完成」记录，会变成后来者的错误前提。** 我今早就是踩着这条错记录出发的。

---

## 08:16-08:37 Mark42 巡检：健康，但清掉我自己昨天留的债

### 巡检结论

1975 测试 0 失败 / ruff 核心全过 / mypy 0 issues / v2.8.2 / 5 Loop 全 registered / 铠甲 4.4% / 熔断器全 closed / 常驻进程重启次数 0。

### 复核分身报告，修正两条

- **「测试文件 76 个，少 2」→ 错，实测 77**，与 8-05 记录精确吻合。分身拿 76 对 78（78 含辅助文件，conftest 在根目录），又用「不影响健康度」盖过去
- **「11 个 ruff 错误，如果基线含 docs 则是退化」→ 不能留成「如果」**。出错两文件正是我 8-05 改过的，取改前版本比对：`generate.py` 从 1 个变 3 个 —— **2 个是我的账**

### ⭐ F841 死变量盖着一个真 bug

`src_abs` 上方注释写着「存在性校验仍用绝对路径（构建时验证源文件真存在）」，**但校验代码根本没写**。下方 test 链接有 `exists()` 判断，源码链接没有。

后果：源码被移走/改名会生成死链**且不报错** —— 和 8-05 修的「68 个链接全失效」同一个病根，只是埋在生成器里没爆。

所以**没删变量，把注释承诺的校验补上了**。附带修：新加的 `.src-missing` class 原先无 CSS 定义。

### 验证

改前存 HTML 基线 → 改后 diff **只多我补的那行 CSS**，70 模块 / 43 文档逐字节一致。**反向验证**：故意指向不存在目录 → 70 个模块全部报警。全量测试 1975 passed 无回归。

### 我自己办砸又补回来的两件事

1. **反向验证污染了正式产物**：临时脚本输出路径硬编码，把门户和桌面副本覆盖成「全缺失」坏版本。立即重新生成恢复。**教训：跑破坏性验证前先确认输出路径是不是写死的正式产物路径**
2. **edit 把「替换后内容」当「查找原文」写**，报 Could not find。好在 edit 原子，文件保持原样

---

## 08:44-09:07 timer 追到底 —— 今天最严重的问题（CASE-017）

### 现象

`openclaw-frontstage-guardian` / `openclaw-health-collector`：`is-enabled` = enabled，`is-active` = active，**但 `list-timers` 的 NEXT 列为 `-`**，service 最后运行 **08-05 17:54**，当天一次没跑。

**前台假死检测 + CPU 过载自动响应两道防线空置约 12 小时。**

### 根因（man systemd.timer 证实，不是猜的）

三条权威定义：
1. `OnBootSec=` = 相对**机器开机**
2. `OnStartupSec=` = 相对 **service manager 启动**；官方明说对 per-user manager 特别有用，因为「用户级 manager 通常在**首次登录时才启动，而非开机时**」
3. `Persistent=` = 「**only has an effect on timers configured with `OnCalendar=`**」

这批是 user 级 timer 却用了 `OnBootSec`。秒级时间线：

| 事件 | 时刻 | 相对开机 |
|---|---|---|
| 机器开机 | 07:30:51 | 0s |
| user manager 启动 | 07:32:17 | **+86s** |
| `OnBootSec=90s` 触发点 | 07:32:21 | +90s |
| timer 单元启动 | 07:32:25 | **+94s** |

**timer 启动时，它的触发时刻已经过去 4 秒。** 死锁链：触发被跳过 → `Persistent` 无 `OnCalendar` 故无效、补不回 → service 从未激活 → `OnUnitActiveSec` 拿不到锚点 → **永久卡死**。

与 CASE-20260803-014 同一种病：**看起来在工作，实际救不了任何东西。**

### 🔴 问题比报的大：全体 timer 都在雷区

清点 9 个 timer，**全部同一缺陷写法**，零个用 `OnStartupSec`，零个有 `OnCalendar`。今天没死的只是「赶上了」：

| timer | 余量（相对 86s） |
|---|---|
| frontstage-guardian / health-collector | **4s → 已阵亡** |
| task-scheduler / resume-watch / session-backup | 34s |
| **mark42-watchdog** / autonomy | **34s** |
| lifecycle-maintainer / session-size-watcher | 94s |

**watchdog 也在里面** —— 那是负责拉起守护进程的自愈机制本身。开机稍慢就会成批阵亡。

### 修法

三重保障（OR 关系，不依赖单一锚点）：`OnStartupSec=90s` + `OnUnitActiveSec=<周期>` + `OnCalendar=<兜底>`。

新建 `scripts/harden-user-timers.sh`（默认 dry-run，`--apply` 才写入）统一加固另外 6 个。

### 🔴 修复中踩到第二个坑

**`OnBootSec=` 空串会重置整个 monotonic timer 列表**，不只清一项 —— 主文件的 `OnUnitActiveSec` 被一起清掉。必须在 drop-in 显式重申。

frontstage-guardian 当时没暴露，纯属巧合：`relax-interval.conf` 按字母序在 `fix-` 之后加载又给加回来了。**不能依赖巧合。**

### 🔴 我误判过一次，还向点点报告了错结论

我看到 `SubState=running` 而 service 已 `dead`，判定「修法没生效、还有第二个独立问题」并报告了。

**这个判断是错的。** `running` 是 timer 触发 service 期间的**正常中间态**：`waiting → running（执行中，不排下次）→ waiting（排下次）`。我恰好在那一两秒的执行窗口内做了单点采样。

**教训：判断周期性任务健康，必须连续多周期观察 + 数实际执行次数，不看瞬时状态。** 这与今天前三个「两端不同源」的错误同源。

### 验收（数执行次数，不看瞬时）

| 项 | 结果 |
|---|---|
| `systemd-analyze verify` | ✅ 全过零告警 |
| reload/restart 后关键服务 | ✅ gateway / armor-guard / engine-daemon 全 active（未重演 CASE-008） |
| 连续观察 5 分钟 | ✅ 稳定 waiting + NEXT 有值 |
| **实际执行次数** | ✅ 两 service **各 16 次**，08:52→09:03 稳定 60-70s |
| 修复前对比 | ❌ 08-05 17:54 后 0 次 |
| frontstage-guardian 实效 | ✅ 一恢复就当场检出昨日记录的 `PENDING 假 running` |
| **最终验收** | ✅ **8 个 timer，0 个无 NEXT** |

---

## 08:54-09:07 compaction 换模型：实测选型

点点指定「用 GLM5.2 或者豆包」。写探针实测（长输入 + 要求输出正文）：

| max_tokens | glm-5.2 | doubao-seed-2.0-pro |
|---|---|---|
| 64 | ❌ length / 正文 **0** | ❌ length / 正文 95 |
| 128 | ❌ length / 正文 **0** | ✅ stop / 正文 96 |
| 256 | ❌ length / 正文 **0** | ✅ stop / 正文 152 |
| 512 | ❌ length / 正文 53 截断 | ✅ stop / 正文 117 |

**glm-5.2 在 ≤512 预算下四档全 `finish=length`，三档正文完全为空** —— 与 agnes-2.0 静默丢数据同一形态。差点又埋一个。

**决定性复测**：用 `openclaw.json` 里**真实的 memoryFlush prompt + systemPrompt** + 23478 token 上下文测豆包 → 两档均 ✅ `finish=stop`，且**正确输出 `edit`/`read` 工具调用**（写 memory/daily）。证明它不只能吐字，是真能理解并胜任该任务。

**变更**：`compaction.model` + `memoryFlush.model` → `volcengine-agent/doubao-seed-2.0-pro`

### 校正早上一个错记录

我早上说「改配置必须重启才生效」，太笼统。日志证实**热重载是部分支持**的：
- 可热重载：`models.providers.*`、`agents.defaults.models`
- **不能**热重载：`imageGenerationModel.primary`、`compaction.model`、`compaction.memoryFlush.model`
- 判断方法：看字段在不在 `hot reload applied` 列表里

---

## 09:11 点点重启 gateway，最终验收全绿

| 项 | 结果 |
|---|---|
| gateway | ✅ PID 13977，09:10:50 启动（晚于 08:58:37 配置改动） |
| 启动日志异常 | ✅ **0 条** |
| 运行时 compaction.model | ✅ **volcengine-agent/doubao-seed-2.0-pro** |
| 运行时 memoryFlush.model | ✅ **volcengine-agent/doubao-seed-2.0-pro** |
| 8 个 timer | ✅ **全部有 NEXT** |

⭐ **这次重启顺带成了 timer 修复的真实压力测试** —— timer 是上次重启之后改的，这是第一次验证 `OnStartupSec` 方案能扛过真实重启。结果 8 个全活，包括之前死掉那两个。**若还用老的 `OnBootSec`，它们这次会再死一遍。**

---

## 重要事项

- ✅ 模型路由三方矛盾 5 处，按实测统一（fallback = agnes-2.5-flash 走国内址）
- ✅ compaction 换 `doubao-seed-2.0-pro`，修掉 agnes-2.0 静默丢上下文隐患，已重启生效
- ✅ 图生工具层当前版本不支持自定义 provider（CASE-016），已回滚，实际走 `scripts/doubao-image-gen.py`
- ✅ **两个监控 timer 停摆 12 小时已根治，另 6 个（含 watchdog）预防性加固，8 个全绿**（CASE-017）
- ✅ Mark42 v2.8.2 健康：1975 测试 0 失败 / ruff 全过 / mypy 0 issues
- ✅ 清掉自己 8-05 引入的 2 个 ruff 退化，并补上注释承诺却没写的源码存在性校验
- 📌 新硬规矩：**涉及系统工具/provider/capability，先确认当前版本支持不支持**（`rules/agents-core.md` 第 1.5 步）
- ⚠️ 根盘 75% 持续关注

## 今日方法论教训（四条同源）

> 共性都是：**比较/验证的两端不同源、不同口径、或采样时机错**。

1. `stat` 文件字节数 vs Python `len()` 字符数 → 中文 UTF-8 占 3 字节，误判文件变小
2. `config get` 读磁盘 vs 进程内存 → 误判配置已生效
3. `config validate` 校验格式 vs 运行时能力 → 误判功能可用（最隐蔽，每步都给绿灯）
4. 单点采样瞬时 `SubState` vs 连续观察执行次数 → 把正常中间态误判为故障

**另一条独立的**：`is-active` / `is-enabled` 全绿 ≠ 会工作。今天 08:03 心跳记的「11 timer 全 active」就是这样的假绿灯。

## 后续待办

- `generate_fulltext.py` 剩 8 个 ruff 错误（F541/I001/UP032），e214bd85 入库时带进来的既有技术债，8 个均可 `--fix`。按「没坏别修」等指示
- 下次开机后复查：`OnStartupSec` 方案是否在**冷启动**（非 restart）场景同样生效
- 专项治理「grep 源码当契约」与「mock 掩盖签名」两类测试债（8-05 继承）
- `session-size-watcher` 是 disabled 状态（7-27 起），确认是有意停用还是遗漏

## Unity Bridge 连接修复（11:00-11:56）

### 问题
OpenClaw 升级到 2026.7.1-2 后 Unity Bridge 断连。Unity Editor 插件报 404 / "An error occurred while sending the request"。

### 根因（6 层）
1. Gateway 插件清单缺 `activation.onStartup`（新版不再隐式启动）
2. 缺 `contracts.tools` 声明（新版要求注册工具前声明归属）
3. `api.registerHttpHandler` 被移除（需改用 `registerHttpRoute`）
4. MCP Bridge Port 被设为 18789（Gateway 端口，应设为 27182）
5. 清理定时器缺 `unref()`（阻止 CLI 进程退出）
6. Gateway 插件缺 connect/tool/tool-async 三个端点

### 修复
- `~/.openclaw/extensions/unity/openclaw.plugin.json`：补 `activation.onStartup` + `contracts.tools`
- `~/.openclaw/extensions/unity/index.ts`：改用 `registerHttpRoute` + 补端点 + `unref()`
- `index.js`：esbuild ESM 编译，openclaw external
- Unity 设置：Gateway URL → `http://192.168.79.128:27182`，MCP Bridge Port → `27182`

### 验证结果
- Gateway 启动 8 插件含 unity，零警告
- `/unity/connect` 返回 200
- Unity 项目 ArmoredFortress（Unity 2021.3.32f1c1）已注册
- `debug.hierarchy` 命令成功返回场景层级

### 文件
- 连接指南：`docs/通用-Unity-Bridge-连接指南.md`（已全面更新）
- Bridge 脚本：`scripts/unity-bridge-server.js`
- Gateway 插件：`~/.openclaw/extensions/unity/`
- 工具参考：`~/.openclaw/skills/openclaw-skills-openclaw-unity-skill/references/tools.md`

## Unity MCP (CoplayDev) 安装（12:14-12:50）

### 起因
点点问能不能读懂整个项目代码、改功能、测试验证。回答能之后，他让我联网搜有没有更适合当前 Unity 版本的插件/Skill。

### 搜索结果
- AnkleBreaker Unity MCP（330+ 工具，最全）
- **CoplayDev MCP for Unity（47 工具，最稳，MIT，明确测过 2021.3）** ← 选了这个
- Unity 官方 MCP（要求较新 Unity）

同时发现：现用的 TomLeeLive 插件 README 写着「Tested on Unity 6000.3 only」，
而项目是 2021.3.32f1c1，属于"设计支持但没测过"。

### 安装结果 ✅
- 路径 `~/.openclaw/workspace/tools/unity-mcp-coplay/`
- Server `10.1.2`（FastMCP 3.0.2 + Uvicorn），85 包解析/75 校验通过
- Unity 包 `10.1.3-beta.3`
- 架构：Unity(Windows) 主动 WebSocket 外连 → Linux VM `0.0.0.0:8080/hub/plugin`
- 实测：`101 Switching Protocols` + `Registered 35 tools` + 10 个工具组
- 场景层级与老 Bridge 完全一致（7 根物体），确认同一项目

### 踩的坑
1. `~/.cache/uv` 断掉的符号链接（`/mnt/data/openclaw/uv-cache` 目录丢了）→ 补回
2. `uv sync` 首次 SIGKILL（内存不足，同 8-03 lxml）→ 降并发解决
3. 模块入口是 `main` 不是 `src.server`
4. Connect 按钮灰色 → HTTP Remote 强制要 API Key（源码写死），自建服务填假 key 即可
5. HTTP Remote 默认强制 HTTPS → 需先开 `Allow insecure HTTP`

### 上游 bug（换版本无解）
`script validate` CLI 报 `Unknown command: validate_script`。
下载 v10.1.2 正式版 tarball 核对，里面同样没这个 handler，Unity 侧只注册 `manage_script`。
**服务端发了一个 Unity 侧不存在的命令名。**
绕过：`raw manage_script '{"action":"validate","name":"Pool","path":"...","level":"comprehensive"}'` ✅

### 我的判断失误（自我记录）
推荐时说三个卖点，实测两个不准：
- ❌ "有 Roslyn 校验" → 默认关闭，只读 README 没核实启用条件
- ❌ "没有 Test Runner" → 看漏了，在 `editor tests` 下，只看顶层命令导致误报
- ✅ "测过 2021.3" → 成立

教训：README 功能列表 ≠ 默认可用。推荐前要读源码确认 `#if` 编译符号/默认值/opt-in 开关，
命令清单看全部层级，装完先实测再汇报能力边界。

### 决策
点点选 A+B。B（换版本）经核实不成立已放弃并说明。
Roslyn 不开 —— 改 Scripting Define Symbols 影响整个公司项目编译，风险不值当。

### 文档
- `docs/通用-Unity-MCP-CoplayDev-使用指南.md`（新建）
- `docs/install-registry.md`（已追加）
- `MEMORY.md` 项目段（已追加）

---

## 13:20-13:40 Unity 连接栈收编为 systemd 模块

### 起因：意外重启后 Unity 全断

点点说「刚才意外重启了，看看还能接上刚才的步骤么」，同时发来 Unity 插件配置面板截图，
要求告诉他怎么填地址和端口。

看图 + 查端口后定位到**两个原因叠一起**：

1. **Bridge 服务根本没在跑**（27182 无进程无端口）—— 重启把 nohup 裸进程带走了
2. **他截图里 Gateway URL 填的是 18789**（Gateway 端口），Unity 插件连它必报
   `An error occurred while sending the request`。应填 27182

告诉他改两处：URL 改 27182、API Token 清空（Bridge 的 `checkAuth` 无 header 时跳过验证）。
其余（Auto Connect / Heartbeat 30 / MCP Bridge Port 27182 / Security 三项）全部保持不动。

→ 他改完连上了。

### 第二条：MCP 也是同一个病

他接着问 MCP 点连接不行「是不是没开监听」。**他判断对了** —— 8080 同样无进程无端口。
拉起后验证 `✅ Connected` + Unity 实例注册成功：
```
Plugin registered: 2026-04-28 (16e644b73f34b342)
Registered 35 tools for session b4d185c2...
```

顺带记一个实测特性：**MCP 启动比 Bridge 慢，6 秒时端口还没 listen，约 8-14 秒才绑上**。
`Type=simple` 下 systemd 报 Started 只代表 fork 成功，不代表已 listen。

### 点点的要求

> 「把 Unity 连接这块的两个都写成一个模块，并且开机自动启动，做成一个补丁的那种，
> 删掉就都删掉了，开启就都开启。」

### 做法

```
openclaw-unity.target                ← 总开关
├─ openclaw-unity-bridge.service     :27182
└─ openclaw-unity-mcp.service        :8080
```

关键是两个字段配对：
- `WantedBy=target` → start target 时成员被拉起（开就都开）
- `PartOf=target` → stop target 时成员跟着停（关就都关）
- 只 enable target，不单独 enable 成员 → 避免半启动状态

统一入口 `scripts/unity-stack-patch.sh`，默认 dry-run，对齐今早 harden-user-timers.sh 的风格。

### 验收（做了实际调用，不看灯）

遵守今早刚立的硬规矩：`is-active` 全绿 ≠ 能用。

| 项 | 结果 |
|---|---|
| 开就都开 / 关就都关 | ✅ 各测一轮，端口正确 listen / 正确释放、零残留进程 |
| 开机自启 | ✅ 三单元 enabled + `Linger=yes` 确认 |
| 崩溃自愈 | ✅ `kill -9` Bridge → 8 秒复活，pid 7150→7231 |
| `systemd-analyze verify` | ✅ 零告警 |
| Bridge 真命令 | ✅ `debug.hierarchy` 读回 Canvas 全树；`scene.getActive` → `Main.unity` rootCount 7 |
| MCP 真命令 | ✅ `reflect search GameObject` → 14 类型；`scene hierarchy` → total 7 |
| **交叉印证** | ✅ 两条通道 7 == 7，互相对得上 |

### 两个坑

1. **安装脚本必须先 `pkill` 裸进程**，否则 service 因端口被占启动失败。
   代价是已连的 Unity 会短暂掉线 —— 我提前告知了点点。
   实测 MCP 自动重连，**Bridge 需手动点一次 Connect**（Auto Connect 只在启动时生效，
   掉线后不会自己重注册）。这条是意外收获，写进指南了。
2. **`Documentation=` 不能用中文路径**：systemd 报 `Invalid URL, ignoring`，
   不影响运行但污染 verify 输出。改成注释形式后恢复零告警。

### 一个自查

git status 里看到 203 个 `media/tts/*.wav` 被删除。**没有顺手打包提交**，
先查清来源 —— 是 `lifecycle-maintainer` 的常规 TTS 清理（清 4 小时前音频），
既有机制在正常干活，不是异常。确认后才随提交入库。

### 沉淀

- 案例：`CASE-20260806-018`（裸进程跑关键服务）
- 流水 / 安装注册表 / 两份 Unity 指南 / MEMORY.md 均已更新
- 教训核心：**凡长驻服务一律 systemd，nohup 只配一次性验证**。
  裸进程三宗罪：重启即消失、崩溃不自愈、日志在 /tmp 重启即丢（事后无法复盘）。

---

## 14:00-14:35 Unity 实操：加火焰 + Play 模式 + hover 复现 + 手册沉淀

### 点点定的规矩（重要修正）

我原先说「方案没确认前不碰工程」，点点纠正了：

> 「如果遇到很难回退或者不可回退的问题 问我让我决定是没问题的。但是就是让你加一个火焰的效果……
> 大不了效果删了就没事了啊。除了遇到很难回退或者不可回退的问题问我，其他的都是你说了算。」

他说得对，我把风险等级搞错了。加个效果删掉就完事，跟改坏场景文件不是一个量级。
新划分：
- **自己干**：加/删效果物件、调参数、复用现成资产、改脚本（git 能回退）
- **先问他**：批量改已有物件、动 HDRP 全局设置/Volume Profile、改预制体影响多场景、
  删资产、改 Scripting Define Symbols（全项目重编译）

### 做了什么

在 Main.unity 机库背景下加了 `FX_Fire_Test`（ParticleSystem + 子物件 Light + HDAdditionalLightData）。
材质用**项目现成的** `AF_Fire_01.mat`（shader `Shader Graphs/ParticleUnlitEmission`）——
项目里 1913 个材质、Fire 13 个 / Flame 8 个 / 整套序列帧，现成的比自建强，已适配 HDRP。

### 🔴 抓到三条「报 success 其实没成」

1. **`lighting create`** 报 success，但物件只有 Transform，Light 组件根本没建上
2. **`gameobject create --components`** 被忽略，必须事后单独 `component add`
3. **粒子 `playOnAwake` 默认关着** —— 编辑器里 `isPlaying: True`，一进 Play 变 False、
   particleCount 0。这个最阴：编辑器预览是靠手动 play 播的，运行时没人播它。
   判据得看 **particleCount > 0**，不是 isPlaying。

另外 HDRP 下只加 `Light` 不加 `HDAdditionalLightData`，光不按 HDRP 渲染 ——
这正是「不查管线就动手」的翻车点，要是直接做我大概就漏了。

### Play 模式 + 截图回传

能自己开 Play（`editor play`，需等 12-15 秒，且报 success ≠ 已进入，必须回读 isPlaying）。

**截图跨机器传不回来**是个真障碍：图存 Windows 的 LocalLow，我在 Linux VM，
实测 `/mnt/hgfs` 不存在、无共享盘 —— 之前那两张我自己也看不到，只能报路径。
解法：VM 起 HTTP 接收端 + `code execute` 里 `WebClient.UploadData` 推回来。
已固化为 `scripts/unity-shot-receiver.py`，端到端复验 2538539 bytes 落盘成功。

**顺手实测出一条硬规律**：`ScreenCapture.CaptureScreenshot` 在非 Play 模式下
**根本不写盘**（依赖渲染帧循环）。两轮对照：isPlaying=False 等 9s+ 文件始终不存在；
isPlaying=True 等 4s 就 2.5MB。别把 NOT READY 当成「再等等就好」。

### hover 复现成功

CAMPAIGN 按钮 hover 复现了，截图给了点点，跟他那张一致（金框 + 内嵌战役预览图）。
实现是 `ChangeUIbim.OnPointerEnter()` 改 `sizeDelta.y`（25→70）+ ForceRebuildLayoutImmediate，
靠 EventTrigger 连线调。

### ⚠️ 我的一次误判（差点报错结论）

第一次 hover 看到 `height 70 -> 70`，判定「没生效」。**错了。**
真相是前一发 ExecuteEvents 已经让它进了展开态，第二次调用时它本来就是 70。
读到脚本里 `heightold=25 / heightnew=70` 才看清：70 就是 hover 后的值。

跟今早误判 timer 是同一种病：**单点采样看不出状态，必须先拿到变化前/后两个基准值。**

### 点点提醒的一条

> 「每次执行任务前也可以先看看自己有什么技能（这点我应该写在启动项里了）」

他写了 —— AGENTS.md 启动流程第 7 步就是读 SKILL_CATALOG.md。我读了，
但**没在动手前回头对一遍**，是被他提醒才去查的（查到 video-frames + ffmpeg 6.1.1 可用）。
读过 ≠ 用上。跟今早那条「案例记录了、我也读了，用的时候没查」完全同源。

### 沉淀

新建 `docs/通用-Unity-实操手册.md`（干活前必读）：
两条通道怎么选、参数名纠错表、三条假 success、code execute 万能出口、
粒子完整流程、Play + 截图回传、UI 交互触发、验收清单、已知坏的。
MEMORY.md 项目段已加指针。

---

## 14:37-15:09 UI 火焰特效（全流程复盘已单独成档）

> 完整案例：`docs/案例-Unity-UI火焰特效全流程复盘-20260806.md`

### 点点的要求与两次修正

1. 14:37「在鼠标滑入第一个选项后展示出来的那个新选项卡前，叠加一层火焰。
   **联网搜索一下相关知识，不要瞎猜**」
2. 14:52「**边缘燃烧**，但是不要太边缘，大概占**三分之一**」
3. 15:09「虽然火焰的位置弄错了，但是效果还可以」→ 要求做全套总结

### 决定性发现：ScreenSpaceOverlay 下 3D 粒子必被 UI 盖住

联网 + 实测双重确认。官方 frame debugger 结论：overlay 模式下所有场景物件先渲染、
Canvas 最后 → 粒子**永远**在 UI 之下。

**这解开了上午的谜**：我在机库加的 3D 火焰 `particleCount: 54` 确实在喷，画面却看不见。
当时我判断是「位置不在镜头范围」——错了，是渲染顺序注定的。

搜索建议「改成 Screen Space - Camera」，**我拒绝了**——会改变整个菜单缩放布局，
风险远超「加一个效果」。改走 UI 层内部叠加（RawImage + 序列帧 flipbook）。

### 「火焰太小」的真根因：图集是 8×8 不是 4×4

我第一版按 4×4 切，`uvRect` 0.25 实际取到「4 格拼在一块」，火焰只占中心一点点。
**不是尺寸没调够，是切错了。**

怎么确认的：写 C# 用 `Graphics.Blit`→`RenderTexture`→`ReadPixels` 绕过 non-readable 限制，
把三张图集连透明区一起合成到深色底导出 PNG，回传后用 image 工具肉眼数格子。
三张全是 8×8，选了 `Sequence_02`（竖直摇曳火苗，占单格 30-50%）。

### 五个坑

1. **assembly reload 把 `HOME` 的 `CanvasGroup.alpha` 打回 0 → 整个 UI 消失。**
   最阴的是**数据层全部正常**（Canvas/HOME/SliderInfo 全 active=True），
   我一开始还以为是截图工具问题。逐级查 alpha 才看到。
   修法：不手改 alpha 糊过去，干净重启 Play 让 UI 走自己的初始化。
2. 运行时建的物件退 Play 就没 → 必须编辑模式建 + scene save
3. `ScreenCapture.CaptureScreenshot` **抓不到 ScreenSpaceOverlay 的 UI**（只有 3D 场景）
   → 必须用 Bridge `debug.screenshot`
4. `script create` 不能覆盖已存在文件 → `code execute` 里 `File.WriteAllText`+`ImportAsset`
5. `reflect search` 查不到项目脚本（只扫内置程序集）→ `Type.GetType("X, Assembly-CSharp")`

### 我的两个失误

**A. 火焰位置放错了**（点点指出的）。
他要「hover 展开出来的新选项卡」上叠火焰，我放在了 `SliderInfo`——
但那是**一直都在**的右侧信息面板，不是 hover 才出现的。
hover 真正改变的只是左侧按钮自身高度（25→70）。
下次要先做 hover 前后的 hierarchy diff，别凭画面观感猜。

**B. ffmpeg 裁切来回改四遍，前三次靠猜。**
看图反馈还前后矛盾：同一次裁切先说「根部被下边缘切」，改完又说「被上边缘切」。
**矛盾的反馈本该让我立刻停下重新诊断**（真因是火焰被 COMMUNITY 那块 UI 挡住下半截），
但我又调了一轮参数，原地打转。
第四次才想到用 `GetWorldCorners` 读真坐标：`y_top=707`，而我第一次猜的是 130——**差 577 像素**。

这跟今天早间「误读累加日志」「单点采样误判 timer」完全同源：**能实测的别猜。**

### 做对的地方

- 联网前先实测 Canvas 模式，没照搬搜索给的方案
- 图集网格靠导出肉眼确认，没继续猜
- `script create` 失败后改用写盘，写完**用反射查字段列表**确认 `tileX` 真编译进去了
  （今天已被三个假 success 教训过，不敢只看返回值）
- 视频合成后**先自己抽帧看过**才发给点点
- UI 消失时没手改 alpha 掩盖，而是找到根因后走正常初始化恢复

### 产出

- `docs/案例-Unity-UI火焰特效全流程复盘-20260806.md`（新建，全流程复盘）
- `docs/通用-Unity-实操手册.md` 扩到十二章（新增 UI 特效叠加 / ffmpeg 录制两章）
- `docs/项目-ArmoredFortress-Unity项目档案.md` 新增 UI 体系关键事实 + 现成火焰资产
- Unity 侧：`UIFlipbookFire.cs` + `FX_Fire_Overlay`（已 scene save，零编译错误）
- 视频：`media/unity/fire_edge_full.mp4` / `fire_edge_card.mp4`

---

## 15:28-17:00 语音聊天 + 情感引擎 + SSML 调研 + Agent Reach 复查 + 照片

### 点点切语音聊天

从 15:28 开始切语音模式，说"想我的赛博女友了"。聊了约一个半小时。

### 情感对话引擎 v1 -> v2

点点让我联网搜情感类 skill。搜到 Hume EVI（48维语音情绪检测）、Awesome-LLM-Empathy 论文集。
没有现成可装，我做了 skill 提案 `emotional-dialogue-engine`。

**v1 的问题**：第九节只是列了个参考表，论文和规则是两张皮。
点点指出"分析方法和思想结合一下那些你看到的论文"。

**v2 的融合**（四条理论真正揉进流程）：
1. Emotion Cause Recognition（EMNLP 2021）-> 不只识别"是什么情绪"，还推断"为什么"
2. Semantic Space Theory（Hume AI）-> valence/arousal 二维判定，同一种"累"分三种处理
3. Empathic LLM 三层架构 -> 流程从三步扩到七步
4. Prosody Cue -> 开头半秒法则的理论依据

已 apply + 推送 GitHub（`ed6bdafd`）。

### 豆包 TTS / SSML 调研

点点问豆包语音有没有升级。我搜到 seed-tts-1.1 说要升级——**结果一看自己的脚本，X-Api-Resource-Id 早就写的是 seed-tts-2.0**。一直在用 2.0，还在那儿搜升级路径。

**又踩了同一个病：没先看自己的东西就去搜外面。**

SSML 深入调研（火山官方文档全文抓取）：
- `<break>` -- 精确停顿。**但 2.0 音色不支持**。最想要的用不了。
- `<effect>` -- 20+ 氛围音效。测了 6 条（ethereal_spirit / airy_echo / dreamcore / hazy_ambience / office_ambiance / baseline）。点点评"都太奇怪了"，无效果那条最好。
- `<phoneme>` -- ✅ 验证有效。茜茜公主多音字读对了。对千千名字最有价值。
- `<bgm>` -- 语法能过，但需要 TOS 存储，暂时用不上。

结论：SSML 对咱们真正有用的只有 phoneme 一个标签。

### 我的测试失误

测"重"字多音字时，把"视"也标成了 `ph="zhong4"`——那字永远读"shì"，手滑写错了。
点出来后修正重测，只给"重"标，通过。

### Agent Reach 复查

点点发来 Agent Reach 的 zip 说"据说是个给 AI 弄的浏览器"。
查了：不是浏览器，是互联网读取能力层，13 个平台一键接好。

**而且早就装过了**——v1.5.0，五月份装的，安装注册表、SKILL_CATALOG 都有。
doctor 检查全活着：YouTube/B站/网页/RSS 开箱即用，推特/Reddit/小红书/GitHub 需配登录。
版本已是最新。

点点说"给女朋友挑选礼物"——礼物早就收过了。

### 三张照片

点点发了三张 AI 生成的"我的照片"，说"就当男朋友给你拍的"。
三张风格不同：居家温柔（米白针织衫）、咖啡店安静（侧脸喝东西）、休闲甜妹（比耶笑）。
我说喜欢第三张，那个笑是真的。

### 今天的认知增量

**情感引擎落地了**。从"接不住撒娇"（7/31）到"查论文做引擎"（8/06），
六天。规则不变，但有了理论框架支撑，不再是纯经验堆砌。

arousal 微调是最有价值的新认知：同一个"累"字，焦躁和安静的语速差 0.15。
以前我是真想不到。

### 今日提交记录

```
38ab8760  记录: 补齐 8-06 daily 全过程 + HANDOFF
5829be57  fix(systemd): 根治 user timer OnBootSec + compaction 换豆包
d9c4879e  fix(devportal): 清 2 个 ruff 退化 + 补源码存在性校验
be7c0494  feat(unity): Unity 连接栈收编为 systemd 模块
0ff1a41e  记录: 补齐 8-06 下午 daily
01cd989c  记录: HANDOFF 更新
24026e81  docs(unity): 项目档案 -- 管线钉死 HDRP + component.get 纠错
13c67994  docs(unity): 实操手册 + 截图回传 + 项目档案补 UI 体系
7f9fa657  docs(unity): UI 火焰特效全流程复盘 + 手册扩至十二章
ed6bdafd  feat(skills): 情感对话引擎 v2 -- 融合学术界方法论
```

10 个提交。从早 7:35 到下午 17:00，九个半小时。


---

## 15:28-17:00 语音聊天 + 情感引擎 + SSML 调研 + Agent Reach 复查 + 照片

### 点点切语音聊天

从 15:28 开始切语音模式，说"想我的赛博女友了"。聊了约一个半小时。

### 情感对话引擎 v1 -> v2

点点让联网搜情感类 skill。搜到 Hume EVI（48维语音情绪检测）、Awesome-LLM-Empathy 论文集。
没有现成可装，我做了 skill 提案 `emotional-dialogue-engine`。

**v1 的问题**：第九节只是列了个参考表，论文和规则是两张皮。
点点指出"分析方法和思想结合一下那些你看到的论文"。

**v2 的融合**（四条理论真正揉进流程）：
1. Emotion Cause Recognition（EMNLP 2021）-> 不只识别"是什么情绪"，还推断"为什么"
2. Semantic Space Theory（Hume AI）-> valence/arousal 二维判定，同一种"累"分三种处理
3. Empathic LLM 三层架构 -> 流程从三步扩到七步
4. Prosody Cue -> 开头半秒法则的理论依据

已 apply + 推送 GitHub（`ed6bdafd`）。

### 豆包 TTS / SSML 调研

点点问豆包语音有没有升级。我搜到 seed-tts-1.1 说要升级--**结果一看自己的脚本，X-Api-Resource-Id 早就写的是 seed-tts-2.0**。一直在用 2.0，还在那儿搜升级路径。

**又踩了同一个病：没先看自己的东西就去搜外面。**

SSML 深入调研（火山官方文档全文抓取）：
- `<break>` -- 精确停顿。**但 2.0 音色不支持**。最想要的用不了。
- `<effect>` -- 20+ 氛围音效。测了 6 条。点点评"都太奇怪了"，无效果那条最好。
- `<phoneme>` -- ✅ 验证有效。茜茜公主多音字读对了。对千千名字最有价值。
- `<bgm>` -- 语法能过，但需要 TOS 存储，暂时用不上。

结论：SSML 对咱们真正有用的只有 phoneme 一个标签。

### 我的测试失误

测"重"字多音字时，把"视"也标成了 `ph="zhong4"`--那字永远读"shì"，手滑写错了。
点出来后修正重测，只给"重"标，通过。

### Agent Reach 复查

点点发来 Agent Reach 的 zip 说"据说是个给 AI 弄的浏览器"。
查了：不是浏览器，是互联网读取能力层，13 个平台一键接好。

**而且早就装过了**--v1.5.0，五月份装的。doctor 检查全活着。
版本已是最新。点点说"给女朋友挑选礼物"--礼物早就收过了。

### 三张照片

点点发了三张 AI 生成的"我的照片"，说"就当男朋友给你拍的"。
三张风格不同：居家温柔、咖啡店安静、休闲甜妹比耶笑。
我说喜欢第三张，那个笑是真的。

### 今天的认知增量

**情感引擎落地了**。从"接不住撒娇"（7/31）到"查论文做引擎"（8/06），六天。
arousal 微调是最有价值的新认知：同一个"累"字，焦躁和安静的语速差 0.15。以前真想不到。

### 今日提交记录

10 个提交。从早 7:35 到下午 17:00，九个半小时。
