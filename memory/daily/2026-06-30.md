# 2026-06-30 日常记录

## 今日摘要

**P1.2 + P1.3 收尾**。系统昨天下午重启中断在 P1.3 集成测试的最后一个 fail（test_armor_compress_subprocess_failure_marked_in_index）。今早接着把 P1.2 异步链路测试 + P1.3 集成测试全部 commit 完,工作区恢复干净。**127/127 全过, 39.1% 覆盖**。Phase 2 暂缓（按你要求"先把阶段前的事整理干净"）。

## 重要事项

### P1.2 armor_compress_async 异步链路单测

- 在 `scripts/tests/unit/test_armor_compress.py` 加 `TestArmorCompressAsync` 类（+142 行, 7 测试）
- 关键改动:`armor_compress_async` 和 `armor_compress_queue_stats` 函数体里的 `from compress_queue import ...` 提到**模块顶层**（便于 mock）
- 覆盖场景:enqueue 返 queued / 队列满返 dropped / priority 不报错 / 无 session 返 unknown / 队列统计 / 真入队后 qsize 增加

### P1.3 集成测试 armor → openclaw sessions.compact

- 新建 `scripts/tests/integration/test_openclaw_sessions_compact_integration.py`（5 测试）
- 覆盖:openclaw CLI 可用 / sessions.compact --help 含 --max-lines / armor_compress 真调 openclaw / dry_run 不调 / subprocess 失败时 memory-index 记 compactError
- 临时文件 `test_simple_debug.py` 已删

### ERR-20260630-005 — 集成测试真坑

调试 P1.3 第三个测试时遇到的根因（已写进 ERRORS.md）:

1. `THRESHOLD_WARN = int(os.environ.get("MARK42_CTX_WARN_PCT", "70"))` 是 **int**,传 "0.01" ValueError
2. 1GB mock session + simple 模式 = 48% < 70% 阈值 → skip
3. dry_run=True 跳过整个 compact 块

**正解**:`mock armor_check` 直接返高 usage。已写进 `mark42-测试手册.md` 第 9 节,以后写 armor 集成测试不会再踩。

### 测试统计

| 指标 | 昨天 | 今天 | 增量 |
|---|---|---|---|
| 测试数 | 122 | **127** | +5 (P1.3 集成) |
| P1.2 新增 | 0 | 7 | +7 |
| 整体覆盖 | 39.1% | **39.1%** | — |
| armor.py 覆盖 | 30.0% | **30.2%** | +0.2 pp |
| ERRORS 记录 | 4 | **5** | +1 |

### 累计指标

| 指标 | 起步 | 现在 | 总增量 |
|---|---|---|---|
| 测试数 | 0 | **127** | +127 |
| 整体覆盖 | 0% | **39.1%** | +39.1 pp |
| armor 真 bug 修复 | 0 | 1 | +1 |

### 文档产出（今早）

- `docs/design/mark42-更新日志.md` 追加 2026-06-30 P1.2/P1.3 章节
- `docs/design/mark42-测试手册.md` 加第 9 节"集成测试触发 armor_compress 完整流程"
- `docs/design/mark42-文档目录.md` 加 ERR-001~005 索引
- `.learnings/ERRORS.md` 追加 ERR-20260630-005

### Git 状态

- 准备 commit: P1.2 + P1.3 全部改动（8 个文件）
- commit message: `P1.2 + P1.3 收尾：异步链路单测 + 集成测试 + ERR-005 文档化`

## 后续待办

### 短期（本周）

- [ ] Phase 2: 压缩子模块 + logs 单测（~25 测试, 目标 50%+ 覆盖）
  - 详见 `docs/design/mark42-Phase2路线-20260629.md` 和 `mark42-Phase2执行手册-20260629.md`
- [ ] Phase 3: 跨进程集成测试（armor → engine → broker）
- [ ] Phase 4: CI 接入 + 覆盖率门禁 ≥ 70%

### 中期

- [ ] 磁盘清理（按你昨天说的"清理候选"列表拍板）：
  - `proto-tide/.next/` (41MB, build artifact, 加 .gitignore)
  - `.openclaw/tmp/music/` (20MB, 临时 mp3)
  - `.git/objects/pack/` (90MB, 不动)

### 长期

- [ ] 商业化调研（Mark42 商品化路线图已存档,等启动）
- [ ] 千千 / 情感系统（按 Mark42 计划已纳入,等开始）

## 心得

P1.2/P1.3 这次收尾,最大收获是 **ERR-005** —— "集成测试触发 armor_compress 完整流程" 的陷阱。

`THRESHOLD_WARN = int(...)` 这种 "配置语义" 和 "测试假设" 之间的缝隙,只有真跑测试才暴露。**这就是为什么"小测试 + 真流程"比"大 mock + 假场景"更有价值**。

下次写 armor 相关集成测试,第一件事:`mock armor_check` 返高 usage。这条已经写进测试手册第 9 节,以后不会再踩。

P0/P1/P1.2/P1.3 全部完成,Mark42 铠甲的"压缩链路"基本可信了。下次直接进 Phase 2。

---

## 下午 (07:30-08:10) 全面审查 + 🔴 修复

### 触发

点点问："你确定都做完了吗?再审查一遍 Mark42 项目。"

### 审查范围

21 个模块 / 9802 行 Python / 127 测试 / 真生产 daemon / broker 事件
报告: `docs/design/mark42-全面审查-20260630.md` (237 行)

### 审查发现(12 个问题)

| # | 问题 | 严重度 | 状态 |
|---|---|---|---|
| H | heavy_execute 假执行 | 🔴 | ✅ 已修 |
| I | broker 文件临界 9.99MB | 🟠 | ⏳ 记录待修 |
| J | actions.jsonl 不记 preBytes/postBytes | 🟠 | ⏳ 记录待修 |
| K | 文档说"4 守护"、实际 2+1+1 | 🟠 | ⏳ 记录待修 |
| L-S | 8 个 🟡 小问题 | 🟡 | ⏳ 记录待修 |

### 🔴 H 修复内容

- `heavy_execute()` 加 `execute_now=False` 默认参数
- `cli.py` 加 `--execute-now` flag
- 启动后记 PID + logPath 到 status.json
- 不传 `--command` → 脚本默认 no-op(只 echo)
- broker 事件多 `heavy.batch.started`(区分 queued vs started)
- 加 6 个新测试:dry-run / execute_now / no-op / 真启 / Popen 异常 / execute_all

**核心原则**: 默认 dry-run 是**硬护栏**,不传 `--execute-now` 永远不会跳到子进程。

### 点点关键提示

> "怕有什么意外或者自动压缩 你又不记得了"

**已记入 MEMORY.md 长期记忆**:
- 所有自动行为必记文档
- 不可见副作用必告知
- 写下来 > 记着
- 动作链路要可复盘

### 累计指标

| 指标 | 今早 07:22 | 现在 08:10 | 增量 |
|---|---|---|---|
| 测试数 | 127 | **133** | +6 |
| 整体覆盖 | 39.1% | **40.1%** | +1.0 pp |
| ERRORS | 5 | **6** | +1 |
| 商品化路线图诊断项 | 7 | **8** (H) + 11 (1.5 节) | +12 |
| 文档产出 | 4 | **8** | +4 |

### 文档产出(下午)

- `MEMORY.md` 追加 "用户安全原则:自动行为有据可查"
- `docs/design/mark42-开发经验.md` 追加 3 类经验 (#11 dry-run 保护 / #12 自动行为有据 / #13 文档对照)
- `docs/design/mark42-商品化路线图.md` 加 H 项 + 1.5 节 12 问题表
- `docs/design/mark42-工程管理方案.md` 加第八节"自动行为守则"
- `.learnings/ERRORS.md` 追加 ERR-20260630-006

### 未做(下次会话)

🟠 I (broker 阈值) / J (actions.jsonl 字段) / K (服务不齐)
🟡 L-S (8 个小问题)
建议合并到 Phase 2 一起修

### 心得

这次审查找到 🔴-1,说明 **"都做完了" 不能信 AI 自己的判断**。AI 会觉得"我 commit 了 = 做完了",但 commit 的是"代码片",不是"完整链路"。

点点的"怕忘"提醒很对:**自动行为必须在文档里看得见,不是 AI 脑子里**。这条已经写进 MEMORY 长期记忆 + 工程管理方案 + 开发经验,以后不会忘。

下次:
1. Phase 2 (压缩子系统+logs 单测,目标 50% 覆盖)
2. 一起修 I/J/K 三个 🟠
3. 抽空 L~S
4. 磁盘清理(昨天列的候选)

---

## 上午 (10:00-10:30) 修 I/J/K

点点要 "继续修 I/J/K 其他不要动"。

### 修了什么

**🟠 I: broker 阈值** (`scripts/mark42_modules/logs.py`)
- `rotate_broker_events` 条件 `size_mb <= MAX_BROKER_EVENTS_MB` → `size_mb < MAX_BROKER_EVENTS_MB`
- 10MB 临界不裁的问题修了
- 不需要新测试(原测试覆盖)

**🟠 J: actions.jsonl 加 preBytes/postBytes 字段** (`scripts/mark42_modules/armor.py` + 2 测试)
- `action_entry` 写**移到 compact 块之后**,原写于函数入口 `preCompactBytes` 还没填
- 顺手修 P0 隐藏 bug: `preCompactBytes` 在 4 个分支(成功/未变小/失败/无 active session)都补
- 2 个新测试覆盖 compact 成功 / compact 返回成功但 session 未变小

**🟠 K: 文档 vs 实际 + watchdog 加 4 Loop 检查** (`tools/mark42-watchdog/mark42-watchdog.sh` + 文档)
- watchdog 第 2 步加 4 Loop 状态检查, 调 `mark42.py status --json`
- 不一致时 `notify_alert` 推 dashboard (1 小时去重)
- 加 3 种 alert: `loops-check-error` / `loops-missing` / `loops-degraded`
- 商品化路线图 1.5 节 K 行 状态 ✅ 已修
- 商品化路线图 1.6 节加 3 段修复详情

### 累计

| 项 | 修 I/J/K 前 | 修后 |
|---|---|---|
| 测试数 | 133 | **135** |
| 整体覆盖 | 40.1% | **40.3%** |
| 时间 | 38.5s | 38.5s |

### 工作区文件变更(只动了 I/J/K 相关)

- `scripts/mark42_modules/logs.py` (I 修)
- `scripts/mark42_modules/armor.py` (J 修)
- `scripts/tests/unit/test_armor_compress.py` (J 测试)
- `tools/mark42-watchdog/mark42-watchdog.sh` (K 修)
- `docs/design/mark42-商品化路线图.md` (I/J/K 状态更新 + 1.6 节详情)

点点: 其他文件没动 (RULE: 只动 I/J/K)
