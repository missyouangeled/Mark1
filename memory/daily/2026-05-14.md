# 2026-05-14

## 今日新增决策

- 用户确认继续推进昨天保存的“前台独立监工方案”，但要求把使用方式改成**可开关的监工服务**。
- 新口径：
  - 简单工作默认不开监工服务
  - 复杂工程默认开启监工服务
  - 用户可随时明确要求“开监工服务 / 关监工服务”
- 已同步更新 `PLANS.md`：把原先偏“默认常驻式监工”的思路，改成“服务开关 + 脚本监工为底座 + 前台状态面板 + 按需监工分身”的模型。

## 当前默认开工顺序

1. 先补齐监工服务的开关语义与默认判定
2. 实现 `scripts/openclaw-supervisor-status.py`
3. 先输出 `~/.local/state/openclaw/supervisor/supervisor-status.json`
4. 验证 `idle / running / stalled / failed / done`
5. 再接入 Control UI 的现有本地健康入口

## 当前状态

- 方案已按新要求修订完成。
- 已完成 `scripts/openclaw-supervisor-status.py` 最小版落地。
- 已完成状态验证：`disabled / armed / running / stalled / done / failed / stopping` 都已做本地自测。
- 已完成最小 live 验证：真实短任务能被监工脚本识别为 `running`。
- 已把监工状态接入 `scripts/openclaw-local-health-diagnose.py` 的输出链路；当前本地健康页面 / `status.json` 已包含 `supervisor` 字段与“监工”卡片。
- 随后继续把监工控制语义从单次开关升级成长期可控模型：
  - `policyMode = auto / force_on / force_off`
  - `taskActive = true / false`
  - `desiredState` 由策略 + taskActive 自动推导
- 已补充命令入口：
  - `--set-policy-mode auto|force_on|force_off`
  - `--activate-task`
  - `--deactivate-task`
- 已把本地健康页面中的监工明细更新为显示：策略 / taskActive / 有效意图。
- 已同步更新 `TOOLS.md` 的监工速记、常用命令与“简单工作 / 复杂工程”自动判定口径。
- 已把新的监工服务语义写回 `AGENTS.md`，替换旧的“默认常驻监工分身”口径，避免未来新会话继续按旧规则执行。
- 当前默认基线已统一为：`auto + taskActive=false`（自动模式下未激活，而不是强制关闭）。
- 因为当前这轮本身属于复杂工程，已实际切到：`auto + taskActive=true`，当前监工状态显示为“待命中”。
- 已新增 `tools/openclaw-supervisor/README.md` 与配套 systemd 模板：
  - `tools/openclaw-supervisor/openclaw-supervisor-watch.service`
  - `tools/openclaw-supervisor/openclaw-supervisor-watch.timer`
- 已在当前公司 Linux 机器上实际安装并启用用户态 timer：`openclaw-supervisor-watch.timer`。
- 已验证：timer 启动后立即触发一次刷新，并进入等待状态；`systemctl --user list-timers` 可看到下一次触发时间。
- 继续补上了监工分身管理入口：`scripts/openclaw-supervisor-subagent.py`
  - 当前支持：`list / spawn / kill`
  - 实现方式：
    - `tools.invoke -> subagents` 负责 `list / kill`
    - `chat.send -> /subagents spawn ...` 负责 `spawn`
- 已确认一个关键约束：gateway 的 `tools.invoke` 直调 `sessions_spawn` 会被拒绝为 `Tool not available: sessions_spawn`；因此分身自动化不能直接走那条路，必须改走上面的可用组合。
- 已完成内部 smoke test：对 `agent:main:main` 可成功 spawn 一个带 `main-supervisor-lite｜` 前缀的子分身，并能在 `subagents list` 的 recent 里看到完成记录。
- 用户又补了一条非常关键的前台绑定要求：如果 WebChat / Control UI 当前页面满了，点 `+` 换出一个新的会话页，监工/后台回报也必须默认跟到新页面，不要继续落回旧 dashboard 页。
- 已据此把 `scripts/openclaw-supervisor-subagent.py` 补成“前台绑定解析器”：
  - 新增 `resolve-frontstage`
  - owner 默认先收敛到共享父会话（通常 `agent:main:main`）
  - 真正要发回前台时，再优先解析到同一父会话下最新的 dashboard 会话
- 已做真烟测：把一个旧 dashboard key 传给 `resolve-frontstage`，返回的 `targetSessionKey` 会自动指向较新的 dashboard 前台，而不是旧页。
- 继续补了真正的“前台投递”收口：
  - `scripts/openclaw-supervisor-subagent.py` 新增 `send-frontstage`
  - 内部先做 `resolve-frontstage`，再用 gateway `chat.inject` 把消息直接落到解析出的前台 dashboard
  - 这样监工发状态时不需要额外跑一轮模型
- 已验证：用旧 dashboard key 执行 `send-frontstage`，消息实际写进了较新的 dashboard transcript，而不是旧页 transcript。
- 继续把自动回报也接上了：
  - `scripts/openclaw-supervisor-status.py` 新增 `--notify-transitions`
  - 只在 `stalled / failed / done` 这些状态变化时触发一次前台回报
  - 会写 `~/.local/state/openclaw/supervisor/notify-state.json` 做事件去重，避免 systemd timer 每 30 秒刷屏
- 当前公司 Linux 机上的用户态 service 模板与已安装 service 都已改成带 `--notify-transitions`
- 已做闭环烟测：`supervisor-auto-notify-smoke` 完成后，状态脚本可识别 `done`，随后成功写出 `notify-state.json`，事件日志记录了 `notify_sent ... messageId=...`，并把一条 `[监工] ... 已完成。` 的 assistant note inject 到当前 dashboard 前台。
- 继续补齐了另外两条自动回报烟测：
  - `supervisor-auto-failed-smoke`：任务按预期 `timed_out` 后，状态脚本识别为 `failed`，`notify-state.json` 更新为 failed 事件，事件日志出现 `notify_sent ... status=failed ...`，并把 `[监工] ... 异常结束，我正在接手检查。` inject 到当前 dashboard 前台。
  - `supervisor-auto-stalled-smoke`：对运行中的静默任务用较低 `stalled-after-seconds` 做烟测后，状态脚本识别为 `stalled`，`notify-state.json` 更新为 stalled 事件，事件日志出现 `notify_sent ... status=stalled ...`，并把 `[监工] ... 已有 4 秒无新进展。` inject 到当前 dashboard 前台。
  - 同一条 `supervisor-auto-stalled-smoke` 后续恢复完成后，又被状态脚本识别为 `done`，`notify-state.json` 进一步更新为该任务的 done 事件，事件日志出现 `notify_sent ... status=done ...`，并把 `[监工] ... 已完成。` inject 到当前 dashboard 前台；说明这套监工不仅会报“卡住”，也会在恢复后自动补“已完成”。
- 本轮收尾时已把监工服务恢复回默认基线：`auto + taskActive=false`；当前状态重新回到“监工服务自动待机”，`activeTasks=0`，`main-supervisor-lite` 活跃分身也为 0。
- 用户随后提出了更大一层的主会话稳定性想法：希望把监工、本地健康、异常补位等“本应直接回主会话的辅助信息”先统一过一层实时处理脚本，再回到主会话，尽量减少英文、乱码、乱信息、消息突然消失这类前台体验问题；但当前第一阶段先不强行接管正常主回复，只先统一辅助消息。
- 已据此新增第一阶段最小 broker：`scripts/openclaw-frontstage-broker.py`
  - 当前只做 `emit`
  - 当前来源：`supervisor` / `local-health`
  - 当前状态文件：`~/.local/state/openclaw/frontstage/broker-state.json`
  - 路线：broker -> `send-frontstage` -> `chat.inject`
  - 规则：按 `source + eventKey` 去重，不接管正常主回复
- 已把 `scripts/openclaw-supervisor-status.py` 的自动回报改走 broker，而不是直接调用 `send-frontstage`
- 已把 `scripts/openclaw-local-health-diagnose.py` 补上 `--notify-frontstage`
  - 当前只在 `warn / critical / recovered` 这些健康状态变化时发辅助摘要
  - 已做 smoke：手工把 local-health transition state 伪装成 `warn`，再运行脚本后，broker state 里已出现 `local-health` 的 `recovered` 前台投递记录，说明这条统一入口已接通
- 用户随后又明确强调：当前轮里做出的各种修补不能只在这个会话临时生效；默认应做成刷新 / 换会话 / 重启 gateway / 切换模型后仍可继续生效，并且要把落点记清楚，方便以后继续改。
- 据此，聊天页“进行中”状态修补已从“手工改一次当前 `dist/control-ui/assets/index-*.js`”升级成正式补丁：并入 `scripts/apply-openclaw-control-ui-branding.py`
  - 脚本现在除了品牌覆盖，也会顺手给聊天页打“进行中状态”补丁：`sending / stream / canAbort / queue.length > 0`
  - 由于公司 Linux 机上的 `~/.config/systemd/user/openclaw-gateway.service.d/branding.conf` 已通过 `ExecStartPre` 在每次 gateway 启动前自动执行这支脚本，所以这条修补在重启后也会自动重打
  - 已补记录位置：`TOOLS.md` 与 `docs/公司-Linux-OpenClaw-维护说明.md`
  - 已现场验收：重跑 `python3 scripts/apply-openclaw-control-ui-branding.py` 后，输出里明确列出 `chatRunningPatched` 的目标 asset，且 grep 已确认目标条件仍存在于安装目录下的 `dist/control-ui/assets/index-*.js`
- 用户随后明确要求继续把 `broker / watcher` 这条线也按“刷新 / 换会话 / 重启 / 换模型后仍生效”的标准收一遍，并继续推进 broker 第二阶段。
- 当前审计结论：`scripts/openclaw-frontstage-broker.py` 本体已经是正式长期链路；`scripts/openclaw-supervisor-status.py` 与 `scripts/openclaw-local-health-diagnose.py` 也都已经由用户态 systemd service/timer 稳定触发；真正还缺的是 `scripts/openclaw-frontstage-recovery-watch.py` 之前还只是脚本，没有稳定自动触发和恢复去重状态。
- 据此已继续把 watcher 补成正式组件：
  - `scripts/openclaw-frontstage-recovery-watch.py` 现已增加：`notify-state.json`、`frontstage-recovery-events.log`、稳定 `eventKey` 的 anomaly / recovered 过渡逻辑
  - 当前 `pendingProjection`（history 追赶最新 transcript 的短窗口）不会再被当成 anomaly 通知，减少误报
  - 第二阶段已真正接进 broker：来源名 `frontstage-recovery`，由 watcher 在 anomaly / recovered 变化时调用 broker 发回当前 dashboard
- 当前已新增并安装：
  - `tools/openclaw-frontstage-recovery/README.md`
  - `tools/openclaw-frontstage-recovery/openclaw-frontstage-recovery-watch.service`
  - `tools/openclaw-frontstage-recovery/openclaw-frontstage-recovery-watch.timer`
  - `~/.config/systemd/user/openclaw-frontstage-recovery-watch.service`
  - `~/.config/systemd/user/openclaw-frontstage-recovery-watch.timer`
- 当前已现场验收：
  - `python3 -m py_compile scripts/openclaw-frontstage-recovery-watch.py` 通过
  - timer 已 `enable --now`
  - 手工跑 service 返回 `Result=success`
  - watcher 当前会输出 `last-report.json`，并在本轮活跃会话场景下把结果判成 `pendingProjection` 而不是误报 anomaly
  - 合成验收已确认：同一 anomaly 不会重复出 candidate；anomaly 恢复后会给出 `recovered` candidate
- 随后又继续把第二阶段往前推了一步：
  - `scripts/openclaw-frontstage-recovery-watch.py` 新增读取 `sessions.list` 的目标 session 快照，并把 `status / hasActiveRun / updatedAt / startedAt / endedAt` 一并写进报告
  - 当前本轮活跃 dashboard 的实际观测结果已变成：`pendingProjection=true` 且 `pendingReason=session-active-run`，提示语也改成“当前 session 仍有 active run；chat.history 暂时没追上最新 transcript，这轮先按进行中处理。”
  - 这意味着 watcher 现在不只是拿“30 秒内 history 没追上 transcript”做宽限，还会把真正仍在跑的 active run 当成进行中窗口处理
- Control UI 这边也继续把“活着信号”补强：
  - `scripts/apply-openclaw-control-ui-branding.py` 的正式补丁链路现已把聊天页“进行中”条件进一步补成 `loading / sending / stream / canAbort / queue.length > 0`
  - 已现场验收：重跑补丁脚本后，安装目录下 `dist/control-ui/assets/index-*.js` 已确认带上 `e.loading||e.sending||e.stream!==null||!!e.canAbort||(e.queue?.length??0)>0`
- 随后又继续把第二阶段往“tool-final / 刚结束空窗”那层推进：
  - `scripts/openclaw-frontstage-recovery-watch.py` 现已新增 `session-recently-ended` 这类 `pendingProjection` 原因：若目标 session 刚结束不久，但 history 还没追上 transcript，会优先按恢复中的短窗口处理，不急着报 anomaly
  - Control UI 的正式补丁链路也进一步把 `session.hasActiveRun / session.status=running` 纳入聊天页“进行中”条件，避免有 run 仍在后台，但前台既不流字、也不转圈
  - 已现场验收：当前安装目录下的前端资产已确认命中 `a?.hasActiveRun===!0||a?.status===\`running\``；watcher 当前报告也已带出 `sessionSnapshot.status=running`、`hasActiveRun=true` 与 `pendingReason=session-active-run`
- 用户在 10:01 又补了一条更基础、以后默认要长期遵守的修改工作流：凡是他已经明确提出诉求的改动，第一步先查之前有没有类似功能/入口/补丁；第二步检查与现有功能和逻辑有没有冲突，若有问题先及时回报并给出解决方案；第三步改完后默认接成正式补丁或自动链路，不要只在当前会话临时有效，并把落点记清楚，方便以后继续改。
- 中午用户又补了一段与千千有关、此前还没落档的新加坡回程记忆：从新加坡回来的那天，你们坐去机场的出租车时，他一路没说话，因为知道回天津后就要分开；后来才发现千千一直在看着他，车开到一半还问他“为什么不开心”。到了机场，她跟他哭了，说了很多之前一直自己承担的事情；而在飞回天津的飞机上，她又主动拉过他，深情地吻了他。这段内容现已补进 `state/ex-personas/qianqian/memories.md` 与 `memory/stories.md`。
- 当天下午还做了一次当前 Control UI 主会话媒体链路的真实验收：先用“纯白图 + 单 `bi` 声 + 简单测试视频”做最小烟测，再用更直观的“三色图片 / 三段测试音 / 带移动白条的视频”做二次验收。用户随后明确反馈：图片、音频、视频当前在前端都“完全没问题”。这意味着当前主会话前端至少已通过：图片显示、音频播放、视频展示/播放 这三条最小真实验收。
- 下午继续把 broker 数据层 1.0 往“可长期运转、升级后可按表重装”方向收口：
  - `scripts/openclaw-frontstage-broker.py` 的 `rebuild-views` 现已不再强依赖伪造的 `--source/--event-key/--message` 参数；`emit` 才要求这些参数
  - 新增 broker 周期重建 user systemd 模板：`tools/openclaw-frontstage-broker/openclaw-frontstage-broker-rebuild.service` / `.timer`
  - 当前用户态单元已安装到 `~/.config/systemd/user/`，并已启用 timer
  - broker 视图 / manifest 当前新增 `freshness` 元数据，包含 source 文件路径、是否存在、文件 mtime、报告时间戳，后续 renderer / 排查脚本可直接据此判断新鲜度
  - 已修掉一个关键时间语义问题：`updatedAt` 现在表示真实 `rebuild-views` 时间，不再错误沿用旧 `broker-state.json` 的更新时间
  - `scripts/test-frontstage-broker.py` 已把 `freshness` 与 `updatedAt == freshness.rebuiltAt` 拉进最小回归，当前仍 `ALL PASS`
  - `scripts/apply-openclaw-frontstage-broker-data.py` 新增 `--install-user-systemd`，可把 broker rebuild 的 user systemd 安装 + 启用也收进正式 apply 入口；本机已现场跑通
  - 相应 README / 维护说明 / 补丁注册表 / 重建清单已同步更新
- 下班前用户又明确反馈一次新的前台体感故障：Control UI 里主回复本来像在一点点往外吐，但随后又突然消失。现场排查后已先定位并补了两层：
  - `scripts/openclaw-frontstage-recovery-watch.py` 之前会把 silent `NO_REPLY` / 空可见文本的 assistant turn 直接剔掉，导致 `transcriptLatestAssistant=null`、`historyLatestAssistant=null`，从而把这类“边回边消失”的 turn 错判成 `ok`。现已新增 `assistant_turn_missing_visible_text` 异常类型，并把对应回归用例补进 `scripts/test-frontstage-recovery-watch.py`；当前回归 `ALL PASS`
  - 为了更快发现这类问题，`tools/openclaw-frontstage-recovery/openclaw-frontstage-recovery-watch.timer` 已从“开机约 90 秒后首跑、之后每 45 秒”调整为“开机约 60 秒后首跑、之后每 15 秒”
  - 进一步顺着 Control UI dist 现场定位到一个直接导致“突然消失”的收口点：assistant final 为空 / silent 时，前端会立刻走 `Gl(e)` 强制 reload `chat.history`，从而把本地刚看到的阶段性内容马上清掉。现已把这条路径改成更保守的处理：不再在 silent/empty final 上立刻强制 reload；该补丁已并入 `scripts/apply-openclaw-control-ui-branding.py` 的正式重打链路，并已在当前 dist 上重打验证
- 用户随后明确要求：剩余约 30 分钟先不要再散着做，而是按“25 分钟继续实作 + 最后 5 分钟只做总结”的方式收口。当前已据此把最后阶段拆成：A) 查 runtime/silent turn 来源，B) 继续收紧 Control UI 前端收口逻辑，C) 确认 recovery watcher 联动链路与文档，D) 最后 5 分钟停止新改、只做总结。
- 同期用户又追问 broker 算不算最终完成版。当前已明确写进 `PLANS.md`：broker **不算长期意义上的最终版**，但**算当前阶段目标下可交付的 sidecar 数据层 1.0**；明天若继续 broker，优先顺序应是：1) 事件契约正式化，2) 统一快照模型再收一轮，3) source→broker 字段映射再顺一轮，4) 只让辅助 renderer 评估读 broker，5) SSE/WebSocket/HTTP 接口继续后置。
- 10:06 之后继续专门收 broker 第二阶段的 recovered 尾巴：当前已定位到一个明确一致性问题——`scripts/openclaw-frontstage-recovery-watch.py` 的 recovered 判定之前主要依赖 `previous_report`，没把 `notify-state.json` 中“上一条已发状态”当成强依据；因此会出现“报告已经恢复正常，但 notify-state 仍停在 anomaly”的不一致。
- 已据此做最小修补：
  - `build_notification_candidate(...)` 现在同时接收 `previous_report + previous_notify + current_report`
  - 若 `previous_notify.status == anomaly`，且当前已不再 anomaly / pending，就会优先补一条 `recovered|<previous_notify.eventKey>`，确保通知状态能从上一次已发 anomaly 正确收回来
- 已做两层验收：
  - 函数级合成验收：当 `previous_report` 已正常、但 `previous_notify` 仍是 anomaly 时，当前逻辑已能正确生成 recovered candidate
  - 当前 live 会话下再次运行 watcher 时，由于目标 dashboard 仍 `hasActiveRun=true`，因此结果仍被正确判成 `pendingProjection(session-active-run)`，这时不发 recovered 是预期行为，不算 bug
- 按用户关于“大版本升级后仍可逐项恢复补丁结果”的要求，当前还新增了两份正式文档：
  - `docs/通用-OpenClaw-补丁注册表.md`
  - `docs/通用-OpenClaw-补丁重建清单.md`
- 当前注册表已纳入至少这些正式补丁：
  - Control UI 品牌补丁
  - 聊天页“进行中”活着信号补丁
  - frontstage broker
  - supervisor 自动回报
  - local-health 前台回报
  - frontstage recovery watcher
  - NVIDIA 音频 gateway patch
- 为了让 broker 第二阶段后续能持续回归验证，当前还新增了：
  - `scripts/test-frontstage-recovery-watch.py`
  - 已现场验收：`python3 scripts/test-frontstage-recovery-watch.py` 返回 `ALL PASS`
- 11:24 用户进一步明确了一个重要的路线收敛：虽然长期仍想把工作层 / 数据层 / 渲染层拆开，但当前阶段先不要全面开拆，**先只升级 broker 为数据源**；其他层先规划，不先落地。这个方向已按方案文档边界写入 `PLANS.md`，并明确标注了当前可行度评估、与现有 broker/watchers/Control UI 的冲突检查，以及“OpenClaw 主体稳定性优先、broker 先做 sidecar 数据层”的总原则。
- 随后已开始实际动手做 broker 升级，而且按“最小增量、不碰主链”的原则落地成当前第一步：
  - `scripts/openclaw-frontstage-broker.py` 在保留现有 `emit` / 前台投递行为不变的前提下，新增 sidecar 数据层产物：
    - `~/.local/state/openclaw/broker/events.jsonl`
    - `~/.local/state/openclaw/broker/views/frontstage.json`
    - `~/.local/state/openclaw/broker/views/health.json`
    - `~/.local/state/openclaw/broker/views/tasks.json`
    - `~/.local/state/openclaw/broker/views/recovery.json`
  - 现有兼容文件 `~/.local/state/openclaw/frontstage/broker-state.json` 继续保留，不打断旧链路
  - 当前视图还支持从旧 `broker-state.json` 回填，所以哪怕某个 source 不是本轮刚发的新事件，也能补出对应视图
- 随后又继续补了一步升级友好性：
  - `scripts/openclaw-frontstage-broker.py` 新增 `rebuild-views` 入口
  - 这个入口会直接从现有 source 状态文件与兼容 `broker-state.json` 重建 broker 视图，不需要等下一次正好有辅助消息进来
  - 这使得以后 OpenClaw 大版本更新后，按补丁重建清单执行时，broker 数据层不再依赖“刚好触发一次新事件”才能恢复
- 在这基础上又继续补了一层：
  - 当前 broker 数据目录已新增 `manifest.json`，用于明确记录 schema 版本、当前视图文件与 source 状态文件路径，方便以后 renderer 或重建脚本按统一入口读取
  - 当前还新增 `scripts/apply-openclaw-frontstage-broker-data.py`，把 `py_compile + broker 测试 + rebuild-views` 收成一条正式可执行的最小重装入口
- 为了保证 broker 这步不是“写了就算”，当前还新增了：
  - `scripts/test-frontstage-broker.py`
  - 已现场验收：`python3 scripts/test-frontstage-broker.py` 返回 `ALL PASS`
  - `python3 scripts/apply-openclaw-frontstage-broker-data.py` 已成功运行
  - 实际 smoke：`broker-data-smoke` 已成功写出 `events.jsonl` 与 `views/frontstage.json`，且当前 `views/health.json / tasks.json / recovery.json` 与 `manifest.json` 也都已存在
