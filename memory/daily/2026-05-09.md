# 2026-05-09

## 会话摘要

- 用户一早特意提前到公司，明确要求继续 `ChatTTS 方案A`。
- 我先接着昨天的 `HANDOFF.md` / `PLANS.md` 往下做，直接进入“官方完整 DVAE 资产的兼容加载测试”。
- 已再次核对上传到本机的官方资产：`tmp/upload-drop/chattts-20260508/DVAE.safetensors` 含完整编码侧键，`Decoder.safetensors` 仍为 decoder-only。
- 随后本地直接构造 `DVAE` 模型做加载匹配测试，确认官方 `DVAE.safetensors` 与当前 v0.2.x 运行时的主要差异并不是“缺层”，而是 24 个 `decoder_block.*.weight` / `decoder_block.*.gamma` 的命名差异。
- 我把这 24 个键改名后重新生成了新的 `tmp/voice-replies/chattts-full-attempt/asset/DVAE_full.pt`；改名后再做 `load_state_dict` 检查，结果已经达到 `missing=0 / unexpected=0`。
- 为了真正验证落地链路，我新增了复现脚本：`tmp/voice-replies/chattts-run-plan-a.py`。
- 第一次跑脚本时，先被 ChatTTS 自带的 asset sha 校验拦住；原因是本地 `DVAE_full.pt` 已经过键名修正，哈希不再等于官方原始文件。随后在脚本里显式绕过校验后继续测试。
- 继续测试时，普通 TTS 已成功跑通，产出 `tmp/voice-replies/chattts-plan-a-normal.wav`。
- 再往下测 `sample_audio_speaker()` 时，第一次卡在参考音频 dtype 问题：`soundfile` 默认读出 `float64`，而 torchaudio 的 mel 变换要求 `float32`，报错 `double != float`。将参考音频显式转成 `np.float32` 后已修复。
- 修完后，`sample_audio_speaker()` 已成功产出 `spk_smp`（长度 720），说明 encoder / sample-audio 编码链确实已经跑通，不再只是“能加载但不能 encode”。
- 最后继续用 `spk_smp + txt_smp` 做 zero-shot 验证，也已成功生成 `tmp/voice-replies/chattts-plan-a-zero-shot.wav`。
- 产物已额外用 `file` 核对：两个输出都是 `16-bit mono 24000Hz WAV`。
- 随后我继续把方案A从“验证脚本”往“可复用入口”收口：
  - 新增兼容补丁模块：`tmp/voice-replies/chattts_plan_a_compat.py`
  - 新增资产准备脚本：`tmp/voice-replies/chattts-prepare-plan-a-assets.py`
  - 新增说明文档：`tmp/voice-replies/README-chattts-plan-a.md`
- 我还顺手确认了官方 `Decoder.safetensors` 也不是结构不兼容，而是同类的 `weight -> gamma` 键名差异；修正后也可直接转成当前运行时可加载的 `Decoder.pt`。
- 接着我用“官方 DVAE + 官方 Decoder（都经过键名修正转换）+ 现有 GPT/Embed/Vocos/tokenizer 资产”的更干净组合，重新准备出 `tmp/voice-replies/chattts-plan-a-runtime/`，并再次完整跑通了 `tmp/voice-replies/chattts-run-plan-a.py`；说明当前方案A已经不再依赖 hybrid 目录里的旧 `Decoder.pt`，整体比前一轮更干净。
- 我又继续往更真实场景试了一步：让 `tmp/voice-replies/chattts-run-plan-a.py` 支持直接读取外部 `mp3` 参考音频并自动重采样到 `24k`，随后用现有 `tmp/voice-replies/zh-hybrid-default-template.mp3` 做了一次功能验证。
- 结果是：外部 mp3 入口本身已经通了，`sample_audio_speaker()` 能成功产出更长的 `spk_smp`（长度 1744）；但由于我手头没有这条模板音频的精确逐字转写，只能临时塞一个占位 `txt_smp`，最终 zero-shot 在起步阶段直接报了 `unexpected end at index [0]` / `StopIteration`。
- 这一步反而把真正缺口钉得更准：**当前方案A要继续往“真实外部参考音频 zero-shot”推进，下一步关键不是修链路，而是拿到一段干净参考音频对应的精确文本转写。**
- 随后用户提醒我：既然有联网能力，就应该直接去互联网上找“公开参考音频 + 精确文本”配对样本。我继续做了公开搜索，确认这类材料确实存在，而且很快锁定了最适合当前用途的一组来源：`AISHELL-3 Baseline Samples` 页面（`https://sos1sos2sixteen.github.io/aishell3/`），页面直接给出样本音频与对应中文文本 / 拼音；OpenSLR `SLR93` 页面同时确认该数据集许可证为 `Apache License 2.0`。
- 我进一步直接从网页源码里抠出了可下载的音频源地址，并成功下载了三条公开样本到本地：`tmp/voice-replies/public-ref-samples/aishell3/raw1.wav`、`raw2.wav`、`raw3.wav`，同时整理了逐字转写到 `tmp/voice-replies/public-ref-samples/aishell3/TRANSCRIPTS.md`。
- 其中三条公开样本分别对应：
  - `raw1.wav` → `在教学楼内释放大量烟雾`
  - `raw2.wav` → `不过英特尔之后不会继续接受如此大的损失`
  - `raw3.wav` → `替我播放相思风雨中`
- 我随后直接用其中一条真实外部参考样本 `raw3.wav + 替我播放相思风雨中` 再跑了一轮 zero-shot，结果成功产出：`tmp/voice-replies/chattts-plan-a-aishell3-raw3-zero-shot.wav`。
- 这说明：**方案A 现在已经不仅能用“模型自己先生成的参考音频”做 zero-shot，也已经能用互联网上找到的公开中文样本（带精确转写）完成真实外部参考 zero-shot。**
- 用户随后明确要求按“先做2，再做1”的顺序继续。我先继续搜索更像聊天口吻的公开中文样本，确认了三类更值得后续挖的来源：
  - `MagicHub / RAMC`：是真正的 spontaneous conversation，网页能直接拿到样本音频，但当前网页没直接露出对应文本。
  - `Hugging Face` 上的对话切片（例如搜索结果里看到的 `Izzyzlin/CFSDD` viewer 片段）：文本非常口语，像“你想想你以后的话想找什么工作啊？”这类真实聊天句子；但当前这台机器 shell 直连 `huggingface.co` / `datasets-server.huggingface.co` 仍报 `Network is unreachable`，所以暂时只能把它记为高价值候选源，不能直接在这台机器上批量取样。
  - `Chinese Dialogues / COERLL`：对话型、带完整转写、许可清晰，但实际资源落在 Google Drive，更适合后续通过宿主机浏览器去挑具体音频。
- 在此基础上，我按用户要求回头补跑了 AISHELL-3 另外两条公开样本：
  - `raw1.wav + 在教学楼内释放大量烟雾` → 成功产出 `tmp/voice-replies/chattts-plan-a-aishell3-raw1-zero-shot.wav`
  - `raw2.wav + 不过英特尔之后不会继续接受如此大的损失` → 成功产出 `tmp/voice-replies/chattts-plan-a-aishell3-raw2-zero-shot.wav`
- 到这里为止，AISHELL-3 这组三条公开样本 `raw1/raw2/raw3` 都已经各自完成了一轮真实外部参考 zero-shot 验证，没有再出现前面那种因为 `txt_smp` 不精确而在起步阶段直接 `StopIteration` 的问题。
- 但用户随后明确指出：我发的这条“10 秒左右试听 + 音量提高约 40%”样本，主观听感明显比昨天最后确认的版本差，且“越来越差”。这条反馈很关键，说明我不该把当前 ChatTTS zero-shot 验证链路产物直接拿来当正式试听基准。
- 已当场重新钉清原因：昨天用户明确觉得最好的，是 `zh-hybrid-noiz-natural-plus20-20260508-1225.mp3` 这条 `Noiz + 中文混合模板` 链路；而我这次发的试听却改成了 `ChatTTS 方案A + AISHELL-3 公共样本 raw3 + 长文本 zero-shot + 10 秒裁切 + 提升约 40% 音量` 的实验链路。它虽然证明了技术链路可跑，但不应被当作“比昨天更好听”的试听版本。
- 也就是说，当前要继续推进时，必须把两件事拆开：
  1. `ChatTTS 方案A` 继续只负责技术验证 / 找更自然的参考源；
  2. 面向用户主观听感的正式试听，默认仍应优先回到昨天已经确认“完美”的 `中文混合模板版本 / Noiz 链路`，除非新方案真有把握超过它。
- 用户随后进一步澄清了真正主线：Noiz 只是保底版本，当前仍然要继续开发 `ChatTTS`，而且应该**直接回到昨天那条效果不错的 hybrid 版本**，把它做成稳定版本，而不是继续混入今天的 zero-shot 分支。
- 我已按这个要求回到昨天 `chattts-hybrid-infer.wav` 对应的 exact 链路，并新增稳定脚本：`tmp/voice-replies/chattts-run-hybrid-stable.py`。
- 这条稳定脚本明确保持昨天那条 working recipe：
  - `ChatTTS v0.2.x` 运行时
  - `tmp/voice-replies/chattts-hybrid/` 资产目录
  - sha 校验绕过
  - `DVAE/DVAEDecoder strict=False`
  - tokenizer 兼容补丁
  - 不混入今天的 public-ref zero-shot / encoder 补全分支
- 随后已用这条稳定脚本直接生成了一条约 10 秒的中文样本：
  - wav：`tmp/voice-replies/chattts-hybrid-stable-preview.wav`
  - mp3：`tmp/voice-replies/chattts-hybrid-stable-preview.mp3`
  - 实测时长：约 `10.123s`
- 这一步的意义是：`ChatTTS` 现在终于重新回到了“先把昨天那条已经讨喜的 hybrid 声音固化下来”的正确主线上，而不是继续拿 zero-shot 实验分支去做试听。
- 用户随后试听这条 `chattts-hybrid-stable-preview.mp3` 后明确反馈“这个很完美”，并进一步要求在此基础上 **语速再快 15%**。这说明当前主观听感最接近用户期望的，不是今天白天那条 public-ref zero-shot 实验样本，而是“昨天的 ChatTTS hybrid 路线 + 固化成稳定脚本”的这一条线。
- 用户同时明确补充了路线判断：`Noiz` 现在应作为**保底版本**保留，但主开发方向仍然是 `ChatTTS`；也就是说，后续不是放弃 ChatTTS 回到 Noiz，而是要把这条 ChatTTS hybrid 版本继续做稳、做可复用。
- 在用户确认这条 **提速 15% 的 ChatTTS hybrid stable** 没问题后，我继续把“可切换音色”这件事往前落了一步：
  - 给 `tmp/voice-replies/chattts-run-hybrid-stable.py` 增加了可选 `spk_emb` / `spk_emb_file` 支持，允许同一条稳定链路改用指定的 ChatTTS speaker embedding 出音。
  - 新增抽样脚本：`tmp/voice-replies/chattts-hybrid-sample-speakers.py`，用于从当前 hybrid working path 直接采样随机 speaker embedding 并落盘。
  - 生成并保存了 3 条新的预设 speaker embedding：
    - `tmp/voice-replies/chattts-hybrid-presets/preset-1.spk.txt`
    - `tmp/voice-replies/chattts-hybrid-presets/preset-2.spk.txt`
    - `tmp/voice-replies/chattts-hybrid-presets/preset-3.spk.txt`
  - 同时用统一测试文本、统一提速 `15%` 导出了 4 条可并排试听的 mp3：
    - `tmp/voice-replies/chattts-hybrid-presets/default-plus15.mp3`
    - `tmp/voice-replies/chattts-hybrid-presets/preset-1-plus15.mp3`
    - `tmp/voice-replies/chattts-hybrid-presets/preset-2-plus15.mp3`
    - `tmp/voice-replies/chattts-hybrid-presets/preset-3-plus15.mp3`
- 这说明：当前这条 `ChatTTS hybrid stable` 路线已经不仅能稳定复现“用户认可的默认音色”，还已经具备了**通过保存/切换 `spk_emb` 做预设音色库**的基础；后面封装时，最稳的方向就是“稳定脚本做底座 + 预设音色清单/Skill 做上层入口”。
- 随后用户确认“四条都可以”，并明确要求把整个 `ChatTTS stable` 封成一个可切换音色的正式入口；我已按这个方向完成正式封装：
  - 新建 Skill：`skills/chattts-stable/`
  - 正式主脚本：`skills/chattts-stable/scripts/chattts_stable.py`
  - preset 配置：`skills/chattts-stable/assets/presets.json`
  - preset embedding：`skills/chattts-stable/assets/presets/preset-{1,2,3}.spk.txt`
  - 本地包装器：`tools/voice-reply/chattts-stable.sh`
  - 打包产物：`dist/chattts-stable.skill`
- 这版正式入口的关键默认值已经固化：
  - 默认 preset：`default`
  - 可切换 preset：`preset-1` / `preset-2` / `preset-3`
  - 默认 tempo：`1.15`
  - 默认资产链：`tmp/voice-replies/chattts-hybrid/`
- 已完成最小验证：
  - `--list-presets` 正常输出
  - `default` 短句渲染成功：`tmp/voice-replies/chattts-stable-verify-default.mp3`
  - 非默认 preset 渲染成功：`tmp/voice-replies/chattts-stable-verify-preset1.mp3`
- 过程中补了一处兼容性修正：若从系统 `python3` 直接调用正式脚本，它会自动 re-exec 到 `~/.local/share/openclaw-voice-venv311/bin/python3`，避免 PyTorch C-extension 导入失败；`ffmpeg` 也增加了本机已知用户态安装路径兜底。

## 当前结论

- `ChatTTS 方案A` 到今天早上这轮为止，已经从“理论可行 / 资产已到手”推进到：**本机 CPU 上可实际完成 普通TTS → sample_audio 编码 → zero-shot 产物生成 的完整功能链验证**。
- 当前仍保留一个旧有 caveat：加载 GPT 时依然会看到 `UNEXPECTED/MISSING` 提示（如 `embed_tokens.weight` 等），但这次普通 TTS 与 zero-shot 都已成功出结果，所以它暂时更像既有 hybrid 资产/运行时混配留下的问题，而不是这轮 encoder 补全的主阻塞。
- 这意味着：接下来如果要继续推进，重点已经不再是“能不能跑通”，而是要转向 **听感验收、是否值得继续稳化、以及要不要把这条链路进一步整理成更干净的可复用方案**。
