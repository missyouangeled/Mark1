# 2026-06-29 日常记录

## 今日摘要

**Mark42 测试体系 Phase 1 收尾**。从零测试起步 → 装正式 pytest 体系 → **111 个测试 / 37.8% 覆盖** → **发现并修复 1 个真 bug**（armor.py 写文件顺序错）。
**下午**：补齐所有文档（手册 / 收官 README / 更新日志 / ERRORS）。模型套餐 5h 限额到了一次，重启后继续完成。

## 重要事项

### Mark42 测试体系三阶段（详见 docs/design/mark42-测试体系-Phase1收官-20260629.md）

1. **Phase 0/1 起步（上午开始）**：装 pytest + 5 插件 + conftest + 24 个测试 + pyproject 配置
2. **Phase 1 续（上午中段）**：发现并修复 armor.py 真 bug（`_save_json` 在 `compactTriggered` 字段设置前调用，导致字段永远丢失）
3. **Phase 1 收尾（上午末）**：写 test_engine.py (22) + test_heavy.py (29) + test_cli.py (16) + 磁盘清理

### 关键设计收获（写进 .learnings/ERRORS.md ERR-001~004）

1. **armor.py 写文件顺序 bug**（真 bug，测试发现的）
2. **`from .X import Y` 缓存陷阱**：reload config 后 `heavy.SCRATCH` 仍是旧值
3. **status_dashboard 函数体内 import**：mock target 用完整路径
4. **fcntl 文件锁 + mock**：mock file handle 不能 fileno()
5. **hard-code 路径不被 XDG_STATE 派生**：`SCRATCH = Path("/mnt/...")` 要 conftest 单独处理

### 文档产出（下午）

- `docs/design/mark42-测试手册.md`（实战手册，8.7KB）— 写测试先看这个
- `docs/design/mark42-测试体系-Phase1收官-20260629.md`（收官 README）— 新会话接力用
- `docs/design/mark42-更新日志.md` 追加 2026-06-29 章节
- `docs/design/mark42-文档目录.md` 加 3 个测试文档索引
- `.learnings/ERRORS.md` 追加 ERR-002/003/004
- `PLANS.md` 追加 Phase 1 三阶段快照

### Git 3 个 commits

```
4d69b00 Mark42 测试体系 Phase 1 收尾：engine/heavy/cli 测试 + 磁盘清理
79a26e9 Mark42 测试体系 Phase 1 续 + armor.py 写文件顺序 bug 修复
57c965e Mark42 测试体系 Phase 1：conftest + 24 个测试 + pyproject
```

### 累计指标

| 指标 | 起步 | Phase 1 收尾 | 增量 |
|---|---|---|---|
| 测试数 | 0 | **111** | +111 |
| armor.py 覆盖 | 0% | 50%+ | +50 pp |
| engine.py 覆盖 | 0% | 56.7% | +56.7 pp |
| heavy.py 覆盖 | 0% | **85.9%** | +85.9 pp |
| cli.py 覆盖 | 0% | 39.7% | +39.7 pp |
| **整体覆盖** | 0% | **37.8%** | +37.8 pp |

### 真生产状态

- ✅ mtime 验证 5 次零污染
- ✅ mark42 status 命令正常（91.4% armor usage）
- ✅ 4 个 systemd 服务 active
- ✅ 4 个 Loop 全部 registered

## 后续待办

### Phase 2（下次会话，预计 1 周内）

- 压缩子模块（smart_crusher / compression_algorithms / code_compressor 等）单测 ~25 测试
- logs 单测 ~10 测试
- 目标：整体覆盖到 50%+

### Phase 3（预计 1 周）

- 集成测试（armor → engine → broker 端到端）
- 目标：整体覆盖到 65%+

### Phase 4（预计 3 天）

- CI 接入 + 覆盖率门禁 ≥ 70%
- pyproject 加 `--cov-fail-under=70`

### 清理候选（待你拍板）

| 项目 | 大小 | 风险 |
|---|---|---|
| `proto-tide/.next/` | 41MB | build artifact，加 .gitignore |
| `.openclaw/tmp/music/` | 20MB | 生成的 mp3，临时文件 |
| `.git/objects/pack/` | 90MB | git 历史，**不动** |

## 心得

今天最大的收获不是"写了很多测试"，是**测试帮我抓到了一个真 bug**。armor.py 写文件顺序错——5 个红测试精确指出位置，修复后立刻全绿。这就是测试的价值，不是"覆盖率多少"能衡量的。

Phase 1 收官，质量门禁建立起来了。下次写新代码前先写红测试，已经不是"建议"而是"流程"了。
