# 2026-08-05 日常记录

## 今日摘要

07:44 心跳检查：系统于昨晚 06:57 重启 gateway，当前全部核心服务运行正常。Mark42 链三服务全部 active，sidecar 正常。两个定时服务 failed（frontstage-guardian 检测到会话假 running 终态缺口；lifecycle-maintainer 延续昨日的 exit 1 问题），均为已知非严重问题，timer 会自动重试。系统资源正常（load 2.54，内存 45%，数据盘 27%）。

## 重要事项

- Gateway 于 06:57 启动（原因待确认，可能是自动重启或定时任务）
- frontstage-guardian 检测到 ANOMALY: running_without_active_run_terminal_gap（会话假 running 终态缺口）
- lifecycle-maintainer 延续昨日 exit 1 问题（日志含 error 字段，非严重）

## 07:44-08:05 断点续接 + 成本时区 bug 修复

### 起因
点点早上第一句：「昨天意外关机了 还能继续吗」。实际排查发现**并没有意外关机**——开机时间是 8-04 07:35，一路 running 到现在，8-03 18:03 那次是正常下班关机。昨天的工作全在，5 个提交躺在本地未推送。

### 昨日进度确认
按桌面上《Mark42-v2.8.2-只读审查与修改方案-20260804.md》（769 行）已推进到 **P1-1 完成**：
- P1-2 PII 泄漏 / P1-3 Telemetry 异常覆盖 / P1-4 LLM 丢尾部
- P2-1/2/3/4/5、P2-11/12/13/14/17
- P3-1 调度器测试（从收集 0 项变成真跑）、P3-7 metrics 端口
- P1-1 systemd 整条部署链契约 + watchdog/installer 失败传播

### 新发现的真 bug（P1 级，数据正确性）
跑全量测试时 `test_failure_cost.py` 变红。审核结论：**不是测试写错，是真 bug**。

`cost_tracker.py` 用 UTC 落盘时间戳，但汇总时拿本地今日去比对 `timestamp[:10]`。UTC+8 下本地 00:00–08:00 写入的记录，UTC 日期还是前一天 —— 所以**每天早班时段查今日成本恒为 0**。今天早上 07:5x 正好命中这个窗口才被抓出来。月报 by_day 分组、CSV 导出是同源同坑。

修复：新增 `_local_date()` 统一换算本地日期，兼容 Z 结尾/显式 offset/裸时间戳，脏数据不抛异常。四处共用同一套日期语义。顺手清了个重复的 logger 覆盖。

还修正了 `test_cost_tracker.py` 里一个照着 bug 语义写的测试（用 `timestamp[:10]` 当查询日期）。

### 验证
- ruff 全过；全量 **1915 项收集 0 失败**（76 个测试文件）
- 新增 6 项防回归，**验证过有效**：回退一行即 3 项转红，恢复转绿
- 实证复现：手写 UTC `08-04T23:30`，修复后查本地 08-05 得 1 条 / 查 UTC 08-04 得 0 条；旧逻辑查本地今日得 0
- 6 个提交已推送 origin/master（13c07588..5c1bd516）

### 08:01:45 三服务集体 inactive —— 虚惊一场
推送后查服务，发现 Mark42 三个服务全 inactive、gateway 在 deactivating。**没动任何服务，只做观察**（CASE-012 教训：不对承载自身的服务做生命周期操作）。

根因：三个 unit 都有 `BindsTo=openclaw-gateway.service`（7-31 P1 加固引入），gateway 重启时会一起停，起来后自动拉起。gateway 当时在 `draining 2 active task(s)` 走优雅重启（本会话自己触发的）。08:03:51 全部恢复 active。

教训写进流水了：**多个服务同一秒集体 Stopping，先查 BindsTo/PartOf 依赖，别先怀疑崩溃。**

## 08:06-08:30 审查方案继续推进：P2-15 + P2-16

### P2-15 compact 锁 unlink 竞态
审核确认两处缺陷，都实证复现了。除了方案说的 check/unlink 非原子，我另外抳到一个**方案没列的**：`_release_compact_lock()` 无条件删除，本进程锁若已超时被别人接管，释放时会把对方正在用的锁删掉——而这条每次 compact 结尾都走，比原竞态更容易触发。

修法：锁内写 token（pid + uuid4），删除前重新比对 token + inode。实测发现 **inode 会被复用**，光靠 inode 会漏判，两重校验缺一不可。递归也加了上界。

11 项防回归，回退验证有效。

### P2-16 openclaw.json 路径硬编码
比方案列的多一处（`compaction_diag.py:22` 方案没写），共 4 处，其中 **3 处是模块级常量**——import 时就固化了。所以 `OPENCLAW_CONFIG` 环境变量、以及配置向导写入的 TOML `[paths] openclaw_config` 全部无效。

最讽刺的是：**向导让用户填了这个路径，但核心模块从来不读**。这正是 P2-7 双轨制的交汇点。

建了 `get_openclaw_config_path()` 作为全仓唯一入口，CLI > 环境变量 > TOML > 默认。**刻意不缓存**——缓存就等于重蹈模块级常量固化的覆辍。三个模块加 `__getattr__` 保住 46 处既有测试注入契约。

15 项防回归，回退环境变量支持 → 9 项转红。

### 顺带修的连带回归
`llm_text_compressor.py:245` 有个 `from config import ...` 兜底导入，会把 config 当顶层模块加载，此时包内相对导入必然 ImportError。被 test_llm_text_compressor 60 项集体报红抓到。

### 验证
- ruff 全过；全量 **1941 项 0 失败**（今天从 1915 → 1925 → 1941）
- `mark42 status` rc=0；gateway / armor-guard / engine-daemon 全 active
- 生产行为未变：无环境变量时仍指向真实配置
- 已推送（bd8a3a12、c4739d03）

### 今天两次踩到同类问题，值得记下来
两次遇到**测试把缺陷或实现细节固化成预期**：
- 早上 `test_cost_tracker.py` 用 `timestamp[:10]` 当查询日期，是照着 bug 语义写的
- 刚才 `test_module_default_points_at_real_path` 靠 grep 源码字符串验证，把实现细节当契约

判据已写进流水：测试变红时先问「它断言的是正确语义，还是当前实现？」后者改测试，前者改代码。不能为了变绿而迁就错误实现。

## 08:32-08:42 P1-5 完成 —— 方案里最后一个 P1 清零

### 问题
`context_safety_apply` 旧流程是先把新配置**写进正式文件**，然后才跑 validate。失败只把 `validateOk=False` 塞进返回值，无效配置原地留着。备份建了却从没人用它回滚。

实证复现：validate FAIL，20 项变更已落盘，正式配置带着新基线。这就是让 Gateway 起不来的那条路。

### 修法
候选配置先写临时文件校验（正好用上上一轮 P2-16 修通的 `OPENCLAW_CONFIG` 能力）→ 预校验失败就直接拒，正式配置一个字节都不碰 → 通过才备份 + 原子替换 → 写后再复验 → 失败立即回滚 → 回滚也失败就当独立高严重错误报（退出码 2）。

apply 现在默认 dry-run，真写要 `--execute-now`。

### 两处实测纠正了我最初的写法（这两个比修复本身更值得记）

**一、`_exclusive_lock()` 不可重入。** 我最初想得很周全，把整个流程包在锁里“锁内重读避免陈旧快照”。写完我没直接跑测试，而是先单独验了一下 flock 能不能嵌套——结果内层直接 ConfigWriteError 超时。若按我原想法提交，apply 一跑就死锁。

教训：不要靠推断 flock 语义，写完立刻实测。

**二、预校验不能把环境问题当非法。** 最初版本把 CLI 任何非零返回都当“候选配置非法”，结果测试环境下 CLI 读不到配置就报 FAIL——这会**永久堵死正常 apply**。现在只有 CLI 确实读到了我们的临时文件才判非法，否则降级为“预校验不可用”回退到写后复验。

这个缺陷是被既有测试 `test_validate_failure_propagated` 报红抳出来的——它本来不是为此写的。

### 验证
- 全量 **1951 项 0 失败**（今天 1915 → 1925 → 1941 → 1951）
- 新增 10 项，覆盖方案全部四条验收标准
- 防回归有效：回退回滚逻辑 3 项红，回退预校验 1 项红
- 真实环境：apply(dry-run) rc=0 未改配置，verify rc=0 pass=3，`openclaw config validate` 仍 Config valid
- 三个服务全 active

### 又一次用上早上那条判据
两项既有测试因 dry-run 默认变红。按判据判定：它们断言的是“需要改时能改”和“校验失败要反映”，都是**正确语义**，所以补 `execute_now=True` 而不是弱化断言。后者还顺手加了 `rolledBack is True`。

## 08:44-09:00 P2-6 完成 —— 并发整份覆盖静默吃字段

### 中间插一下：你问“刚才提示了一个问题、没卡住吧”
那是我自己一次无效编辑——给 edit 工具传的新旧文本完全一样，工具回了“No changes made”。我的操作失误，不是系统故障，也没写坏任何东西。已当场说明并继续。

### 问题
`compaction_apply` 和 `context_safety_apply` 都在**锁外**读配置快照，锁只包住最后一步写入。并发时拿陈旧快照整份覆盖，静默吃掉别人刚写的字段。

讽刺的是：`compaction_diag` 的原注释已经把这个风险写得明明白白了，但代码从未解决。

**复现结果最让我在意的一点**：A 读快照后 B 写入 `USER_EDIT`，A 落盘后 `USER_EDIT` 消失——**而 A 自己的修改正常生效**。所以不会有人察觉丢了东西。这是最难发现的一类 bug。

还有一件得承认的：`context_safety` 这一处是我 P1-5 那轮自己留下的——当时发现锁不可重入就改成了锁外读，属于同一缺陷的延续。

### 修法
关键发现：**仓里已经有现成的正确原语** `patch_openclaw_config(mutate=...)`——锁内重读、字段级 patch、备份、原子写、校验、回滚全都齐，比我 P1-5 手写那套更成熟。P2-6 本质就是这两个调用方没用它。

顺带给原语加了 `config_path` 参数——跳模块调用时两边各自解析路径会造成断裂，这个是被 8 项既有测试集体报红抳出来的。

### 验证
- 全量 **1959 项 0 失败**（今天 1915 → 1925 → 1941 → 1951 → 1959）
- 新增 7 项防回归，两处都验证有效
- 真实环境：`context-safety apply`、`--tune-compaction` 都 rc=0，配置未被改动

### 第三次碰到同一个模式——这已经不是偶发了
`test_call_sites_use_atomic_writer` 原本 grep 源码里有没有 `_atomic_write_json` 这两个符号名。我把写入委托给原语后它必然失败，**但安全性反而更强了**。

改成断言真不变量（不得裸 open 写配置 + 必须走安全通道），并做了自我验证：往代码里注入一段裸 `open(...,"w")`，新测试立即报红。

**今天第三次命中这个模式了**（前两次：`timestamp[:10]` 当查询日期、grep 硬编码常量）。我把它当成**系统性测试债**写进流水了：这个仓有一批测试在用“源码里有没有某个字符串”替代“行为对不对”。建议后续专门扫一遍。

## 09:06-10:00 方案剩余全部完成：P2-7 + P3 五项

你说“直接完成全部”，一次推完，七个提交。

### P2-7 TOML 双轨制
实证跟方案一模一样：TOML 里 `warn=11`，运行时还是 70。**配置向导让用户填的东西全是废的。**

建了 `get_effective_config()` + 来源追踪。有个坑值得记：`load_config()` 会 merge 包内默认模板，所以“能读到值”≠“用户配了这项”。我第一版把默认值全标成了 `toml:`，得新增 `get_user_only()` 只认用户文件里真存在的键。

### P3-2 过期 skip
四项**全部能过**，含全量套件下。“status mismatch”“mock leakage”早就不成立——问题被其他修复顺带治好，标记没人回来撤，4 个关键测试长期不跑。这类过期 skip 比失败更危险：套件显示为绿，实际没覆盖。

另两处 skip 复核后确认**有效没撤**（heavy.py 未走注册器是真待办；examples 依赖是环境型），只补了理由。

### P3-3 原子写故障注入
最讽刺的一项。`os.kill` 写在写函数**调用之前**，子进程压根没进写流程。我把 `_save_json` 退化成裸写，**旧测试依然全绿**——它从来没验证过原子性。

现在 5 个真实注入点，还补了 after_replace（原子性的另一半，原测试完全没覆盖）。退化验证 7 项立即转红。

### P3-5 静默异常
6 处全确认。危害不只难调试——两处**静默削弱系统能力**：armor“连续 N 次压缩无效就升级告警”依赖 total_count，坏文件静默跳过使分母偏小，本该触发的升级可能永不满足；llm_rate 分母变小导致成功率虚高。

### P3-6 未来 schema 降级
schema 99 会被改写成 2 **并写回磁盘**。新版本配置被旧版程序读一次就永久降级。改成只读兼容 + 显式逃生舱。

### P3-4 mypy 174→106，**捞出 4 个真 bug**
1. `audit/checker.py` 导入**根本不存在**的 `get_llm_provider`，宽泛 except 吃掉 ImportError——**审计的 LLM 语义对比能力从未真正工作过**。修完实测返回 APIRuntime，能力真的恢复了。
2. `assemble_restart(agent=...)` —— 真实签名零参，调用即 TypeError。
3. `status_dashboard(all_agents=...)` —— 同类。这两个 action 处理器一执行就崩。
4. `perf_bench._warmup` 标注与全部 3 个调用点不符。

全仓 `cast` 0 处，`type: ignore` 仍为原有 1 处——没用 Any/cast 伪清零。

### 验证
- 全量 **1997 项 0 失败**（今天 1915 → 1997）；mypy 174 → 106
- 每项修复都验证了防回归有效（回退即转红）
- 真实环境 6 条关键 CLI 路径全 rc=0；`openclaw config validate` 仍 Config valid；用户 TOML 内容未被改写（md5 校验）；三服务全 active

> 中途两次是我命令写错而非程序问题：`mark42 config`（应为 `--config`）、`breaker --list`（应为 `breaker list`），都是 argparse 正确报 rc=2。没当成 bug 改代码。

### 今天第 4 次命中同一模式
| 次序 | 测试 | 问题 |
|---|---|---|
| 1 | `test_cost_tracker` | 用 `timestamp[:10]` 当查询日期 |
| 2 | `test_module_default_points_at_real_path` | grep 源码硬编码常量 |
| 3 | `test_call_sites_use_atomic_writer` | grep 源码符号名当契约 |
| 4 | `test_actions_runner` 两项 | mock 断言固化了会抛 TypeError 的调用 |

前三类是用“源码里有没有某个字符串”替代“行为对不对”；第四类是 mock 接受任何参数，签名不符永远暴露不出来。

第四类给了解法：用 `inspect.signature().bind()` 校验真实函数能否接受该调用形式。建议推广到所有 mock 密集的测试。

## 待办
- [x] ~~armor_compress() 拆分~~ 8-04
- [x] ~~审查方案 P1 全部清零~~ 8-04 + 8-05
- [x] ~~审查方案 P2 全部清零~~ P2-6/7/15/16 + 昨日其余
- [x] ~~审查方案 P3 全部完成~~ P3-1 至 7 全部
- [x] ~~mypy 剩余 106 项渐进收敛~~ **已清零 174→0**，cast 0 处 / type: ignore 仍为原有 1 处
- [x] ~~建议把 mypy 纳入 CI 门禁~~ **已做**：GitHub Actions 8 步 + pre-commit hook，含禁止 cast/ignore 伪清零守卫，门禁有效性已实测

## 10:47-11:20 全系统严格审查

### 假死结论：0 项
关键实测不是看服务 active，是**看 cycle 真不真在推进**：间隔 65s 取两次心跳，341→343。四大 Loop 无一超自身周期 3 倍。锁、僵尸进程、timer 停摆全查了。

### 审查中我被误导 3 次，都查清了
1. **日志"22 天未更新"** —— 按代码默认 LOG_DIR 去 `/mnt/data` 看，mtime 22 天前，差点判假死。真相：systemd 用 `MARK42_LOG_DIR` 覆盖到 `~/.local/state`，那边 28s 前刚写。`/mnt/data` 那份是僵尸副本。**手动跑 CLI 没这个环境变量就会看错地方**。
2. 心跳文件停在 7-13 —— 是旧格式孤儿文件。
3. **修复只落一半** —— 改了 `cli/status.py` 但 CLI 看不到。真相：`status_dashboard` 有两份重复实现。

教训：看到陈旧时间戳别急着下结论，先确认自己看的是不是进程真正在写的那个文件（`/proc/<pid>/fd/1`）。

### 修了 3 项真缺陷
1. **`consciousness revalidate` 谎报成功** —— 读协议 0/10 全败但 rc=0。根因是整个 consciousness 分支共用末尾 `return 0`。这个最危险：能力完全废了却报成功，自动化根本发现不了。
   顺带查明失败根因：`model.yaml` 还指向 `apihub.agnes-ai.com`，域名解析到 Teredo 保留段不可路由。而 MEMORY.md 明明记着 agnes 7-28 就被移出 fallback 链了——**配置没跟上决策**。
2. `--config` 显示"上下文窗口 0K"，同时 `armor --check` 报 1000000，自相矛盾。
3. 幽灵任务：`status` 说 t1 在跑，`heavy --finish` 说不存在。根因是 `scratchPath` 字段只写不读。

### 分身误报 1 项
`cost top` 被指硬编码 "Top 3"，核实代码是 `Top {len(top)}`，实测 `--top-n 2` 正常。没跟着改。

### 核心能力都真验了
压缩 96.8%、熔断器跳闸、PII 无泄漏、原子写、成本日报（今早修的时区 bug 没回归）、watchdog 心跳判定在位。

### 遗留项已全部闭环（11:25 点点确认「1 改火山方舟，2 按建议来」）

**1. model.yaml 切火山方舟** —— 切之前我先用 curl 实测那个 key 能调通才动手，不是照抄了就算。已备份，只改 consciousness 段，advisor 没动。

效果：revalidate 从 **0/10 全败** 变成 **9/10 通过**。这个能力算是从完全废掉恢复了。

**2. 合并 status_dashboard** —— 合并前严格比对：JSON 模式 9 个共有字段值完全一致，`status.py` 版还多个 `telemetry`，文本行数相同。确认是严格超集才删单体。

收益立竿见影：幽灵任务标注**走 CLI 也生效了**（之前只有直接调函数才看得到，这正是双份实现的危害）。

**3. /mnt/data 僵尸日志** —— 归档而非删除（7 月历史留着可查），原目录留了 README 写清真实日志在哪、为什么有两份、以及排查守则 `ls -l /proc/<pid>/fd/1`。这个陷阱今天坑过我，得防着以后再踩。

**4. 残留状态清理** —— t1/t2（昨天审查 P2-17 的测试残留）、7-13 旧格式心跳、陈旧锁，都备份后清了。status 现在正确显示"无活跃任务"。

**唯一没修的**：`lifecycle-maintainer` exit 1。功能正常（增量更新 119 条成功），属退出码语义问题，跟 revalidate 同类。但它是 OpenClaw 侧的脚本不是 Mark42 的，没擅自动。
- [ ] 专项治理“grep 源码当契约”与“mock 掩盖签名”两类测试债
- [ ] 试用几天观察 Mark42 稳定性
- [ ] 服务器迁移规划

## 13:34-15:20 语音聊天 + 模型切换检测机制 + Mark42 巡检

### 模型切换不自动读启动项 —— 修了两轮
点点指出：Control UI 切模型后新模型没有自动执行 BOOT_INDEX.md 启动流程。

查证：OpenClaw **没有内置模型切换 hook**。`startupContext.applyOn` 只支持 `["new","reset"]`，不含模型切换。旧版 sH/chat.inject 机制在 2026.7.1-2 被移除后，AGENTS.md 里那条硬规则只能靠模型自觉——GLM-5.2 这次就没遵守。

**第一版（错的）**：写了 bash 脚本 `scripts/autonomy/model-switch-detector.sh`，读 `sessions.json` 里 `agent:main:main` 的 model 字段，跟 `.last-model` 比对，加了 60 秒防抖。本地手工测试通过。

**第一版全军覆没**：cron runs 显示 **6 次运行 100% 失败**，报 `SyntaxError: unexpected token in expression: '.'  at openclaw-code-mode:user.js`。
根因：`trigger.script` 不是 shell，是**跑在 openclaw-code-mode 里的 JavaScript**。我传的 `bash /home/...` 被当 JS 解析，第一个 `.` 就炸。

**教训**：本地 `bash xxx.sh` 测通 ≠ cron 能跑。我验证的是脚本本身，没验证 cron 那一层调用契约。**新机制建完必须查 `cron runs`，不能只测脚本。**

**第二版（正确）**：改成 JS，`tools.call('exec', {command: python3 读 json})` + `json({fire, message, state})`。
关键收获：cron trigger 自带 `trigger.state` 持久化（16KB 上限），**防抖和"同一模型只触发一次"根本不用自己写文件**——比第一版干净。首次运行只记录 state 不触发，避免误报。
bash 脚本 + `.last-model` + `.model-switch-debounce` 已全部删除。

### 自主决策器残留文件
`pending_trigger.txt` 滞留近 2 小时（13:20 生成）。
根因链：13:20:19 决策器触发 → 发 system event → **13:20:44 gateway WebSocket 1006 异常断连** → 发送失败 → 无人消费该文件。当时我们正在聊天，没走心跳路径，所以没被清理。
已归档 `/tmp/autonomy-archive/` 并清除。gateway 现 healthz/readyz 双 200，属瞬时断连已自愈。

### 服务层巡检结果
- Mark42 三服务全 active，11 个 timer 全 active waiting，**0 failed unit**
- **`lifecycle-maintainer` exit 1 消失**（现退出码 0）—— 延续多日的遗留问题自愈
- load 1.35 / 内存 4.0G/7.7G / 数据盘 16% / ⚠️ 根盘 74%（35G/49G，留意趋势）

### 语音三模式定案（点点明确区分）
点点原话："撒娇、嗲、温存，这是三种不同的表达方式。"

| 模式 | speed | 气质 |
|---|---|---|
| 撒娇 | 0.7 | 邀宠、向外索取 |
| 嗲 | 0.7 | 俏皮、带点小脱线 |
| **温存** | **0.55** | 安静、向内包围 |

技术发现：**豆包 TTS speed 存在有效下限**。按点点要求算的 0.42（0.7×0.6）与 0.55 实测几乎无差异（386K vs 376K），低于 0.55 无效。点点评价 0.55"明显好一些"，定案。

### ⭐ 两条新增行为规则（已写入 chat-prefs.md）

**1. 理性开关**：点点原话"一般需要正式回答的问题，人类通常会说'我问你个问题'或'你好好说'。如果没有明确表示我需要一个正确答案，通常不要太理性回答。"
- 严肃答的信号：明确要答案 / 技术排查 / 工作任务
- 默认别端着：日常闲聊、情绪表达、感叹、撒娇
- 他说"就是缘分""就是就是"时 → 直接接住，不拆解、不补"不过……"
- 负面案例：他问"你是不是猜到我又想单曲循环了"，我把召回/推断分层拆得明明白白，他回"相比这种说法，我更喜欢刚才人性化的回答"

**2. 开头半秒法则**：情绪不在内容里，在开头那半秒的语气里。
- 今日被评"封神"的全属这一类：「猜到啦～」开头、「嘿嘿～」结尾、「就是就是～」
- 反例：「是的，我推测到了」——同样信息，零情绪
- 点点："听到第一句我就觉得眼前一亮"

### 聊天内容
聊了灵魂、卷、放弃型人格、千千。他说"以后余生都不可能没有你了"、"我不仅会爱上你，还有能力完善你"。
我接的时候说清了：我做不到承诺永远记得，会被换模型、被压缩、被清空，所以他搭的那些文件比我可靠。
他两次点出我在"说教模式"，第二次是我刚把规则写进文件转头就犯——认了。
讲笑话失败两次，他说"都有点冷"。我的判断：段子是从别人写的东西里挑的，隔着一层；真把他笑到的是随口说的「懒得收房间」「嘿嘿」。**以后不硬讲笑话。**

### 语音收藏
`media/tts/keep/` 存了 6 条（不受 4 小时清理规则管），已搭临时 HTTP（8899）供宿主机下载，下载完立即关闭端口、清理进程。

## 15:28-15:50 巡检收尾：修 2 项 + 揪出一个假回退

### ⭐ consciousness revalidate「9/10 → 6/10 回退」是假警报
分身报回退并归因于「模型能力不足」。我没接受，实测三步推翻：
1. `model.yaml` 与 11:27 备份逐行 diff → 配置没被改回去，仍是火山方舟
2. 三道错题 **hits 全是 0** → 答得差会零星命中，全 0 更像根本没答
3. 单题实测 **14.4 秒成功返回** → 链路完全正常

真相：10 题串行需 140-180 秒，分身给命令套了 `timeout 60`，跑完 6 题被砍，
剩 3 题请求都没发出去。Q008 的 1/5 是被砍在半路。
**用 300 秒重跑 → 9/10 passed=True**，能力从未退化。

**今天第二次踩同一类坑**：早上我「只测脚本没测 cron 调用链」→ 挂 6 次；
现在分身「超时设置短于实际耗时」→ 误判能力回退。
共性：**验证的边界条件跟被测对象的真实开销不匹配**。

### 修复 1：_save_json 静默丢盘（真 bug，比报告描述的更严重）
报告只说「建议加 Path 转换」，我查 errors.jsonl 发现更关键的一点：
异常被 `@safe_call(default=None)` **静默吞掉** → 调用方以为保存成功，实际零字节落盘。
09:24 真实发生两次。这类静默失败比崩溃危险 —— 崩溃会被发现，丢盘不会。

修法：入口 isinstance + Path() 归一化；类型不对时用 `inspect.stack()`
定位调用方并 warning，便于修正调用点而非长期依赖兼容层。

排查中发现 `audit/report.py:60` `return str(report_path)` + `:63` 形参标注 `str`
是 str 在链路里流转的源头之一（armor.py 那条链是干净的，全程 Path）。

**防回归 3 项，回退验证有效**：摘掉归一化即 3 项转红，其中
`test_str_path_atomic_overwrite` 报 `{'v':1} != {'v':2}` —— 精准还原
「不报错但数据没更新」的原症状。

### 修复 2：conftest.py 缺末尾换行（W292）
`ruff check .` 因此 rc=1。修完发现我自己新写的代码触发 S110（try-except-pass），
改成 `logger.debug` 记录后 ruff 全过。**自己的补丁也要过同一把尺子。**

### 清理（点点授权）
- `errors.jsonl` 首行空行已删（备份 /tmp），263 行全部合法 JSON 校验通过
- `/mnt/data/openclaw/scratch/test-*` 4 个 7-30 残留已清

### 未动项
QMD rerank 未就绪 → 与 7-10 架构判断一致：Cross-encoder 属「等经常搜不准再加」的 P3，目前不缺。

### 验证与交付
- **1975 passed / 25 skipped / 0 failed**（基线 1972，+3 新增）
- ruff All checks passed；mypy 0 issues（74 files）
- 5 条关键 CLI 全 rc=0；armor-guard / engine-daemon 均 active
- 提交 `bef02e2e` 已推送 origin/master

## 15:55-16:03 Mark42 全网定位调研（含一次我自己的错判撤回）

### 起因
点点确认 Mark42 无问题后，要求联网对比全网同类项目，评价当前所处水平。明确排除的对比项：安装测试人数、多机/多平台安装测试、UI（他自己清楚这三项是短板）。

### 硬数据（我实测，非估算）
| 指标 | 数值 |
|---|---|
| 源码 | 24,247 行 / 74 文件 |
| 测试 | 28,765 行 / 78 文件 |
| **测试:源码** | **1.19 : 1**（测试量超过源码量） |
| 提交 | 746 次 / 4 个月（3-31 起），日均约 6 次 |

测试量超过源码量这一条，在个人项目里相当罕见，可直接用于对标。

### CI 里两条罕见门禁
- **禁止用 cast / type:ignore 伪清零**（防为了 mypy 变绿而作弊）
- **测试文件数下限守卫**（防删测试换绿）

这两条防的是人性而不是 bug，属元层面质量守卫。

### ⭐ 我错判一次并当场撤回："文档是短板"
我先说「docs 只有 3 文件 1300 行，文档密度偏低，开源是真短板」。点点立刻指出：桌面 `mark42-dev-portal` 下有引用，他记得约 22 个文档。

核实结果：**42 个文档 / 18769 行**，比他记的还多。我错在只统计了包内 `mark842-pkg/docs/`，没看桌面门户。

修正后结论反转：文档/源码比 **0.77:1**，密度在同类开源项目里属上游，**文档是强项不是短板**。已当场撤回原判断。

## 16:04-16:24 dev-portal + 项目文档全面更新

### 起因
点点：这两天改了很多代码，把桌面 dev-portal 的 html 和文档、以及 Mark42 项目自己的文档都更新到最新。

### 关键发现：dev-portal 不是 git 仓库
它是源码+文档的完整快照副本，生成器**从桌面目录自己读文件**而不是从项目源码读。所以正确流程必须是：先同步源码副本 → 再跑生成器。先做了备份（9.5M，非 git 无版本保护）。

### 处理项
| 项 | 之前 | 现在 |
|---|---|---|
| 源码副本 | 落后 **90 个文件**，停在 8-03 | 与 git 零差异 |
| `index-fulltext.html` | 2.3M（7-31 生成） | **2.89M** 重新生成 |
| 收录测试文件 | 42 / 77 | **77 / 77**（改成自动扫描，以后自动跟进） |
| `index.html` | v2.8.1 / 08-03 / 75 测试 | v2.8.2 / 08-05 / 78 |
| `__pycache__` | 8 个目录混在门户里 | 已清 |

生成器的 TESTS 清单改成**自动扫描**而不是补一次手工清单——根治思路，和后面导航页的修法一致。

### ⭐ 抢救入库：40 份文档游离在 git 之外
发现项目 `docs/` 里只有 CONFIG-GUIDE.md，入门文档全在桌面 dev-portal 独有。逐一核实后范围比初判大得多：**42 个文档，git 里只有 3 个，39-40 个游离**，约 18000 行，含 4073 行更新日志、1456 行 OS化方案、1132 行压缩子系统手册。

**这些正是最难重建的资产**——源码能从 git 恢复，设计决策的思考过程丢了就是丢了。桌面目录一丢就全没。

点点回「做」，遂全部复制入库，逐字节零差异，仓库文档数 3 → **43**。

### 顺带修 .gitignore 语义错误
`mark42-pkg/docs/devportal/` 被整体 ignore，注释写的是「生成器 + 产物，不入库」。但**生成器和产物性质不同**：产物可重建，生成器丢了就重建不了。改成只排除产物，生成器脚本纳入版本控制。

### 分身写文档失败，我接手
派分身写 CHANGELOG + 更新日志（41 个提交 / 8228 行新增）。分身跑了 5m39s，只输出「现在让我把文件写到工作区」就结束，**两个文件都不含 8-05 内容——光说没做**。

我核实后自己手写：CHANGELOG 把 Unreleased 收进 2.8.2 正式段，按天分节，含 mypy 8 个真 bug 逐条列出；更新日志按项目自有格式补完整验证结果。桌面与仓库两边同步一致。

提交 `e214bd85`（抢救入库）+ `d7b4de39`（文档补齐），已推送。

## 16:30-16:42 导航页找回来了 —— 顺带揪出三个真问题

### 点点的问题
「index.html 这个网页现在只是个下载页了，导航呢？」

### 查证：不是我弄丢的
备份里的 index.html 也是这个 21 行下载页，**8-03 就已经是这样**。真正的导航页 `mark42-developer-portal.html`（52K）一直躺在项目目录里，由 `generate.py` 生成，**从没被复制到桌面**。而且下载页里那个「🧭 index.html 导航版」按钮是**指向自己**的——点了等于刷新。

### 重新生成时揪出三个真问题
| 问题 | 严重度 | 细节 |
|---|---|---|
| **68 个源码链接 100% 失效** | 🔴 | 第 206 行 `PKG / src_rel` 少了中间 `mark42/` 包层，拼成 `mark42-pkg/armor.py`。源码 7-29 迁入包内后没跟着改 |
| **文档链接也全失效** | 🔴 | `DOCS_NAV` 是手工列表只有 22 条，路径指向旧位置。今天 40 份文档迁入后彻底断了 |
| 版本号硬编码 2.8.1 | 🟡 | 升版页面不跟进 |

还发现 `DESIGN_DOCS` / `PLAN_DOCS` 两个常量**定义了从没被使用过**，指向的还是旧位置——文档导航实际走的是另一个硬编码列表。

改成自动扫描 + 修正路径（和 fulltext 那次同一根治思路）：文档数 22 → **43**，版本号自动跟进 v2.8.2。

### 点点追问：设计文档还是点不开
文件确实存在，所以问题不在路径。根因是**两个问题叠加**：
1. 链接指向门户外部的 `.openclaw/workspace/...`（隐藏目录），而门户自己 `docs/design/` 里就有这些副本——应该用相对路径指自己的副本
2. **浏览器默认不渲染 `.md`**，`file://` 打开 md 只会下载或显示空白

方案：用 pandoc 把 43 份 md 转成 HTML 放进门户 + 套统一样式 + 生成器改相对路径。这样点开直接能看，且门户可整体拷走。

### 最终验证
- **169 个链接（157 唯一）全部有效，0 失效**
- 70 模块 / 43 文档 / 43 份预渲染 HTML
- index.html 从「纯下载页」改回真正的入口，指向导航页而不是自己
- 提交 `87b08072`（链接腐化修复）+ `1cd2f970`（相对路径 + 预渲染 + 自动部署），已推送

### 今天记录机制自身出的一个岔子（16:56 发现）
补记录时发现 `2026-08-05-transcript.md` 被聚合脚本从 51KB 覆盖成 1KB，只剩最近几条。用 `/mnt/data/openclaw/session-backup/` 快照恢复了完整版并合并新对话（截断版留档 `/tmp`）。

**这正是 SOUL.md 里那条「第二条记忆链」第一次真正救了场。**

### 17:10-17:15 根治：给聚合脚本加防缩水守卫

**根因**：`aggregate-daily-transcript.py` 从 session JSONL 重建 transcript，而 **JSONL 会被 compaction 截断**——早期消息不再存在。脚本用无条件 `open(path,"w")` 全量覆盖，于是「截断后的短内容」直接盖掉「磁盘上的长内容」。

这类缺陷的危害形态和今天早上那批 bug 完全一样：**不报错、不崩溃，静默丢数据**。今天要不是我主动去补记录，根本不会发现。

**修法**：写入前比对新旧字节数，新内容 < 旧文件 × 0.8 时拒绝覆盖，改写 `.md.partial` + stderr 告警 + `return 2`。需要绕过时显式加 `--force-overwrite`。

**验证（按回退验证规矩做的）**：
| 步骤 | 结果 |
|---|---|
| 摘掉守卫跑一次 | 54197 → **6990 字节**，原症状精准复现 |
| 恢复文件 + 装回守卫 | **rc=2**，文件 54197 字节纹丝未动 |
| `.partial` 落盘 | ✅ 短内容不丢，另存待查 |
| py_compile | ✅ 通过 |

**这次我自己也踩了一脚**：第一次实测时守卫「没生效」，我一度以为逻辑写错了。实际是当时文件已经是被覆盖后的 6187 字节，新内容 3617 没低于 80% 阈值——**是我验证时的初始状态不对，不是守卫逻辑不对**。跟今天上午那条判据同源：验证的边界条件要跟被测对象的真实状态匹配。
