# 2026-06-09 每日记录

## 主要工作：Gateway 稳定性根治

### 问题
本地健康检查间歇性报告"Gateway 不可达"（CRITICAL），用户询问 Gateway 是否容易断开。

### 调查过程
1. 检查 health-diagnostic.log 历史记录：6 月以来共 5 次 CRITICAL 事件，持续 3-7 分钟，均已自动恢复
2. 关键发现：Gateway 服务进程从未崩溃（systemd 始终 active/running），是 HTTP/WebSocket 层探测超时
3. 深入 Gateway 内部代码：健康探测 WebSocket 超时仅 **3 秒**（`timeoutMs: 3e3`）
4. 分析 06-08 晚间日志：告警前 Gateway 内部操作严重变慢——`usage.cost` 17.6 秒、`sessions.list` 6.5 秒、`chat.history` 7.0 秒
5. 定位根因链：**内存膨胀（>1GB）→ 触发 swap I/O → Node.js 事件循环被阻塞 → WebSocket 3s 探测超时 → 报告「不可达」**

### 解决方案
- **MemoryHigh=1.2G + MemoryMax=1.5G**：在 Gateway 内存接近 1.2G 时 systemd 开始限流，阻止进入 swap
- **不设 CPU 限制**：Gateway 是 I/O 密集型，CPU 限制会损害正常响应速度
- **安全重启脚本** `scripts/openclaw-gateway-safe-restart.py`：保存工作区 → 通知 → 重启 → 验证恢复
- **启动链自动同步**：`ensure-resource-limits.conf` ExecStartPre 每次启动前从仓库同步配置

### 新增核心方法论
点点明确要求把今天的理念写入长期规则：
> 遇到问题必须追根因，禁止躲避式修复。不能通过调参数、放宽阈值、增加重试等"绕开问题"的方式蒙混过关。

流程：收集数据日志 → 定位根因链 → 给出具体方案 → 讨论确认 → 实施彻底修复 → 验证不复现。

### 结果
- Gateway 内存从峰值 885MB 降至当前 337MB ，MemoryHigh 已生效
- 配置已推送 GitHub（commit `fb927d6`）
- 安全重启脚本已验证可用

## 用户反馈
- 对系统稳定性逐步提升感到满意：「感觉系统越来越稳了」
- 强调方法论必须持久化，供后续所有模型遵守

## flush-sync 13:05
# 2026-06-09 记忆刷新

## V3 重命名规则迭代修正

### Wall 目录改名（第二轮修正）
- **前缀修正**：Wall 文件夹下的文件，前缀应为 `Wall_`（不是 `Props_`）。哪个文件夹就加哪个文件夹名的前缀。
- **Wall_ 不重复**：原始名字已带 `Wall` 的，直接首字母大写，不再叠加一个 `Wall_`。例：`wall_H1m_Post.fbx` → `Wall_H_1m_Post.fbx`
- **数字不补零**：`H1m_2m` → `H_1m_2m`（不是 `H_01m_02m`）
- **_x/_xx 只改大小写**：只把 x 改成大写，不改名字其他部分。`_x`→`_X`，`_xx`→`_Xx`
- **映射表格式要求**：放对应文件夹下（如 `Wall/wall-rename-v3-mapping.md`），不用 JSON，用 Markdown 表格，全项包含：子目录、原名、新名

### 最终 V3 基础规则（通用）
| # | 规则 | 示例 |
|---|------|------|
| 1 | 前缀 = 文件夹名；已有不重复；.mat → `Mat_` | `Gate_Single.fbx` → `Wall_Gate_Single.fbx` |
| 2 | 去 Type## / SceneStatic | `_Type01` → 删除 |
| 3 | PascalCase 拆分 | `WithPost`→`With_Post`，`WatchTower`→`Watch_Tower` |
| 4 | 数字规范化（不补零） | `H1m`→`H_1m` |
| 5 | `_x`→`_X`，`_xx`→`_Xx`（只改大小写，不动名字） | 规则如字面 |
| 6 | `#变体` 保留 | `#BrickIndustrial6` → `_Brick_Industrial_06` |
| 7 | .meta 同步 | 自动跟随 |
| 8 | Git 快照改前改后 | `git reset --hard HEAD~1` 回滚 |

## 安装前安全审查机制
点点要求：以后装任何新工具前必须走四项审查（数据安全、系统稳定性、运行负担、依赖审查）。已落成 `docs/通用-安装前安全审查机制.md`，MEMORY.md 已同步该规则。

## unity-file-manager.py 完成
通过安全审查的通用大文件管理器：纯 Python 标准库、零依赖、Git 回滚。8 个子命令：index/find/verify/snapshot/rollback/report/rename/check。288 文件索引 0.03s，查询毫秒级。

## DeepSeek V4 Pro 配置
点点提供了公司购买的 DeepSeekV4 API key（`sk-fd4…fb1c`），配置为 provider `deepseek-company`，模型 `deepseek-v4-pro`，下拉列表名 `Company-DeepSeekV4pro`。已生效。API 地址：`https://api.deepseek.com`。

## Military 目录重命名启动
点点要求处理 `/media/missyouangeled/WD_BLACK/Project_amend_01/Assets/AssetScene/SceneModels/Military`：
- **通用 V3 规则**覆盖全目录
- **特殊覆盖**：BarbedWire→`Military_`前缀、Bunker Group 移入 `Military/Groups/`、RoadSentry `Props_RoadCross` 系列改名、Sandbags `Props_`→`Military_`、Trench/Type-01_wooden 升一级改 `Trench_Wooden_01`、WatchTower 加 `_`
- **SentryBox**：按 PascalCase 拆成 `Sentry_Box`
- **非 Prefab 文件**同样按默认规则处理
- 后台分身 `military_rename_v1` 正在执行

## flush-sync 14:07

## 13:56 — Military 重命名收尾完成

### 背景
子分身 `military_rename_v1` 执行过程中，会话压缩导致 Handoff。重新接管后，发现 V1 脚本已被子分身多次修改变乱，决定从零重写干净版 `scripts/military-rename-v1.py`。

### 执行过程
1. 重写干净脚本（含完整 V3 规则 + Military 特殊覆盖 + 拼写/大小写修正 + 拼音不动）
2. 回滚到改名前的 Git 快照 `cc58a5c`
3. Dry-run 显示 248 项、0 冲突
4. 发现并修复 3 个小问题后重新执行：
   - `c#deguo` / `c#yingmei` → `#` 换 `_`（拼音名保持不动）
   - RoadSentry 特殊覆盖后补做 PascalCase 拆分（`RoadCross`→`Road_Cross`、`SentryBox`→`Sentry_Box`）
   - Speaker 目录下小写 `speaker`→大写 `Speaker`
5. V1 大部分已改名（234 项），剩余 14 项 + 目录残留手动补齐
6. 最终 `--confirm` 完成全部 248 项，0 冲突、0 残留

### 改名明细
| 类别 | 内容 |
|------|------|
| 拼写修正 | shabao→Sandbags、Barrer→Barrier、Grounp→Group、Sandbabs→Sandbags、Bunke→Bunker、Ttench→Trench |
| 目录修正 | `Mdoel/`→`Model/`、`Barrer/`→`Barrier/`、`shabao_H120/`→`Sandbags_H120/` |
| 大小写 | `speaker`→`Speaker`、`searchlight`→`Searchlight` |
| 特殊覆盖 | RoadCross_02→Road_Cross_01、_yingmei→Sentry_Box_Allies |
| 目录移动 | Bunker Group→Groups、Trench Type-01_wooden→Trench_Wooden_01 |
| 拼音不动 | jiqiangyanti、zakou 等 56 项 |

### 产出
- 脚本：`scripts/military-rename-v1.py`（干净重写版）
- 映射表：`Military/military-rename-v1-mapping.md`（248 项全列 Markdown 表）
- Git 快照：改前/改后/最终 三份已提交
