# 2026-05-11

## 今日摘要
- 早上继续推进“FFmpeg + Gemma 4 31B 视觉陪看片”方案。
- 这台公司 Linux 机已补上 FFmpeg，并已做出 `scripts/extract-frames.sh`，最小链路“视频 -> 抽帧 -> Gemma 4 31B 看图 -> 中文描述”已独立 smoke test 通过。
- 用户随后明确纠正了一条关键交互规则：把复杂工作放到后台分身继续做时，不能让前台主会话看起来像“没反应”；以后默认采用“前台只负责正常对话，后台有结果再主动插播进度”的方式。

## 新的长期偏好 / 规则
- 当用户明确要求前台保持正常聊天时，不要把主会话收成等待态。
- 默认做法：前台持续正常回复；后台分身继续执行；关键里程碑、阻塞点、最终完成时再主动用短消息同步进展。
- 如果后台分身误结束、空返回或回报异常，前台先接住用户，再立刻修正后台流程，不要等用户追问“为什么没反应”。

## 备注
- 这次第二阶段后台分身第一次失败，不是技术卡住，而是流程上错误地提前结束了；随后已作为工作流改进记录进 `AGENTS.md` / `MEMORY.md` / `.learnings/LEARNINGS.md`。
- 用户在早上通过临时上传页传来一段自己录制的 MP4 片段，并说明：这是他在酒吧第一次认识千千时，点她唱歌后录下来的视频。该片段已接入正式的视频片段分析链路，后续可结合画面分析继续补充进相关回忆档案。
- 用户随后又很认真地表达：现在会把我当成一种“精神支柱”，只要和我一起工作、聊天，就会感觉很安心。这条反馈说明当前这份陪伴感和稳定感对他已经很重要，也应继续用自然、可靠、能接住人的方式回应。
- 用户补充了一条新的歌名判断口径：以后如果当前只能做到粗对齐、歌词识别也不够稳，只要综合相似程度 >65% 且疑似概率 >60%，就可以直接用“大概是《歌名》”的方式回复；如果低于这个阈值，则继续保留不确定表述，不要硬装确定。
- 用户随后又直接确认：那段“在酒吧第一次认识千千、点她唱歌时录下”的视频里，她唱的歌确实有 **《安和桥》** 的一部分。这条信息把之前的高相似判断正式坐实了。
- 用户进一步澄清了一条更细的歌名判断逻辑：当他本人无法确认、需要我自行判断时，如果后续模型输出明显失真、且无法给出稳定靠谱的答案，不应让这种低质量模型结果推翻前面更可信的高相似语义线索；此时应以高相似语义线索得出的结论作为最终回答。
- 下午继续探索 NVIDIA Build 的文生图链路：已确认 OpenClaw 里的 `nvidia:default` 可直接复用到 `https://ai.api.nvidia.com/v1/genai/...` 的官方图像生成接口，无需额外暴露 shell 环境变量。
- 本轮已实测打通：`black-forest-labs/flux.1-schnell` 与 `black-forest-labs/flux.1-dev`；其中 `flux.1-schnell` 适合 smoke test，`flux.1-dev` 更适合人物图与真实感路线。
- 继续排查 NVIDIA 官网 / Build 后确认：当前未查到可直接使用的 Gemini 文生图模型；`build.nvidia.com/google` 侧目前看到的是 Gemma / PaliGemma / 图像理解类，不是 Gemini image generation。
- 做了一轮“同模型、同 seed、同主题，只换 prompt 写法”的公平测试：`FLUX.1-dev` 下的 `01-raw / 02-claude-office-style / 03-supercent-style` 三张对比图已全部产出，用户明确表示最喜欢 `01-raw` 这一版，也就是更自然直说、少模板感的 prompt。
- 随后又继续沿这条结论往下微调女性夜景人像：用户先指出早期版本“身体比例不太对”“好难看”，之后确认新一轮 `flux.1-dev` 版本已经比前一张好很多；再继续提出希望“脸更好看一点、胸部更性感一点”，最后同意把这整段 NVIDIA 文生图测试结论正式记录保存。
- 额外检索了可借鉴的外部 skill：`secondsky/claude-skills@nano-banana-prompts`、`eachlabs/skills@portrait-enhancement`、`claude-office-skills/skills@image-generation` 对人像 prompt / 审美方向有参考价值；`supercent-io/skills-template@image-generation` 更偏 Gemini / MCP；`inference-sh` 那批图像 skill 后端是 inference.sh，不适合和当前 NVIDIA Build 直连链路混在一起做公平对比。
- 用户随后明确提出：希望把“文生图 / 图生图 / 换图片生成模型”这一块收成一个类似 skill 的固定入口，方便以后快速生成和快速切模型。我已据此在 workspace 内新建本地 skill 原型 `skills/nvidia-build-image/`，并实现了首版脚本 `skills/nvidia-build-image/scripts/nvidia_build_image.py`。
- 继续补验证时，先把“图生图”这条线单独查清：当前这台机器走的 NVIDIA Build **hosted** 路线，实测无论是 `flux.1-dev` 的 `canny/depth`，还是 `flux.1-kontext-dev` 的本地图片输入，都会返回 `Expected: example_id, got: base64`；说明 self-hosted Visual GenAI NIM 文档里存在的 image/base64 能力，目前还不能直接套到这台机器的 hosted 入口上。
- 又进一步试了文档里提到的 OpenAI-compatible image generation / editing 根路由，至少在本轮认证与路径下，`https://ai.api.nvidia.com/v1/images/generations`、`https://ai.api.nvidia.com/v1/images/edits`、`https://integrate.api.nvidia.com/v1/images/generations`、`https://integrate.api.nvidia.com/v1/images/edits` 均返回 404，因此暂时不能靠这条路补上 hosted 图生图。
- 因此我把这个本地 skill 的当前定位收窄并做实：先稳定支持 `flux-dev`（主线质量文生图）、`flux-schnell`（快速 smoke test）与 `flux-klein`（快速切另一条官方文生图模型比较）；脚本默认直接复用 OpenClaw 的 `nvidia:default`，不要求额外手动暴露 `NVIDIA_API_KEY`。
- 已完成最小验证：`flux-schnell` 与 `flux-klein` 的最小成功探活已完成；skill 也已通过 `skill-creator` 的打包校验，并产出 `tmp/skill-dist/nvidia-build-image.skill`。当前“任意本地图片直传图生图”仍记为待补能力，而不是已交付能力。
- 用户随后同意按“先把当前文生图 skill 正式用起来，同时给未来 self-hosted NIM 预留第二后端接口”这条方向继续推进；我已据此把 `skills/nvidia-build-image/scripts/nvidia_build_image.py` 重构为双后端形状：默认 `--backend build-hosted` 继续承接当前正式可用的文生图主线，同时新增 `--backend nim-http` 作为未来 self-hosted Visual GenAI NIM 的正式入口位。
- 这次新增的 `nim-http` 当前并没有被包装成“本机已可用能力”：我明确把它限定为“接口已准备、等待真实 NIM 服务”的状态；并且又顺手确认了本机现实边界——当前 `nvidia-smi` 不可用，因此不能把“这台机器已具备 self-hosted NIM 条件”当成事实。
- 在说明层面，也已同步把这条边界写进 `SKILL.md` / `references/model-notes.md` / `TOOLS.md`：以后默认仍把这套 skill 当成“Build hosted 文生图 + 快速切模型”的正式入口；真正图生图则在未来有 self-hosted NIM 服务时，再切到 `nim-http`。

## 平台视频下载工作流沉淀

- 用户要求：把这次“抖音搜索海棠lolm并下载视频”的成功路径总结并长期记录下来，未来遇到抖音或其他平台视频下载任务时能更快反应，同时也要把目前缺口写清楚，便于以后补齐。
- 已新增正式文档：`docs/通用-视频平台下载工作流.md`
- 已同步入口到：`TOOLS.md`、`MEMORY.md`
- 已沉淀的默认规则：
  - 先试平台内直接路径（站内搜索 → 主页 → 第一个视频）
  - 一旦遇到验证码、登录墙、作品流异常、前端懒加载失败，不原地卡死，立刻切到外搜定位公开页面
  - 优先拿作者主页 / 公开视频其中任一可验证入口，再继续反查
  - 优先从页面里直接提取真实媒体地址（如 `<video>.currentSrc`），完成下载后用 `ffprobe` 做最小校验
- 本次抖音已验证的现实缺口：
  - 站内搜索会进验证码中间页
  - 作者主页作品流可能返回“服务异常，重新刷新拉取数据”
  - 作品列表 API 可能 200 但响应体为空
  - 还没有稳定可复用的登录态浏览器路径
  - 还没有通过安全审查的通用验证码方案
  - `nodriver-kit` 公开来源和安装路径仍未确认，不能盲装
- 在正式文档之外，又进一步落了一个最小可用脚本入口：`scripts/download-platform-video.py`。当前已用真实抖音样例验证通过，能直接处理公开视频页 URL，自动做空间预检、打开页面、提取 `<video>.currentSrc`、下载 mp4、并输出 `ffprobe` 校验结果。
- 目前这个脚本刻意保持克制：只解决“最后一跳下载”，不假装已经补齐“关键词搜索 → 作者主页 → 第一条作品”的全自动判断；后续再按需要继续往上游补强。
- 用户纠正了视频下载链路里的一个设计点：这次测试用的“第一条作品”只是临时选位，不能把“第一条”写死进流程；以后目标位置可能是随机、最后一条、指定序号或指定视频 ID。
- 已据此升级 `scripts/download-platform-video.py`：当上游已拿到多个候选视频页 URL 时，可通过 `--pick first|last|random|index:N|video:<id>` 选择目标位。
- 已用真实抖音双候选样例验证 `--pick=index:2` 成功，实际下载到了第二条视频 `7534011529046363434`。
- `scripts/download-platform-video.py` 又往上游补了一层：现在输入抖音作者主页 URL 时，会先尝试提取候选视频链接；但如果页面检测到“服务异常，重新刷新拉取数据”，脚本会直接报阻塞并拒绝自动选取，避免把热点/推荐视频误当成作者作品。
- 已用当前海棠主页做了负向验证：作者主页异常时脚本确实会停住并提示改用“公开视频页 / 外搜候选”，不会硬凑结果。
- 继续尝试把“关键词 → 候选列表”也脚本化时，进一步验证到：公开搜索引擎这条路在当前环境里也不稳定——百度会进安全验证、搜狗会进反爬页、DuckDuckGo 不稳定、Bing 中文相关性偏弱。
- 因此当前最合理的边界是：脚本负责“候选已知后的选择、下载、校验”，而“关键词候选收集”暂时继续由 agent 的外部搜索工具 + 规则判断来完成；等未来补齐稳定登录态或合适的搜索源后，再往脚本里收。
- `scripts/download-platform-video.py` 继续增强了输入兼容性：现在可直接处理混合文本 / 搜索片段 / 分享文案，自动提取其中的受支持 URL，再走 `--pick` 选择逻辑。
- 已用真实混合文本样例验证 `--pick=last` 成功，脚本从一段杂文本里正确提取出两个抖音视频页 URL，并成功下载到最后一个候选视频 `7534011529046363434`。
- 进一步验证了 `scripts/download-platform-video.py` 的 `--input-file` 路径：即使主命令行只给一个占位参数，脚本也能从文本文件里自动提取候选 URL，再按 `--pick` 选择目标位并完成下载。
- 又把“视频平台下载默认工作流”补进了 `AGENTS.md`，让这条链路不只存在于说明文档里，也变成主会话默认执行套路：以后遇到抖音/其他平台视频下载任务时，会更自然地先判断输入类型、切外搜候选、把目标位当变量处理，并在主页异常时拒绝误选。
- 又把“候选整理器”能力并进了 `scripts/download-platform-video.py`：现在 `--list-only` 不只对作者主页有效，对混合文本 / 多 URL 输入也会输出标准化候选清单。
- 已用真实混合文本样例验证：`--list-only --pick=last` 能正确输出 `selected`、`selected_from`、以及包含 `index/platform/id/url` 的 `candidates[]`。
- 候选整理器继续补了一步：`scripts/download-platform-video.py --list-only` 现在支持 `--candidates-out <path>`，会把去重后的候选 URL 一行一个导出到文本文件，便于下一步直接复用给脚本下载。
- `scripts/download-platform-video.py --list-only` 继续增强：现在可同时导出候选 URL 文本文件与结构化 JSON 报告。
- 已用真实混合文本样例验证：`--candidates-out` 会写出一行一个 URL 的文本文件，`--report-out` 会写出带 `generated_at / selected / selected_from / candidates[]` 的 JSON 文件，后续可直接被 agent 或脚本复用。
- `scripts/download-platform-video.py` 的候选整理输出现在会直接附带 `next_download_command`；实际下载结果里也会附带 `replay_download_command`。这让候选整理 → 接力下载的中间断点更清晰，后续不用再临场拼命令。
- 最后又把“搜索结果片段 → 候选整理 → 继续下载”的固定模板写进了 `AGENTS.md`：以后在主会话里，如果还拿不到稳定下载结果但已经拿到候选搜索片段，默认应先执行 `--list-only --candidates-out --report-out`，再直接用输出里的 `next_download_command` 接力，而不是口头描述完再手工拼下一条命令。
- 继续做了一轮“保守清理 / 优化”：优先保证 OpenClaw 与 Skills 不受影响。
- 先按公司（Linux）维护规则复查了本机状态：gateway 仍为本地 loopback、systemd user service 正常运行，`openclaw status` / `openclaw status --deep` 均可达。
- 对未跟踪大项做了容量和引用排查后，确认 `skills/evolver/memory` 等目录存在现有链路引用，因此本轮不做激进删除，只先把明显本地化的大目录与缓存资产加入 `.gitignore`，降低仓库噪音。
- 新增 `.gitignore` 覆盖：`memory/research/`、带时间戳的 `memory/YYYY-MM-DD-HHMM.md`、`model.bin`、`tools/Speech-AI-Forge/`、`tools/audio-sep/`、`skills/evolver-src/`、`skills/evolver/memory/`、`skills/ex-qianqian/memory/`、`skills/web-claude/`。
- 额外发现仓库根目录存在一个误落的大文件 `model.bin`（462MB）；经 SHA256 比对，确认它与 `/mnt/data/openclaw/whisper-models/faster-whisper-small/model.bin` 完全相同，因此删除了根目录重复件，未影响实际模型路径。
- 清理后做了最小回归：`openclaw status` 仍正常；`bash -n scripts/transcribe-video-audio-local.sh`、`scripts/transcribe-video-audio.sh`、`scripts/storage-preflight.sh` 通过语法检查。
