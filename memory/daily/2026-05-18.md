# 2026-05-18

- 早上用户确认我已开始处理当前事项后，又补了一句“记得打开监工”。
- 已立即执行：将监工服务切到 `force_on`。
- 当时状态：服务 `armed`，任务激活 `True`，监工状态 `idle`，活跃后台任务 `0`。
- 这也再次确认了用户对当前工作流的偏好：工作推进时，监工要实际开起来，不能只默认口头记住。
- 上午用户进一步指出：如果监工每次做完当前后台任务后都重新回到待命，还得靠他手动提醒再挂下一段，就失去意义；因此要求监工在任务完成后进入等待接续任务窗口，期间继续检查是否出现新后台任务；若窗口内没有新的后台任务或继续信号，则自动关闭监工。
- 已据此修改 `scripts/openclaw-supervisor-status.py`：新增完成后等待接续任务窗口、`waiting` 状态与自动关闭逻辑，并补了测试 `scripts/test-openclaw-supervisor-status.py`；同时更新了 `TOOLS.md`、`tools/openclaw-supervisor/README.md` 与 `MEMORY.md` 留痕。
- 同日上午用户又确认：3 分钟对连续开发活还是偏短，正式改成默认 **10 分钟**，以减少“刚做完一段就掉回待命、还得手动再提醒”的情况。
