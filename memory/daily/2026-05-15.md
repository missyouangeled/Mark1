# 2026-05-15

## 今日工作摘要

- OpenClaw 已升级到 `2026.5.12`；当前已把升级后自检入口补成正式链路：`scripts/openclaw-post-upgrade-self-check.py` + `BOOT.md`。
- `scripts/apply-openclaw-control-ui-branding.py` 已适配新版 Control UI 结构，并继续负责品牌覆盖与聊天页“进行中 / silent final / yielded history reply”这几条正式前端补丁。
- broker sidecar 数据层当前已稳定：`snapshot` 为主视图，`overview` / `frontstage-status.json` 保留兼容别名；公开副本 `jarvis-frontstage-snapshot.json` 可读。
- 2026-05-15 11:00 已做一次 broker 前台真烟测：消息成功投递到当前 dashboard `agent:main:dashboard:0e238e2b-0e3c-4e63-9c1d-790e3f2794cf`。
- 2026-05-15 11:08 又修掉 supervisor 自动回报最后一处前台绑定偏差：
  - `scripts/openclaw-supervisor-subagent.py` 现在在 owner=`agent:main:main` 且存在 dashboard 时，会优先回当前 dashboard，而不再误落回 `agent:main:main`
  - `scripts/openclaw-supervisor-status.py` 已兼容 broker 返回顶层 `messageId`
  - 真烟测已确认 `[监工] frontstage-tail-continue-no-stop 已完成。` 实际写进当前 dashboard transcript，日志记录：`notify_sent ... target=agent:main:dashboard:0e238e2b-0e3c-4e63-9c1d-790e3f2794cf messageId=ab0baee2-9cc5-4301-9cee-bcd554d10942`
- 用户在 12:02 明确补充了一条沟通偏好：只要任务做完，或者已经达到阶段性完成点，就要主动汇报，不要等他追问。
- 用户在 14:17 又把监工规则收紧了一层：像 infos-handle 这种持续开发活，如果决定按“后台推进 + 监工盯着 + 3 分钟回报”的方式做，就必须真卸成后台任务给监工盯；不能只把监工开成 `force_on` 却还在前台当前会话里继续做。当前已把这条写回 `MEMORY.md` / `TOOLS.md` / `AGENTS.md`，作为后续默认执行规则。
- 2026-05-15 17:21 之后按这条新规则真卸后台推进了一轮 broker / infos：
  - `events.recent` 已补成更稳定的 item shape，测试通过
  - broker 现在会正式公开 `contracts.recordTypes / contracts.eventFieldCatalog`，`infos-handle query --kind contract.catalog` 可直接读到这份口径
  - `events.recent` 新增 `latestBySource` 轻量按 source 摘要层，live 查询已确认正常返回
  - 主会话复核通过：`py_compile`、`python3 scripts/test-frontstage-broker.py`、`python3 scripts/test-openclaw-infos-handle.py` 都是 `ALL PASS`
  - 已做一个本地提交点：`7396ede` (`Tighten broker and infos-handle event contracts`)
- 2026-05-15 17:56 又把这条线的续做口令正式拍板：**以后恢复这条主线时，固定就说 `继续 broker / infos-handle`。** 看到这句时，默认恢复 `broker sidecar 数据层 1.0 + infos-handle 查询契约层`，先读 `HANDOFF.md / PLANS.md / tools/openclaw-frontstage-broker/README.md / memory/daily/2026-05-15.md`，跑 `python3 scripts/test-openclaw-infos-handle.py` 与 `python3 scripts/test-frontstage-broker.py`，然后优先做 README/说明对齐，再决定是否补 `latestBySourceItems[]`。

## 当前结论

- `frontstage-recovery -> broker -> 当前前台`：通过
- `supervisor -> broker -> 当前前台`：通过
- 这轮前台辅助消息链路的关键闭环现在已经打通，并且是真实写入当前 dashboard，而不只是本地日志显示“发送成功”。
- 2026-05-15 17:00 左右出现过一轮 `session file locked` / `SessionWriteLockTimeoutError`：旧 dashboard 条目 `agent:main:dashboard:0e238e2b-0e3c-4e63-9c1d-790e3f2794cf` 残留并指向旧 transcript `8648db4e-75f9-413e-b5a5-6c5b720d3afd.jsonl`，导致主 lane 与 dashboard lane 抢同一文件锁。
- 当前已按最保守路线修复：先备份，再从 `sessions.json` 摘除旧 dashboard 索引，随后重启 gateway 清掉运行时残留；旧 transcript / checkpoint / trajectory 共 13 个文件已从 live sessions 目录移出，归档到：`tmp/session-lock-repair-20260515-171211/archived-live-files-8648db4e-75f9-413e-b5a5-6c5b720d3afd/`。
- 修复后核验结果：当前只剩主会话可见、gateway 已恢复正常运行、旧 `.lock` 不存在，日志里未再出现新的同类锁超时。

## 17:50 收尾 / 对接结论

- 已按用户要求在 17:50 停止继续推进 `infos-handle / broker` 新实现，不再继续开新 query / 新迁移点。
- 当前 Git 代码停点仍是 `7396ede`（`Tighten broker and infos-handle event contracts`）；这轮最后复核时，`master` 最近一串相关提交为：`7396ede <- eb8aa4f <- 3aa091e <- 7fe7efd <- ff07c2f <- 2575456 <- 4de50fb <- e7095d8 <- 45258f1 <- a862517 <- 2f57998`。
- 17:50 收尾前再次核验通过：
  - `python3 -m py_compile scripts/openclaw-infos-handle.py scripts/openclaw-frontstage-broker.py scripts/test-openclaw-infos-handle.py scripts/test-frontstage-broker.py`
  - `python3 scripts/test-frontstage-broker.py`
  - `python3 scripts/test-openclaw-infos-handle.py`
  - `python3 scripts/openclaw-infos-handle.py query --kind contract.catalog --format json`
- 当前确认：`queryContractVersion=13`、`brokerContractVersion=2`。
- 下周一一来，第一优先级不是继续加 query，而是先把 `HANDOFF.md / PLANS.md / tools/openclaw-frontstage-broker/README.md` 的 contract 口径对齐一遍，再决定是否需要补一个仅用于稳定顺序的 `events.recent.latestBySourceItems[]`；如果不需要，就保持 v13 停点不动。
- 当前工作区里仍有与本轮无关的脏文件：`.learnings/ERRORS.md`、`memory/.dreams/*`、若干 `scripts/chattts_*` / `scripts/remote_infer*` 试验脚本；后续如需继续提交，应继续按路径精确 `git add`，不要把这些一并混进 broker / infos 路线提交。
