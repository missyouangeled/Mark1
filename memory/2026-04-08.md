# 2026-04-08

- 将当前工作区里的 `self-improving-agent` 从“只在 `agent:bootstrap` 提醒”升级成更主动但保守的版本：
  - 保留原有 bootstrap reminder。
  - 新增 OpenClaw internal hook 自动分流：
    - 用户明确纠正 → `.learnings/LEARNINGS.md`
    - 明显缺失能力诉求 → `.learnings/FEATURE_REQUESTS.md`
    - 高置信失败 / 报错 → `.learnings/ERRORS.md`
    - 低置信信号 → `.learnings/INBOX.md`
  - 新增去重状态文件设计：运行时写 `.learnings/.hook-state.json`，并已加入 `.gitignore`。
- 为了避免 `skills/self-improving-agent/` 因主仓 `.gitignore` 而只停留在本机热改，已新增主仓可追踪覆盖层：`openclaw-env/skill-overlays/self-improving-agent/`。
- 已同步更新恢复链路：
  - `openclaw-env/restore-skills.sh` 会在恢复 skill 后自动应用 overlay。
  - `openclaw-env/restore-openclaw-env.sh` 会优先从 workspace skill 安装 hook，不存在时回退到 overlay，并补齐 `.learnings/INBOX.md` 等文件。
  - `openclaw-env/post-restore-check.sh` 已新增 self-improvement overlay / hook / INBOX 检查项。
- 本地已做的验证：
  - `handler.js` 用 Node 模拟跑通 bootstrap + correction + feature request + error + inbox 分流。
  - `bash -n` 校验 `restore-openclaw-env.sh` / `restore-skills.sh` / `post-restore-check.sh` 语法通过。
  - 用临时 `HOME` 实跑 `restore-openclaw-env.sh`，确认会安装 `~/.openclaw/hooks/self-improvement/handler.js`。
  - 用临时 `HOME` 跑 `post-restore-check.sh`，新增检查项通过；未恢复 provider / cli-anything skill 的警告符合预期。
- 又继续补上第二阶段的 tool 级失败自动沉淀：
  - 研究 OpenClaw 文档后确认：要抓 raw tool 失败，最合适的不是继续堆 internal hook，而是用 native plugin 的 `after_tool_call`；`tool_result_persist` 更偏同步转写结果，不适合做磁盘侧学习落盘。
  - 已新增本地可恢复 plugin：`openclaw-env/plugins/self-improvement-tool-errors/`，通过 `after_tool_call` 自动捕获高置信 tool 失败并写入 `.learnings/ERRORS.md`，模糊信号进 `.learnings/INBOX.md`。
  - 当前覆盖的高置信场景：`exec` 非零退出、tool 显式 error、approval block、policy / host deny、timeout、fetch / connection fail。
  - 明确仍不直接自动落盘的场景：`Command still running` / 需要后续 `process` 的状态，只进 `INBOX.md`；如果 OpenClaw 某次运行根本没把失败暴露到 `after_tool_call`，这个插件也抓不到。
  - 已把恢复链路接上：`restore-openclaw-env.sh` 会安装并启用 `self-improvement-tool-errors`，`post-restore-check.sh` 会检查 plugin 源和安装可见性。
  - 本地验证：
    - 用 Node mock 事件跑通 `exec` 非零退出 / `fetch failed` / approval blocked / still-running 分流。
    - 实跑 `bash openclaw-env/restore-openclaw-env.sh` + `bash openclaw-env/post-restore-check.sh`，检查项通过，plugin 已可被 `openclaw plugins inspect self-improvement-tool-errors --json` 看见。
  - 额外注意：OpenClaw 会提示 `plugins.allow` 为空时非 bundled plugin 可能被自动发现；当前 restore 通过 `plugins enable` 可正常启用，但如果以后要更严一点，可以再把这个 plugin id 加进显式 allowlist。
- 用户新增长期要求：
  - 以后后台分身如果没有任务就关闭，不要挂着。
  - 以后如果判断任务会耗时较长，应优先自动开后台分身执行，避免主会话显得卡住。
  - 以后默认按一套实用的 thinking 规则执行：普通问答 / 简单解释 / 路径明确的小改动保持默认或低；复杂排错 / 架构取舍 / 大改前规划 / 多上下文综合判断 / 高风险恢复与安全一致性问题开高；又长又复杂的任务优先放到后台分身并在分身上使用更高推理。
# 2026-04-08

- 将当前工作区里的 `self-improving-agent` 从“只在 `agent:bootstrap` 提醒”升级成更主动但保守的版本：
  - 保留原有 bootstrap reminder。
  - 新增 OpenClaw internal hook 自动分流：
    - 用户明确纠正 → `.learnings/LEARNINGS.md`
    - 明显缺失能力诉求 → `.learnings/FEATURE_REQUESTS.md`
    - 高置信失败 / 报错 → `.learnings/ERRORS.md`
    - 低置信信号 → `.learnings/INBOX.md`
  - 新增去重状态文件设计：运行时写 `.learnings/.hook-state.json`，并已加入 `.gitignore`。
- 为了避免 `skills/self-improving-agent/` 因主仓 `.gitignore` 而只停留在本机热改，已新增主仓可追踪覆盖层：`openclaw-env/skill-overlays/self-improving-agent/`。
- 已同步更新恢复链路：
  - `openclaw-env/restore-skills.sh` 会在恢复 skill 后自动应用 overlay。
  - `openclaw-env/restore-openclaw-env.sh` 会优先从 workspace skill 安装 hook，不存在时回退到 overlay，并补齐 `.learnings/INBOX.md` 等文件。
  - `openclaw-env/post-restore-check.sh` 已新增 self-improvement overlay / hook / INBOX 检查项。
- 本地已做的验证：
  - `handler.js` 用 Node 模拟跑通 bootstrap + correction + feature request + error + inbox 分流。
  - `bash -n` 校验 `restore-openclaw-env.sh` / `restore-skills.sh` / `post-restore-check.sh` 语法通过。
  - 用临时 `HOME` 实跑 `restore-openclaw-env.sh`，确认会安装 `~/.openclaw/hooks/self-improvement/handler.js`。
  - 用临时 `HOME` 跑 `post-restore-check.sh`，新增检查项通过；未恢复 provider / cli-anything skill 的警告符合预期。
- 又继续补上第二阶段的 tool 级失败自动沉淀：
  - 研究 OpenClaw 文档后确认：要抓 raw tool 失败，最合适的不是继续堆 internal hook，而是用 native plugin 的 `after_tool_call`；`tool_result_persist` 更偏同步转写结果，不适合做磁盘侧学习落盘。
  - 已新增本地可恢复 plugin：`openclaw-env/plugins/self-improvement-tool-errors/`，通过 `after_tool_call` 自动捕获高置信 tool 失败并写入 `.learnings/ERRORS.md`，模糊信号进 `.learnings/INBOX.md`。
  - 当前覆盖的高置信场景：`exec` 非零退出、tool 显式 error、approval block、policy / host deny、timeout、fetch / connection fail。
  - 明确仍不直接自动落盘的场景：`Command still running` / 需要后续 `process` 的状态，只进 `INBOX.md`；如果 OpenClaw 某次运行根本没把失败暴露到 `after_tool_call`，这个插件也抓不到。
  - 已把恢复链路接上：`restore-openclaw-env.sh` 会安装并启用 `self-improvement-tool-errors`，`post-restore-check.sh` 会检查 plugin 源和安装可见性。
  - 本地验证：
    - 用 Node mock 事件跑通 `exec` 非零退出 / `fetch failed` / approval blocked / still-running 分流。
    - 实跑 `bash openclaw-env/restore-openclaw-env.sh` + `bash openclaw-env/post-restore-check.sh`，检查项通过，plugin 已可被 `openclaw plugins inspect self-improvement-tool-errors --json` 看见。
  - 额外注意：OpenClaw 会提示 `plugins.allow` 为空时非 bundled plugin 可能被自动发现；当前 restore 通过 `plugins enable` 可正常启用，但如果以后要更严一点，可以再把这个 plugin id 加进显式 allowlist。
- 用户新增长期要求：
  - 以后后台分身如果没有任务就关闭，不要挂着。
  - 以后如果判断任务会耗时较长，应优先自动开后台分身执行，避免主会话显得卡住。
  - 以后默认按一套实用的 thinking 规则执行：普通问答 / 简单解释 / 路径明确的小改动保持默认或低；复杂排错 / 架构取舍 / 大改前规划 / 多上下文综合判断 / 高风险恢复与安全一致性问题开高；又长又复杂的任务优先放到后台分身并在分身上使用更高推理。
- 已开始接入 QMD 作为 OpenClaw 的 memory backend：
  - 本机已安装 `@tobilu/qmd`，命令可用版本为 `qmd 2.1.0`。
  - `~/.openclaw/openclaw.json` 已切到 `memory.backend = "qmd"`，gateway 也已重启并能看到 QMD memory 启动日志。
  - 目前已确认的真实状态不是“没装上”，而是“已装上并接入，但首轮 embedding / 向量链路未稳定”。
- 当前 QMD 卡点已定位：
  - 不是单纯 CPU 慢，而是本机当前对 Hugging Face 直连失败（出现 `Network is unreachable`），导致 QMD 首轮模型下载 / embed 走不通。
  - 即使如此，QMD 的纯关键词 / BM25 搜索已验证能工作；也就是说当前更准确的状态是“基础本地搜索可用，向量 / rerank 受网络条件影响未跑通”。
- 已决定后续把 QMD 目标从“先追求完整向量能力”收窄为“先在本地稳定用起来”：
  - 研究 OpenClaw 的 QMD manager 后确认：当 `searchMode = "search"` 时本就不该主动跑 embed；而 embed 定时器可通过把 `memory.qmd.update.embedInterval` 压到 `0` / 不启用来停掉。
  - 后续稳定化方向已明确为：固定 CPU-only 思路、优先保住 BM25 / search-only 路线、尽量避免 boot 阶段反复卡 embed，再考虑更高阶向量能力。
- QMD 这一轮排查里，有两个后台分身都已按用户偏好在结束后关闭；其中第二个稳定化分身没有带回新的有效修复，只确认了应继续围绕“本地先稳定用起来”这个更窄目标收口。
