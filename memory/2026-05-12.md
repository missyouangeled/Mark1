# 2026-05-12

- 用户把“主会话始终保持通畅”升级为硬性规定：除纯日常聊天外，当前轮只要属于工作型任务，就默认应有一个轻量监工分身在位，用于兜底、监工、补位回报，避免前台出现“像没反应了一样”的体感。
- 监工分身规则已收敛为：固定保留标签 `main-supervisor-lite`；唯一标号按 `main-supervisor-lite@<runtime-host>` 理解；整体数量应为 `0 或 1`；若存在普通任务分身，则必须同时存在且只存在一个监工分身；无后台任务持续约 2 分钟后，可关闭监工分身。
- 监工异常模板已收敛为：任务空返回时由监工先向前台报告异常并接手检查；任务异常结束时必须先由监工报告“异常结束，正在修复”，再开始排查与修复；用户随后又把自动补报阈值从 30 秒改为 1 分钟，之后又进一步改为 3 分钟：若前台 3 分钟内无可见产出，监工必须主动补一句短进度，这不等于任务必须 3 分钟内完成；同时要避免为了兜底频繁制造当前直聊里的可见内部消息。
- 用户明确：主会话稳定性不仅指文字可回复，也包括音频、视频、图片必须可查看、可播放、可打开；今后凡是涉及主会话附件/媒体链路的改动，都要以主会话里的真实可用作为硬性验收标准。
- 已定位并修复一处主会话语音附件问题：`tmp/voice-replies` 为软链、realpath 落在 workspace 外时，会被媒体安全检查拦为 `path-not-allowed`；当前最小修复是将生成的音频自动 stage/copy 回 workspace 内真实目录 `tmp/voice-replies-staged/` 后再返回给主会话。
# 2026-05-12

- 用户把“主会话始终保持通畅”升级为硬性规定：除纯日常聊天外，当前轮只要属于工作型任务，就默认应有一个轻量监工分身在位，用于兜底、监工、补位回报，避免前台出现“像没反应了一样”的体感。
- 监工分身规则已收敛为：固定保留标签 `main-supervisor-lite`；唯一标号按 `main-supervisor-lite@<runtime-host>` 理解；整体数量应为 `0 或 1`；若存在普通任务分身，则必须同时存在且只存在一个监工分身；无后台任务持续约 2 分钟后，可关闭监工分身。
- 监工异常模板已收敛为：任务空返回时由监工先向前台报告异常并接手检查；任务异常结束时必须先由监工报告“异常结束，正在修复”，再开始排查与修复；用户随后又把自动补报阈值从 30 秒改为 1 分钟，之后又进一步改为 3 分钟：若前台 3 分钟内无可见产出，监工必须主动补一句短进度，这不等于任务必须 3 分钟内完成；同时要避免为了兜底频繁制造当前直聊里的可见内部消息。
- 用户明确：主会话稳定性不仅指文字可回复，也包括音频、视频、图片必须可查看、可播放、可打开；今后凡是涉及主会话附件/媒体链路的改动，都要以主会话里的真实可用作为硬性验收标准。
- 已定位并修复一处主会话语音附件问题：`tmp/voice-replies` 为软链、realpath 落在 workspace 外时，会被媒体安全检查拦为 `path-not-allowed`；当前最小修复是将生成的音频自动 stage/copy 回 workspace 内真实目录 `tmp/voice-replies-staged/` 后再返回给主会话。
- 用户进一步统一概念：在当前 WebChat / Control UI 直聊场景里，“主会话”就指当前正在对话的这个会话；凡是“回主会话 / 向主会话汇报 / 发回前台”，都必须绑定当前会话，不能再混同为内部 `agent:main:main` 之类的基础会话。
- 用户明确当前直聊里的兜底/状态汇报不应过于频繁；可见 watchdog 汇报阈值现统一为 3 分钟，并应尽量减少后台分身完成时对前台的无谓插播，以降低噪音与 token 消耗。
- 语音回复当前已形成可测试的功能链路：分块生成、打包成当前会话可直接发送的 `agentReply`、以及 workspace 内真实可访问媒体路径都已打通；“硬默认”插件原型已存在但尚未正式挂入运行配置，用户同意先测试功能效果，再决定是否正式启用。
- 用户对多段语音的最新偏好已收敛为：若某段句子足够长、结构完整、停顿自然，则保持正常语速；若某段较短，则只轻微降速约 4%，不要再按 8% 大幅放慢。当前听感上第二段通常最接近理想状态；首段和尾段更容易出现吞字、清晰度下降或“没说完”的感觉，因此除轻微降速外，还要把尾句文本本身写完整、把收尾放松，避免戛然而止。
