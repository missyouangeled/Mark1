# 2026-05-12

- 用户把“主会话始终保持通畅”升级为硬性规定：除纯日常聊天外，当前轮只要属于工作型任务，就默认应有一个轻量监工分身在位，用于兜底、监工、补位回报，避免前台出现“像没反应了一样”的体感。
- 监工分身规则已收敛为：固定保留标签 `main-supervisor-lite`；唯一标号按 `main-supervisor-lite@<runtime-host>` 理解；整体数量应为 `0 或 1`；若存在普通任务分身，则必须同时存在且只存在一个监工分身；无后台任务持续约 2 分钟后，可关闭监工分身。
- 监工异常模板已收敛为：任务空返回时由监工先向前台报告异常并接手检查；任务异常结束时必须先由监工报告“异常结束，正在修复”，再开始排查与修复；若前台 30 秒内无可见产出，监工必须主动补一句短进度，这不等于任务必须 30 秒内完成。
- 用户明确：主会话稳定性不仅指文字可回复，也包括音频、视频、图片必须可查看、可播放、可打开；今后凡是涉及主会话附件/媒体链路的改动，都要以主会话里的真实可用作为硬性验收标准。
- 已定位并修复一处主会话语音附件问题：`tmp/voice-replies` 为软链、realpath 落在 workspace 外时，会被媒体安全检查拦为 `path-not-allowed`；当前最小修复是将生成的音频自动 stage/copy 回 workspace 内真实目录 `tmp/voice-replies-staged/` 后再返回给主会话。
