# 2026-03-31

- 用户给我定了称呼：以后我叫“贾维斯”。
- 用户要求：以后每次做完系统性修改，都要及时把相关配置提交并推送到 GitHub 备份。
- 已将工作区 Git 仓库连接到 GitHub SSH 仓库 `git@github.com:missyouangeled/test-git.git`；用户会通过 SSH key + ssh-agent 方式推送，断网/重启后可能需要重新载入 agent。
- 已为 OpenClaw 配置启动提醒：通过 `BOOT.md` 在网关启动后向主聊天路由发送“贾维斯已上线，我在。要开始干活的话，直接喊我。”
- 已配置休眠恢复方案：用户级 systemd 的 `openclaw-resume-watch.service` / `.timer` 与脚本 `scripts/openclaw-resume-watch.sh` 会在检测到休眠/长时间断档后自动重启 `openclaw-gateway.service`，再触发启动提醒。
- 已把本机自动睡眠改为：插电和电池都永不自动睡眠；但息屏/锁屏仍可单独存在，且“只息屏不睡眠”时 OpenClaw 大概率保持在线。
