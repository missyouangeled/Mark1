# 2026-05-18

- 用户今天将 `broker / infos-handle` 明确设为最高优先级，要求别的先不用管，全力推进到“最终完整版本”。
- 用户明确指出监工如果每段任务完成后都要靠他手动提醒续挂，就失去意义；偏好改为：完成后先等待接续任务，若窗口内出现新任务就继续盯，没有再自动关闭。
- 监工等待接续任务窗口已按用户要求从 3 分钟提高到 10 分钟；用户也明确在意这套等待机制不要持续高烧 token。
- 用户中途表达过：更想尽快把这块做好，因为后面还想继续做“一个自己的贾维斯，能随时陪着他”的长期方向；这条动机对当前主线有持续意义。
- 用户还临时提过想查 NVIDIA 官方模型目录里有没有 `GLM-5.1`，以及是否可复用现有 NVIDIA API key 接入；但他同时又要求当前最高优先级仍是 `broker / infos-handle`。
# 2026-05-18

- 用户今天将 `broker / infos-handle` 明确设为最高优先级，要求别的先不用管，全力推进到“最终完整版本”。
- 用户明确指出监工如果每段任务完成后都要靠他手动提醒续挂，就失去意义；偏好改为：完成后先等待接续任务，若窗口内出现新任务就继续盯，没有再自动关闭。
- 监工等待接续任务窗口已按用户要求从 3 分钟提高到 10 分钟；用户也明确在意这套等待机制不要持续高烧 token。
- 用户中途表达过：更想尽快把这块做好，因为后面还想继续做“一个自己的贾维斯，能随时陪着他”的长期方向；这条动机对当前主线有持续意义。
- 用户还临时提过想查 NVIDIA 官方模型目录里有没有 `GLM-5.1`，以及是否可复用现有 NVIDIA API key 接入；但他同时又要求当前最高优先级仍是 `broker / infos-handle`。
- 今天后续又明确把下一阶段扩展目标定成三项：`更丰富的 image/audio`、`Control UI 直连 infos-handle`、`HTTP/SSE 入口`；优先顺序是先 richer image/audio，再接 Control UI，再补最小 sidecar HTTP/SSE。
- 当前已确认：`scripts/openclaw-infos-handle-sidecar.py` 已存在，是 infos-handle 的最小 loopback HTTP/SSE bridge；Control UI 品牌补丁也已预埋 `infosHandleSummaryHref / infosHandleContractHref / infosHandleSseHref`，默认指向 `127.0.0.1:18790`。
- 当前缺口也已确认：仓库里还没有与 infos-handle sidecar 对应的正式 README 与 user systemd service 模板；下一可写轮次优先补 `tools/openclaw-infos-handle-sidecar/README.md` 与 `tools/openclaw-infos-handle-sidecar/openclaw-infos-handle-sidecar.service`，把这条入口从“已有脚本”推进到“可重复启用的正式落点”。
