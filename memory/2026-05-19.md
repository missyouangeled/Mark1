# 2026-05-19 每日记录

## 上午 09:09 — 后台任务异常修复

- 用户反馈：后台任务 `c6bde960` 异常结束，要求查原因并修复
- 诊断结果：子分身 `c6bde960`（broker/infos-handle 方向2→3→4 父任务 `156c2707` 的子分身）在给 `openclaw-infos-handle-sidecar.py` 做加固时超时（08:42→09:07）
- 根因：sidecar 服务端类 `_sse_lock` 重命名为 `_lock`，但 `_handle_sse()` 两处仍引用 `self.server._sse_lock`，SSE 流测试 AttributeError 崩溃
- 修复：改 `_sse_lock` → `_lock`（line 409, 430）
- 全量测试通过，提交推送：`632fe5d` / `e88aff8`

## 上午 09:21 — broker/infos-handle 项目状态检查

- 用户问：项目完成到什么地步，自己网站/App 要数据能不能正常回
- 确认：已到"本机可用"阶段。broker=数据层+compat壳，infos-handle=正式请求层，sidecar 监听 127.0.0.1:18790
- Live 验证全通过：healthz/query/handle/SSE 正常
- 结论：本机 consumer 可用；外部网站/App 还不能直接访问（缺鉴权、反代、公网入口）

## 上午 09:26 — 跨模型续接总结

- 用户要求一份"换任何 AI 模型都能继续"的详细总结
- 写入 `HANDOFF.md` 新章节：`broker / infos-handle 当前版本总纲（跨模型续接版）`
- 含：定位/完成度/可用范围/主入口/验证结果/续做方向/最短接手路径/禁止事项
- 提交：`c7fc123`

## 上午 09:31 — 对接暗号

- 用户要一个"对接暗号"方便换模型时快速开工
- 确认：**继续 broker / infos-handle**
- 写入 HANDOFF.md，提交：`07a15e0`

## 上午 10:25 — 增强轮 5 收口

- 完成 4 个方向：默认 image card 升 v3、`events.timeline`、`health.detail`、recovery `assistant_text_mismatch` 30 秒追赶窗口
- 全量测试通过，提交推送：`88e1123`
- 当前 contract 版本：`QUERY_CONTRACT_VERSION=18`、`REQUEST_CONTRACT_VERSION=6`

## 上午 10:36 — 用户确认继续做公网/跨设备/统一入口/鉴权

- 用户明确要求：公网接口、跨设备直连、Gateway 主链级 HTTP 入口、完整鉴权都继续做
- 最终落地为：sidecar 共享 Gateway Bearer 鉴权 + `0.0.0.0:18790` LAN 直连 + Caddy 统一入口 `:18788`（Gateway `:18789` + sidecar `:18790`）
- 初版提交：`9c4a6b0`

## 上午 11:03 — 项目完善度评估

- 用户要求全面检查 broker / infos-handle 是否算完善
- 检查结论：已是正式可用项目，但还不算完善
- 发现的 4 个收口项：
  1. 统一入口 `:18788` 存在鉴权绕过（反代后 sidecar 误把远程请求看成 localhost）
  2. 文档与真实代码漂移（README 仍写 loopback-only；HANDOFF 写默认 v3 但代码默认仍 v2）
  3. 仓库里的 unified proxy service 模板与现场实际可运行版本不完全一致
  4. 缺少针对代理鉴权与安装模板的一致性回归

## 上午 11:12 — 4 个收口项全部补完

- 修复统一入口鉴权：Caddy 显式透传 `X-Forwarded-For` / `X-Real-IP`；sidecar 仅在请求来自 loopback 代理时才信任这两个头，localhost 直连继续免鉴权，远程/LAN 必须 Bearer token
- 默认 image preset 改为 `summary-card-v3`；默认 `handle --format image` 现在真实返回 `image.summary-card.v3`（`cardVersion=4`）
- 修正 unified proxy 的 repo 模板，并新增 `tools/openclaw-infos-handle-gateway-proxy/README.md`
- 同步更新 `HANDOFF.md`、`TOOLS.md`、`tools/openclaw-infos-handle-sidecar/README.md`、`tools/openclaw-frontstage-broker/README.md`
- 全量回归再次通过，并做 live 验证：`proxy-remote-no-auth=401`、`proxy-remote-with-auth=200`
- 收口提交推送：`9d1018b`

## 上午 11:24 之后 — 继续补第二轮

- 用户继续要求往下补：优先补 `rate limit`、统一入口 `install/verify` 脚本化，以及 HTTPS/TLS 切换入口
- 新增 sidecar 远程限流：仅对非 localhost 客户端生效，默认 `120 req / 60s`；本地 Control UI / 本机脚本不受影响
- sidecar `healthz` 与 server stats 现可返回 `remoteRateLimit` 配置；429 会带 `Retry-After`
- `scripts/test-openclaw-infos-handle.py` 已补远程代理鉴权 + 远程 rate limit 回归，保持 `ALL PASS`
- 新增 `scripts/apply-openclaw-infos-handle-gateway-proxy.py`：可统一完成 Caddyfile 生成（HTTP / HTTPS 模式）、user systemd 安装、reload/restart、以及本地/远程口径 verify
- 更新 `tools/openclaw-infos-handle-gateway-proxy/README.md`、`tools/openclaw-infos-handle-sidecar/README.md`、`TOOLS.md` 与 `docs/公司-Linux-OpenClaw-维护说明.md`
- live verify 结果：
  - `sidecar-local-no-auth=200`
  - `sidecar-remote-no-auth=401`
  - `sidecar-remote-with-auth=200`
  - `proxy-local-no-auth=200`
  - `proxy-remote-no-auth=401`
  - `proxy-remote-with-auth=200`
- apply 脚本 verify 也通过：本地入口正常、LAN 远程无 token 为 401、有 token 为 200
- 收口提交推送：`ea8f008`

## 上午 11:45 之后 — 升级后可用性保障继续补

- 用户明确强调：**OpenClaw 升级以后，这套 broker / infos-handle / sidecar / unified proxy 也要继续能用**
- 据此继续扩充 `scripts/openclaw-post-upgrade-self-check.py`，不再只看 branding / broker / recovery，而是把以下内容纳入升级后自检：
  - `infos_handle_sidecar_live`
  - `openclaw-infos-handle-sidecar.service`
  - `openclaw-unified-proxy.service`
  - `infos_handle_unified_proxy_verify`
  - `test-openclaw-infos-handle.py`
  - `test-infos-handle-frontstage-callers.py`
- 同步更新 `docs/通用-OpenClaw-升级后自检清单.md`，把 sidecar live、proxy verify、service active 状态纳入正式升级后检查项
- `openclaw-post-upgrade-self-check.py --force --print-human` 实测全绿，当前输出已明确包含：
  - sidecar live PASS
  - unified proxy verify PASS
  - sidecar/proxy user service PASS
  - infos_handle_test PASS
  - infos_handle_callers_test PASS
- 当前升级后自检总结已升级为：**关键补丁、broker、infos-handle、sidecar 与统一入口都正常**
- 收口提交推送：`45780ed`

## 上午 11:57 之后 — 正式写入 v1 收口说明

- 用户认可“内部版已经完整，但公网正式版不在 v1 范围内”的判断，要求把这条线的正式收口说明写出来
- 已把 `broker / infos-handle v1 收口说明` 写入 `PLANS.md`
- 当前在方案层正式明确：
  - v1 已完成范围
  - v1 不包含范围
  - 以后默认如何理解这条线
  - 若继续到 v2+ 的优先顺序
- 目标是避免以后继续反复争论“现在到底算没算做完”

## 用户偏好记录
- 倾向本机 consumer 方案，暂不急做公网 API
- 重视文档可跨模型续接的能力
- 对 broker / infos-handle 这条线，偏好先把统一入口、鉴权、模板一致性这些"最后一圈工程收口"补扎实，再谈“完善”
- 对“是否完整”的判断，更在意**升级以后还能不能继续稳定用**，而不是单纯当前能跑
