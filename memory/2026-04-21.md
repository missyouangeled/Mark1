# 2026-04-21

- 配置了本地语音回复能力：在 `tools/voice-reply/` 下使用用户态 `msedge-tts`，新增脚本 `tools/voice-reply/tts.mjs`，可本地生成语音文件。
- 初始语音输出格式选用 `.webm`，后经排查发现当前 OpenClaw / Control UI 对 `.webm` 的音频识别链路不稳定；改测 `.mp3` 后，本地文件生成与 `assistant-media` 访问均正常。
- 排查结论：Control UI 前端本身支持音频附件与 `[[audio_as_voice]]` 渲染；`MEDIA:` 也有真实实现。问题不在“前端完全不支持音频”，而在当前会话里本地语音附件的可用性/判定链路上仍不稳定。
- 对 `/__openclaw__/assistant-media` 做了链路核验：对测试 mp3 返回过 `available:true`、`HEAD 200`、`Content-Type: audio/mpeg`，并补到支持 `Accept-Ranges: bytes` / `206 Partial Content`。
- 但用户在实际 Control UI 中看到新发的 mp3 语音消息仍显示 `Unavailable / Attachment unavailable`。因此截至本次压缩前，"语音回复在当前 Control UI 可稳定使用" 仍未被证实，不能对用户说已经完全可用。
- 用户明确指出“还是无法播放/Unavailable”后，需要记住：不要把“本地生成成功”或“后端接口单测通过”误说成“当前聊天界面可正常收听语音”。
# 2026-04-21

- 配置了本地语音回复能力：在 `tools/voice-reply/` 下使用用户态 `msedge-tts`，新增脚本 `tools/voice-reply/tts.mjs`，可本地生成语音文件。
- 初始语音输出格式选用 `.webm`，后经排查发现当前 OpenClaw / Control UI 对 `.webm` 的音频识别链路不稳定；改测 `.mp3` 后，本地文件生成与 `assistant-media` 访问均正常。
- 排查结论：Control UI 前端本身支持音频附件与 `[[audio_as_voice]]` 渲染；`MEDIA:` 也有真实实现。问题不在“前端完全不支持音频”，而在当前会话里本地语音附件的可用性/判定链路上仍不稳定。
- 对 `/__openclaw__/assistant-media` 做了链路核验：对测试 mp3 返回过 `available:true`、`HEAD 200`、`Content-Type: audio/mpeg`，并补到支持 `Accept-Ranges: bytes` / `206 Partial Content`。
- 但用户在实际 Control UI 中看到新发的 mp3 语音消息仍显示 `Unavailable / Attachment unavailable`。因此截至本次压缩前，"语音回复在当前 Control UI 可稳定使用" 仍未被证实，不能对用户说已经完全可用。
- 用户明确指出“还是无法播放/Unavailable”后，需要记住：不要把“本地生成成功”或“后端接口单测通过”误说成“当前聊天界面可正常收听语音”。
- 用户今天明确澄清了其语音需求：不是继续调音调/情绪/唱腔，而是“只换音色”，即保留接近标准真人女声说话的语气语调，只把 timbre / 声纹换成其提供且已授权的那个人声音色；后续不要把“音色”和“音调/语气”混在一起调。
- 直接把歌曲人声当 `ref-audio` 做 Noiz 合成会把唱歌里的高音区、唱腔和语调一起带进来，用户明确不接受这种结果；正确方向应优先是“先固化音色，再用中性正常说话模板驱动”，而不是继续靠 pitch / emo / 后期修音去硬拧。
- Noiz 的 custom voice 路线在概念上更符合“只换音色”，但当前账号创建自定义音色时返回 `credit limit exceeded`，所以这条路暂时被额度阻塞。
- 已切换到用户要求的“本地免费路线”：用 `uv` 安装了用户态 Python/依赖栈，并在 `~/.local/share/openclaw-voice-venv311` 建好 Python 3.11 + Coqui TTS / XTTS 本地环境；首次下载 XTTS 模型时卡在 Coqui 许可确认，需用户明确同意按其非商业 CPML 继续后才能真正跑出本地“只换音色”测试样音。
- 新增可复用的本地 XTTS 语音回复脚本 `tools/voice-reply/local-xtts-reply.sh`，已在本机实测跑通并更新 `TOOLS.md`，对应提交已推送：`57e938e`。
