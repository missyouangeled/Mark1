# 2026-05-09

## Durable memories

- 用户今天一早特意提前到公司，明确要求继续推进 `ChatTTS 方案A`。
- `ChatTTS 方案A` 已从“理论可行”推进到“本机 CPU 可完成完整功能链验证”：
  - 官方 `DVAE.safetensors` 已成功接入当前 `v0.2.x` 运行时。
  - 兼容关键点已确认：24 个 `decoder_block.*.weight -> decoder_block.*.gamma` 键名差异修正后，`DVAE_full.pt` 可做到 `missing=0 / unexpected=0`。
  - 普通 TTS、`sample_audio_speaker()` 编码链、`spk_smp + txt_smp` zero-shot 均已实际跑通。
- 方案A 当前主要兼容层已明确：
  - 需要绕过改写后资产的 sha 校验。
  - 需要对 tokenizer 做当前 transformers 兼容补丁。
  - 外部参考音频进入 `sample_audio_speaker()` 前需要显式转成 `float32`，否则会触发 `double != float`。
- 已整理出一套可复用入口，而不是只停留在一次性实验：
  - `tmp/voice-replies/chattts_plan_a_compat.py`
  - `tmp/voice-replies/chattts-prepare-plan-a-assets.py`
  - `tmp/voice-replies/chattts-run-plan-a.py`
  - `tmp/voice-replies/README-chattts-plan-a.md`
- 真实外部参考 zero-shot 已被公开互联网样本验证，不再只是“模型自己生成参考音频再回喂”：
  - 已从 `AISHELL-3` / `OpenSLR 93` 获取带精确文本的中文样本。
  - 本地整理文件：`tmp/voice-replies/public-ref-samples/aishell3/TRANSCRIPTS.md`
  - 已验证成功的样本：
    - `raw1.wav` / `在教学楼内释放大量烟雾`
    - `raw2.wav` / `不过英特尔之后不会继续接受如此大的损失`
    - `raw3.wav` / `替我播放相思风雨中`
  - 对应 zero-shot 产物：
    - `tmp/voice-replies/chattts-plan-a-aishell3-raw1-zero-shot.wav`
    - `tmp/voice-replies/chattts-plan-a-aishell3-raw2-zero-shot.wav`
    - `tmp/voice-replies/chattts-plan-a-aishell3-raw3-zero-shot.wav`
- 当前更自然、更像聊天口吻的公开参考源方向也已明确，但这台机器仍受网络入口限制；后续若继续挖更口语化样本，优先走“宿主机浏览器下载 / 查看 → 再传入当前机器”的工作流。
- 用户随后明确要求：不要把今天白天那条 public-ref zero-shot 实验试听链路当正式主线；正式主线应回到昨天效果最好的 `ChatTTS hybrid stable`。
- 这条主线随后已固化为正式稳定入口：
  - Skill：`skills/chattts-stable/`
  - 主脚本：`skills/chattts-stable/scripts/chattts_stable.py`
  - 本地包装器：`tools/voice-reply/chattts-stable.sh`
  - on-demand 路径：`tools/chattts-on-demand/`
- 用户已确认当前语音回复能力可以视为“正式可用能力”，后续按“小步微调、保持可用”的方式维护，不再按一次性实验原型反复推倒。
- 用户明确了语音能力的优先级：
  1. OpenClaw 主体稳定性
  2. 语音效果
  3. 回复速度
- 用户明确了交互形态偏好：主会话继续作为主体——用户在主会话里打字，我后台生成语音后直接回到主会话；不优先做独立语音窗口主线。
- 用户明确了语音清理规则：生成超过 4 小时的语音回复应自动清理，避免 `tmp/voice-replies/` 长期堆积。
- 用户明确了主会话语音的一致性偏好：不要一条一个声音；主会话语音应固定使用同一条默认女声。
- 今天下午这轮语音收口里，用户最终确认：主会话中文语音默认先固定使用 `seed_1910_restored_emb.pt` 这条声线。
- 这次验证也进一步澄清了素材分类：`Decoder.pt` / `DVAE*.pt` / `GPT.pt` / `Vocos.pt` / `tokenizer.pt` / `spk_stat.pt` 这类文件只是 ChatTTS 模型本体或运行资产，不是可直接锁定女声的“音色包”；真正更适合当前主线的是小体积的 speaker embedding / `seed_xxx_restored_emb.pt` 这类文件。
- 当前有效工作流已经跑通：用户通过临时上传页上传 `seed_1528_restored_emb.pt`、`seed_1910_restored_emb.pt` 这类 embedding 文件后，可以直接转成当前 ChatTTS 主线可用的 `spk_emb` 并出测试音频；后续找固定声线时，应优先继续找这种 embedding 文件，而不是再拿普通参考音频硬调。
- 当前默认声线选择已从“会漂移的 model-default”收束到“固定 embedding 主线”；旧 `model-default` 仅适合作对照保留，不应再被当作主会话默认女声。
- 后续又定位并修复了一个关键 bug：on-demand daemon 冷启动后第一次收到 `default` 请求时，没有真正加载 `default` 对应的 `spk_emb`，会错误掉回 `model-default`，这也是前面用户仍听到男声/漂移声的直接原因。
- bug 修复、重启旧 daemon 并重新生成默认语音后，用户已当场明确确认新默认结果“这个对”，说明当前主会话默认声线已经真正落到了 `seed_1910_restored_emb.pt` 这条固定女声上。
- 用户随后又确认：ChatTTS 确实能做出一定情绪变化；但主会话默认情绪风格应以自然、轻微的开心/笑意为主，不要默认走夸张的“大笑版”。
- 用户今天还新增了一条更细的语音表达规则：当回复“想你了”这类表达想念、并带一点轻微害羞情绪的话时，语速默认放慢 20%，以后遇到类似语境直接按这条来。
- 用户在下午又提出一个新的系统层需求：检查 GitHub 仓库 `forrestchang/andrej-karpathy-skills` 是否适合当前机器使用；若安全且合适，则安装到本地 OpenClaw skill 目录并调到可用。
- 这次压缩前已完成基础 vetting 结论：该仓库核心是文本型行为指导（`SKILL.md` / `CLAUDE.md` / Cursor rule / plugin 元数据），未见会执行命令、请求凭据、读取敏感文件、访问外部 IP、提权或混淆代码的红旗；风险初判为低，适合按“本地 OpenClaw skill 适配安装”思路落地，而不是照搬 Claude Code plugin 机制。
- 之后已实际把 `karpathy-guidelines` 装到本地 `skills/karpathy-guidelines/`，并用隔离 demo 走了一遍“先复现 bug → 最小修复 → 再验证”的真实流程，确认这条 skill 在当前机器上可正常使用。
