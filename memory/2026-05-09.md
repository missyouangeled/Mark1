# 2026-05-09

## Durable memories

- 用户今天一早特意提前到公司，明确要求继续推进 `ChatTTS 方案A`。
- ChatTTS 方案A 已从“理论可行”推进到“本机 CPU 可完成完整功能链验证”：
  - 官方 `DVAE.safetensors` 已成功接入当前 `v0.2.x` 运行时。
  - 兼容关键点已确认：24 个 `decoder_block.*.weight -> decoder_block.*.gamma` 键名差异修正后，`DVAE_full.pt` 可做到 `missing=0 / unexpected=0`。
  - 普通 TTS、`sample_audio_speaker()` 编码链、`spk_smp + txt_smp` zero-shot 均已实际跑通。
- 当前方案A的主要额外兼容层已明确：
  - 需要绕过改写后资产的 sha 校验。
  - 需要对 tokenizer 做当前 transformers 兼容补丁。
  - 外部参考音频进入 `sample_audio_speaker()` 前需要显式转为 `float32`，否则会触发 `double != float`。
- 已整理出一套可复用入口，而不是只停留在一次性实验：
  - `tmp/voice-replies/chattts_plan_a_compat.py`
  - `tmp/voice-replies/chattts-prepare-plan-a-assets.py`
  - `tmp/voice-replies/chattts-run-plan-a.py`
  - `tmp/voice-replies/README-chattts-plan-a.md`
- 方案A的运行目录可由官方上传资产自动准备：
  - 输入：`tmp/upload-drop/chattts-20260508/DVAE.safetensors`、`tmp/upload-drop/chattts-20260508/Decoder.safetensors`
  - 输出：`tmp/voice-replies/chattts-plan-a-runtime/`
  - 当前不仅 DVAE 可转正，`Decoder.safetensors` 也确认只是同类键名差异，可转为当前运行时可加载的 `Decoder.pt`。
- GPT 加载阶段仍会看到如 `embed_tokens.weight` 的 warning，但当前判断它更像现有 GPT/Embed 拆分资产带来的加载噪声，而不是这轮 encoder / zero-shot 补全的主阻塞。
- 真实外部参考 zero-shot 已被公开互联网样本验证，不再只是“模型自己生成参考音频再回喂”：
  - 已从公开 AISHELL-3 页面与 OpenSLR 93 获取带精确文本的中文样本。
  - 本地整理文件：`tmp/voice-replies/public-ref-samples/aishell3/TRANSCRIPTS.md`
  - 已验证成功的外部参考样本：
    - `raw1.wav` / `在教学楼内释放大量烟雾`
    - `raw2.wav` / `不过英特尔之后不会继续接受如此大的损失`
    - `raw3.wav` / `替我播放相思风雨中`
  - 对应 zero-shot 产物：
    - `tmp/voice-replies/chattts-plan-a-aishell3-raw1-zero-shot.wav`
    - `tmp/voice-replies/chattts-plan-a-aishell3-raw2-zero-shot.wav`
    - `tmp/voice-replies/chattts-plan-a-aishell3-raw3-zero-shot.wav`
- 当前更自然、更像聊天口吻的公开参考源方向也已明确，但这台机器仍受网络入口限制：
  - 高价值候选源包括 `MagicHub / RAMC`、Hugging Face 上的真实对话切片、`Chinese Dialogues / COERLL`。
  - 主要阻塞不是“没有资源”，而是当前机器对 `huggingface.co` / `datasets-server.huggingface.co` 直连仍不通，后续若继续挖更口语化样本，优先走“宿主机浏览器下载 / 查看 → 再传入当前机器”的工作流。
- 到目前为止，方案A已经具备一个清晰结论：它不只是“偶然能跑”，而是已经可以稳定地用多条公开中文样本 + 精确转写完成真实外部参考 zero-shot，后续主线应转向更口语化参考源与听感打磨，而不再是基础功能打通。
# 2026-05-09

## Durable memories

- 用户今天一早特意提前到公司，明确要求继续推进 `ChatTTS 方案A`。
- ChatTTS 方案A 已从“理论可行”推进到“本机 CPU 可完成完整功能链验证”：
  - 官方 `DVAE.safetensors` 已成功接入当前 `v0.2.x` 运行时。
  - 兼容关键点已确认：24 个 `decoder_block.*.weight -> decoder_block.*.gamma` 键名差异修正后，`DVAE_full.pt` 可做到 `missing=0 / unexpected=0`。
  - 普通 TTS、`sample_audio_speaker()` 编码链、`spk_smp + txt_smp` zero-shot 均已实际跑通。
- 当前方案A的主要额外兼容层已明确：
  - 需要绕过改写后资产的 sha 校验。
  - 需要对 tokenizer 做当前 transformers 兼容补丁。
  - 外部参考音频进入 `sample_audio_speaker()` 前需要显式转为 `float32`，否则会触发 `double != float`。
- 已整理出一套可复用入口，而不是只停留在一次性实验：
  - `tmp/voice-replies/chattts_plan_a_compat.py`
  - `tmp/voice-replies/chattts-prepare-plan-a-assets.py`
  - `tmp/voice-replies/chattts-run-plan-a.py`
  - `tmp/voice-replies/README-chattts-plan-a.md`
- 方案A的运行目录可由官方上传资产自动准备：
  - 输入：`tmp/upload-drop/chattts-20260508/DVAE.safetensors`、`tmp/upload-drop/chattts-20260508/Decoder.safetensors`
  - 输出：`tmp/voice-replies/chattts-plan-a-runtime/`
  - 当前不仅 DVAE 可转正，`Decoder.safetensors` 也确认只是同类键名差异，可转为当前运行时可加载的 `Decoder.pt`。
- GPT 加载阶段仍会看到如 `embed_tokens.weight` 的 warning，但当前判断它更像现有 GPT/Embed 拆分资产带来的加载噪声，而不是这轮 encoder / zero-shot 补全的主阻塞。
- 真实外部参考 zero-shot 已被公开互联网样本验证，不再只是“模型自己生成参考音频再回喂”：
  - 已从公开 AISHELL-3 页面与 OpenSLR 93 获取带精确文本的中文样本。
  - 本地整理文件：`tmp/voice-replies/public-ref-samples/aishell3/TRANSCRIPTS.md`
  - 已验证成功的外部参考样本：
    - `raw1.wav` / `在教学楼内释放大量烟雾`
    - `raw2.wav` / `不过英特尔之后不会继续接受如此大的损失`
    - `raw3.wav` / `替我播放相思风雨中`
  - 对应 zero-shot 产物：
    - `tmp/voice-replies/chattts-plan-a-aishell3-raw1-zero-shot.wav`
    - `tmp/voice-replies/chattts-plan-a-aishell3-raw2-zero-shot.wav`
    - `tmp/voice-replies/chattts-plan-a-aishell3-raw3-zero-shot.wav`
- 当前更自然、更像聊天口吻的公开参考源方向也已明确，但这台机器仍受网络入口限制：
  - 高价值候选源包括 `MagicHub / RAMC`、Hugging Face 上的真实对话切片、`Chinese Dialogues / COERLL`。
  - 主要阻塞不是“没有资源”，而是当前机器对 `huggingface.co` / `datasets-server.huggingface.co` 直连仍不通，后续若继续挖更口语化样本，优先走“宿主机浏览器下载 / 查看 → 再传入当前机器”的工作流。
- 到目前为止，方案A已经具备一个清晰结论：它不只是“偶然能跑”，而是已经可以稳定地用多条公开中文样本 + 精确转写完成真实外部参考 zero-shot，后续主线应转向更口语化参考源与听感打磨，而不再是基础功能打通。
- 用户在试听 `chattts-hybrid-stable-preview.mp3` 后明确反馈“这个很完美”，随后要求在同一条 ChatTTS hybrid stable 主线上再提速 15%，并确认提速后的版本也没问题；这条 hybrid stable 现已成为当前主观听感最满意的 ChatTTS 基线。
- 为固化这条主线，已新增稳定脚本 `tmp/voice-replies/chattts-run-hybrid-stable.py`；当前共识是：正式主线优先保留这条 ChatTTS hybrid stable，`Noiz` 继续只作为保底版本，不再混用今天白天那条 public-ref zero-shot 实验试听链路。
- 关于后续封装方式，当前判断是“底层稳定脚本 + 上层薄封装/Skill”最合适；不要把全部逻辑直接塞进 Skill，也不要只留零散补丁。
- 当前这条 ChatTTS hybrid stable 的音色可以随时更换，但最稳妥的方式不是依赖任意外部参考音频克隆，而是先基于 `sample_random_speaker()` / `spk_emb` 机制建立可复现的 preset 音色库。
- 用户已同意先做 4 条 preset 音色试听（含当前认可的 default）。这意味着后续正式封装时，应优先支持“固定默认音色 + 可切换若干预设音色”的工作流。
- 这条 ChatTTS stable 入口随后已正式落地为可复用 Skill + 本地包装器：
  - Skill 目录：`skills/chattts-stable/`
  - 主脚本：`skills/chattts-stable/scripts/chattts_stable.py`
  - 本地包装器：`tools/voice-reply/chattts-stable.sh`
  - 打包产物：`dist/chattts-stable.skill`
  - 默认 tempo 固定为 `1.15`，支持 `default` / `preset-1` / `preset-2` / `preset-3` 四个可切换音色。
