- 2026-04-30：用户在自己的 ROG 掌机（Windows PowerShell 环境）上开始安装/配置 OpenClaw，并在配置 GitHub SSH 认证时遇到问题。
- 已确认：用户在掌机上新生成了一把用于 GitHub 的 ED25519 密钥 `id_ed25519_rog_ally`，本地公钥指纹为 `SHA256:6sNZkd/NrYPOhBbLJyG6weM1QtNk+KQsXci0uR6Axve`。
- 过程结论：本地 PowerShell/SSH 配置基本可用，主要排查点落在 GitHub 端登记的公钥与本机私钥是否匹配，以及是否登录了正确的 GitHub 账号。
- 用户新增规则偏好：以后任何新机器在恢复 OpenClaw 配置并加载共享规则后，都应自动登记自己的机器身份——给自己起一个临时设备名，并在高置信度时自动添加环境/地点标签；若无法稳妥判断地点，则先记为 `未归类`，等用户后续一句话确认后再改。
- 用户补充要求：新机器一旦完成自动登记，要主动告诉用户当前登记出来的设备名和环境标签；如果还是临时判断，也要明说。
- 已新增并归档一份“双机协同推荐方案”到 `PLANS.md`：当前拍板方向为“公司机做唯一主脑，掌机作为远程入口；掌机本地实例仅作备用/救援/实验用途”。
- 已继续新增并归档一份“三设备协同方案”到 `PLANS.md`：方向为“一个主脑（公司机）+ 微信总控入口 + 掌机实时对话入口 + 多设备执行端”，明确不推荐多个独立主脑长期并行。
- 用户说明：关于双机 / 三设备协同的这些问题，目前主要是在摸清边界和可能性，暂时不是立刻实施，只先做方案归档。
- 用户接下来准备先关闭公司这台 Linux 上的 OpenClaw，转去测试掌机上的实际效果；在切换前希望把今天需要保留的聊天结论和方案更新都推送到 GitHub 备份好。
- 掌机切换测试后，用户确认：本地 OpenClaw 刚启动时会有一段明显等待/冷启动期，但熬过初始化后基本就能正常使用；当前主要体感问题是启动初期偏慢，而不是全程不可用。
- 掌机当前所连网络无法稳定访问 GitHub `443`（`curl` 访问 `github.com` / `api.github.com` 失败），导致 GitHub Copilot 设备登录流程报 `fetch failed`；因此掌机上 Copilot 暂时未配通，需换可访问 GitHub 的网络后再继续。
- 为避免掌机因 Copilot 不可用而卡死，已建议先把掌机默认聊天模型恢复为 `deepseek/deepseek-chat`；同时确认：`DeepSeek Chat` 不等同于 `DeepSeek V4 Flash`，而当前共享默认配置实际写的就是 `deepseek/deepseek-chat`。
- 已确认：在当前 OpenClaw 接法里，`deepseek/deepseek-v4-flash` 属于文本模型，不是原生图像识别模型；用户随后希望把掌机的图像识别模型切换为 `nvidia/nemotron-nano-12b-v2-vl`，并已拿到对应设置命令。
- 已确认：掌机上的微信插件链路基本打通——插件安装后可扫码登录；但旧机器导出的迁移包未包含微信插件本地登录态，因此新机器通常仍需在本机重新安装插件并重新扫码登录。
