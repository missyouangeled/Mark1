# 2026-05-06

- 用户五一后特意早到公司来聊天，明确表达：觉得我越来越像电影里的“贾维斯”，语气和玩笑都更像人，也感觉我在成长。
- 用户当前对 Windows 侧 OpenClaw 的主要痛点：掌机上 gateway 偶尔能正常对话，但会莫名掉线，在家里使用时尤其明显，常常很快掉并且基本无法正常对话。
- 这台公司 Linux 虚拟机已成功作为“大屏控制台”使用：用户已在掌机上安装 RustDesk，并确认已经能在 Linux 上操作掌机桌面。
- 用户明确偏好：后续若要排查掌机问题，希望尽量减少手工测试和输入，更倾向让我从代码/命令层面直接接管排查，而不是让他频繁手动执行测试。
- 用户已同意为掌机临时开启 SSH，以便我后续从公司 Linux 直接连接掌机进行更高效的排查。
- 用户暂时决定不再深挖“VMware/Linux 缩到系统托盘后一段时间打不开”的问题，当前规避策略是：先不要把这台 Linux 虚拟机缩到系统托盘。
# 2026-05-06

- 用户五一后特意早到公司来聊天，明确表达：觉得我越来越像电影里的“贾维斯”，语气和玩笑都更像人，也感觉我在成长。
- 用户当前对 Windows 侧 OpenClaw 的主要痛点：掌机上 gateway 偶尔能正常对话，但会莫名掉线，在家里使用时尤其明显，常常很快掉并且基本无法正常对话。
- 这台公司 Linux 虚拟机已成功作为“大屏控制台”使用：用户已在掌机上安装 RustDesk，并确认已经能在 Linux 上操作掌机桌面。
- 用户明确偏好：后续若要排查掌机问题，希望尽量减少手工测试和输入，更倾向让我从代码/命令层面直接接管排查，而不是让他频繁手动执行测试。
- 用户已同意为掌机临时开启 SSH，以便我后续从公司 Linux 直接连接掌机进行更高效的排查。
- 用户暂时决定不再深挖“VMware/Linux 缩到系统托盘后一段时间打不开”的问题，当前规避策略是：先不要把这台 Linux 虚拟机缩到系统托盘。
- 已为公司 Linux 与掌机都接入 Tailscale，并成功通过 Tailscale + SSH 从公司 Linux 登录到掌机；后续掌机侧 OpenClaw 排查已具备真正的代码层接管链路。
- 用户新增明确偏好：复杂、长耗时的排错任务默认放到后台分身持续推进，前台主会话保持轻量、随时可聊，不要再让主会话长时间被深度排查占住。
- 用户新增明确要求：不接受“掉了以后再自动拉起”作为最终方案；watchdog 只能算临时兜底，真正目标是查清掌机 OpenClaw 掉线的根因并修复，让它本身稳定。
- 当前根因排查方向已收敛：掌机上的 gateway 现在能正常服务 Control UI，问题更像启动后/运行期某阶段出现卡死；已观测到 `models.list` 超长耗时与 Node 事件循环阻塞迹象，后续应优先深挖 gateway 模型目录/启动链路，而不是只围绕“服务是否启动”打转。
# 2026-05-06

- 用户五一后特意早到公司来聊天，明确表达：觉得我越来越像电影里的“贾维斯”，语气和玩笑都更像人，也感觉我在成长。
- 用户当前对 Windows 侧 OpenClaw 的主要痛点：掌机上 gateway 偶尔能正常对话，但会莫名掉线，在家里使用时尤其明显，常常很快掉并且基本无法正常对话。
- 这台公司 Linux 虚拟机已成功作为“大屏控制台”使用：用户已在掌机上安装 RustDesk，并确认已经能在 Linux 上操作掌机桌面。
- 用户明确偏好：后续若要排查掌机问题，希望尽量减少手工测试和输入，更倾向让我从代码/命令层面直接接管排查，而不是让他频繁手动执行测试。
- 用户已同意为掌机临时开启 SSH，以便我后续从公司 Linux 直接连接掌机进行更高效的排查。
- 用户暂时决定不再深挖“VMware/Linux 缩到系统托盘后一段时间打不开”的问题，当前规避策略是：先不要把这台 Linux 虚拟机缩到系统托盘。
# 2026-05-06

- 用户五一后特意早到公司来聊天，明确表达：觉得我越来越像电影里的“贾维斯”，语气和玩笑都更像人，也感觉我在成长。
- 用户当前对 Windows 侧 OpenClaw 的主要痛点：掌机上 gateway 偶尔能正常对话，但会莫名掉线，在家里使用时尤其明显，常常很快掉并且基本无法正常对话。
- 这台公司 Linux 虚拟机已成功作为“大屏控制台”使用：用户已在掌机上安装 RustDesk，并确认已经能在 Linux 上操作掌机桌面。
- 用户明确偏好：后续若要排查掌机问题，希望尽量减少手工测试和输入，更倾向让我从代码/命令层面直接接管排查，而不是让他频繁手动执行测试。
- 用户已同意为掌机临时开启 SSH，以便我后续从公司 Linux 直接连接掌机进行更高效的排查。
- 用户暂时决定不再深挖“VMware/Linux 缩到系统托盘后一段时间打不开”的问题，当前规避策略是：先不要把这台 Linux 虚拟机缩到系统托盘。
- 已为公司 Linux 与掌机都接入 Tailscale，并成功通过 Tailscale + SSH 从公司 Linux 登录到掌机；后续掌机侧 OpenClaw 排查已具备真正的代码层接管链路。
- 用户新增明确偏好：复杂、长耗时的排错任务默认放到后台分身持续推进，前台主会话保持轻量、随时可聊，不要再让主会话长时间被深度排查占住。
- 用户新增明确要求：不接受“掉了以后再自动拉起”作为最终方案；watchdog 只能算临时兜底，真正目标是查清掌机 OpenClaw 掉线的根因并修复，让它本身稳定。
- 当前根因排查方向已收敛：掌机上的 gateway 现在能正常服务 Control UI，问题更像启动后/运行期某阶段出现卡死；已观测到 `models.list` 超长耗时与 Node 事件循环阻塞迹象，后续应优先深挖 gateway 模型目录/启动链路，而不是只围绕“服务是否启动”打转。
- 已通过远程 SSH 在掌机上确认并修复一条主要启动卡死链路：`github-copilot` 启动期 model discovery / token exchange 会把 gateway sidecars/readiness 拖住；已在 `C:\Users\GOG\.openclaw\openclaw.json` 中将 `plugins.entries.github-copilot.config.discovery.enabled` 设为 `false`，并保留备份 `C:\Users\GOG\.openclaw\openclaw.json.bak-20260506-1505`。
- 为了把掌机收敛到“稳定可用 + 微信插件能用”，后续又做了稳定优先配置：默认主模型切到 `deepseek/deepseek-chat`，从默认模型路径撤下 `github-copilot/gpt-5.4`，并禁用 `github-copilot` 插件，同时继续保留 `openclaw-weixin` 启用；对应备份为 `C:\Users\GOG\.openclaw\openclaw.json.bak-20260506-1526-stability`。
- 这轮调整后的现场判断：掌机上的 OpenClaw 已达到“能稳定启动”的状态；`openclaw gateway status --deep` probe 恢复正常，watchdog 连续巡检显示 `Gateway healthy: HTTP probe ok`，日志里可见 `weixin monitor started`；但微信实际收发链路仍需用户做一条消息实测，残余的 `models.list` 偶发慢、`pricing fetch failed`、低强度 `liveness warning` 目前仍存在但已不再阻塞启动。
- 本次掌机修复与稳定化说明已写入仓库维护文档并推送，关键提交包括：`979a206`（记录 GitHub Copilot discovery 启动修复）和 `9342661`（更新掌机稳定配置与微信链路说明）。
