# 2026-05-14

- 用户把“监工”路线正式改成**可开关的监工服务**模型：简单工作默认不开，复杂工程默认开启，且用户可随时明确要求“开监工服务 / 关监工服务”。
- 监工服务控制语义已收敛为：`policyMode=auto|force_on|force_off` + `taskActive=true|false`；当前默认基线为 `auto + taskActive=false`，复杂工程默认切到 `auto + taskActive=true`。
- 已落地最小监工脚本：`scripts/openclaw-supervisor-status.py`，状态输出到 `~/.local/state/openclaw/supervisor/supervisor-status.json`，控制文件为 `~/.local/state/openclaw/supervisor/service-control.json`。
- 监工脚本当前可表达的主要状态：`service.state = disabled|armed|active|stopping`，`status = idle|running|stalled|done|failed`。
- 本地健康层已接入监工状态：`scripts/openclaw-local-health-diagnose.py` 现在会输出 supervisor 信息，并在本地健康页面里显示“监工”卡片与状态明细。
- 已补齐公司（Linux）机器上的监工 systemd 模板：`tools/openclaw-supervisor/openclaw-supervisor-watch.service` / `tools/openclaw-supervisor/openclaw-supervisor-watch.timer`；当前 timer 已安装到 `~/.config/systemd/user/` 并启用，默认开机约 1 分钟后首次运行、之后约每 30 秒刷新一次监工状态。
- 监工分身管理入口已落地：`scripts/openclaw-supervisor-subagent.py`，当前可通过 gateway RPC 间接做 `list / spawn / kill / resolve-frontstage / send-frontstage`。
- 当前验证得到的稳定通路：`tools.invoke -> subagents` 可做 `list / kill`；`chat.send -> /subagents spawn ...` 可做 `spawn`；`chat.inject` 可做前台状态回报；`tools.invoke -> sessions_spawn` 当前会被 gateway 拒绝为 `Tool not available: sessions_spawn`。
- `main-supervisor-lite` 不再按旧规则默认永远常驻；现在应作为监工服务开启后、且确实需要聊天插播/额外协作时才按需拉起的补充层。
- 用户又补充了一个关键前台绑定要求：若 WebChat / Control UI 用户点 `+` 切到新的 dashboard 会话，监工/后台回报的“当前会话”也应随之前移；默认不要继续打回旧 dashboard 页，而应优先解析到同一父会话下最新的 dashboard 会话。
- 用户进一步澄清：这里所说的“主会话”在 WebChat / Control UI 直聊里，默认应理解为用户当前正在看的前台 dashboard 页面，而不是内部共享 owner 会话 `agent:main:main`；后者只应作为共享父会话/中枢，不应直接作为默认前台回报目标。
- 前台投递这层也已补成固定入口：`send-frontstage` 会先解析同一父会话下最新的 dashboard，再通过 gateway `chat.inject` 把监工状态直接落到那个前台页面，不额外触发模型回复。
- 用户随后认可了“先做辅助消息 broker、暂不强制接管正常主回复”的第一阶段路线；当前已新增 `scripts/openclaw-frontstage-broker.py`，统一接管 `supervisor` / `local-health` 这两类辅助消息，按 `source + eventKey` 去重后再发回当前 dashboard 前台。
- 自动回报闭环也已接上：`scripts/openclaw-supervisor-status.py --notify-transitions` 会在 `stalled / failed / done` 这些状态变化时，自动调用统一前台 broker，再由 broker 走 `send-frontstage` / `chat.inject` 回到当前 dashboard，并把最近一次已发事件记到 `notify-state.json` 里做去重；当前已分别对 `done / failed / stalled` 三种状态做过闭环验证，且已额外验证同一任务可先触发 `stalled` 回报、恢复后再触发 `done` 回报，`notify_sent` 事件与前台 assistant note 都已出现。
# 2026-05-14

- 用户把“监工”路线正式改成**可开关的监工服务**模型：简单工作默认不开，复杂工程默认开启，且用户可随时明确要求“开监工服务 / 关监工服务”。
- 监工服务控制语义已收敛为：`policyMode=auto|force_on|force_off` + `taskActive=true|false`；当前默认基线为 `auto + taskActive=false`，复杂工程默认切到 `auto + taskActive=true`。
- 已落地最小监工脚本：`scripts/openclaw-supervisor-status.py`，状态输出到 `~/.local/state/openclaw/supervisor/supervisor-status.json`，控制文件为 `~/.local/state/openclaw/supervisor/service-control.json`。
- 监工脚本当前可表达的主要状态：`service.state = disabled|armed|active|stopping`，`status = idle|running|stalled|done|failed`。
- 本地健康层已接入监工状态：`scripts/openclaw-local-health-diagnose.py` 现在会输出 supervisor 信息，并在本地健康页面里显示“监工”卡片与状态明细。
- 已补齐公司（Linux）机器上的监工 systemd 模板：`tools/openclaw-supervisor/openclaw-supervisor-watch.service` / `tools/openclaw-supervisor/openclaw-supervisor-watch.timer`；当前 timer 已安装到 `~/.config/systemd/user/` 并启用，默认开机约 1 分钟后首次运行、之后约每 30 秒刷新一次监工状态。
- 监工分身管理入口已落地：`scripts/openclaw-supervisor-subagent.py`，当前可通过 gateway RPC 间接做 `list / spawn / kill / resolve-frontstage / send-frontstage`。
- 当前验证得到的稳定通路：`tools.invoke -> subagents` 可做 `list / kill`；`chat.send -> /subagents spawn ...` 可做 `spawn`；`chat.inject` 可做前台状态回报；`tools.invoke -> sessions_spawn` 当前会被 gateway 拒绝为 `Tool not available: sessions_spawn`。
- `main-supervisor-lite` 不再按旧规则默认永远常驻；现在应作为监工服务开启后、且确实需要聊天插播/额外协作时才按需拉起的补充层。
- 用户又补充了一个关键前台绑定要求：若 WebChat / Control UI 用户点 `+` 切到新的 dashboard 会话，监工/后台回报的“当前会话”也应随之前移；默认不要继续打回旧 dashboard 页，而应优先解析到同一父会话下最新的 dashboard 会话。
- 用户进一步澄清：这里所说的“主会话”在 WebChat / Control UI 直聊里，默认应理解为用户当前正在看的前台 dashboard 页面，而不是内部共享 owner 会话 `agent:main:main`；后者只应作为共享父会话/中枢，不应直接作为默认前台回报目标。
- 前台投递这层也已补成固定入口：`send-frontstage` 会先解析同一父会话下最新的 dashboard，再通过 gateway `chat.inject` 把监工状态直接落到那个前台页面，不额外触发模型回复。
- 用户随后认可了“先做辅助消息 broker、暂不强制接管正常主回复”的第一阶段路线；当前已新增 `scripts/openclaw-frontstage-broker.py`，统一接管 `supervisor` / `local-health` 这两类辅助消息，按 `source + eventKey` 去重后再发回当前 dashboard 前台。
- 自动回报闭环也已接上：`scripts/openclaw-supervisor-status.py --notify-transitions` 会在 `stalled / failed / done` 这些状态变化时，自动调用统一前台 broker，再由 broker 走 `send-frontstage` / `chat.inject` 回到当前 dashboard，并把最近一次已发事件记到 `notify-state.json` 里做去重；当前已分别对 `done / failed / stalled` 三种状态做过闭环验证，且已额外验证同一任务可先触发 `stalled` 回报、恢复后再触发 `done` 回报，`notify_sent` 事件与前台 assistant note 都已出现。
- 第一阶段后来又继续补齐了 `local-health` 的统一投递：`scripts/openclaw-local-health-diagnose.py` 新增 `--notify-frontstage`，只在 `warn / critical / recovered` 这些状态变化时通过 `scripts/openclaw-frontstage-broker.py` 发前台消息，不会周期性“报平安”；相关 user service 模板也已改为带上这个参数。
- 用户明确同意第二阶段先走**shadow watcher**，即先做“检测与补位观察”，不直接拦截正常主回复；目标是盯住“live 里出现过但最终没稳定留在前台/history”的回复，以及“最终可见文本异常、只剩英文控制片段/奇怪字符/空壳”的情况。
- 用户又进一步明确了一个新的前台体验硬要求：只要后台还没真正结束，前台至少也必须有一个“正在进行”的可见信号（例如转圈/进行中图标）；如果后台还在跑但前台完全没反馈，这本身就应视为失败，而不是可接受的偶发现象。
- 针对上面这条，当前已定位到一个 Control UI 前台状态洞：聊天页“进行中”原先主要只看 `sending` 和 `stream !== null`，会漏掉 tool-call 阶段、等待 toolResult、本地队列未清空、run 仍可 abort 但暂时没流字的情况；因此后来把“进行中”判断补成同时考虑 `sending / stream / canAbort / queue.length > 0`，用来减少“后台仍在工作但前台像死了”的无反馈窗口。
- 用户随后又明确补充了一条长期要求：像这种在他提前诉求后做的修补，不能只在当前会话里临时生效；默认应做成刷新/换会话/重启 gateway/切换模型后仍可继续生效的正式补丁，并且要有稳定记录，方便以后继续改动时能快速找到落点。
- 据此，前台“进行中”状态修补不再只允许停留在手工改一次当前 `dist/control-ui/assets/index-*.js` 的层面，而是应并入 `scripts/apply-openclaw-control-ui-branding.py` 这条已有的可重复应用补丁链路；这样公司 Linux 机上每次 gateway 启动前都会自动重打这条补丁，刷新/新会话/换模型不会把它丢掉。
- 用户随后要求继续把 `broker / watcher` 这条线也按同样的“持久生效”标准过一遍，并继续做 broker 第二阶段；当前审计结论是：`broker` 本体、`supervisor`、`local-health` 已经接入长期链路，但 `frontstage-recovery watcher` 之前还只是“脚本能跑”，没有稳定自动触发与去重状态机。
- 据此，第二阶段已继续补成正式链路：
  - `scripts/openclaw-frontstage-recovery-watch.py` 现在除了生成 `last-report.json`，还会维护 `notify-state.json` 与 `frontstage-recovery-events.log`
  - anomaly / recovered 会生成稳定 `eventKey`，不再按 `checkedAt` 每轮换 key，避免 timer 每次都重复刷同一类前台消息
  - 当前 watcher 会把 `pendingProjection`（history 追赶 transcript 的短窗口）排除出异常通知，减少误报
  - 当前 watcher 已接入 `scripts/openclaw-frontstage-broker.py`，来源名为 `frontstage-recovery`
- 为了让这条线在刷新 / 新会话 / 重启后继续有效，当前已新增并安装用户态 systemd：
  - 模板：`tools/openclaw-frontstage-recovery/openclaw-frontstage-recovery-watch.service` / `.timer`
  - 当前用户态：`~/.config/systemd/user/openclaw-frontstage-recovery-watch.service` / `.timer`
  - 默认频率：开机约 90 秒后首次运行，之后约每 45 秒运行一次
- 当前还已补齐维护落点：`tools/openclaw-frontstage-recovery/README.md`、`TOOLS.md`、`docs/公司-Linux-OpenClaw-维护说明.md`
- 在继续推进第二阶段时，又把 watcher 的判定再往前推了一步：`scripts/openclaw-frontstage-recovery-watch.py` 现在会额外读取目标 dashboard 的 `sessions.list` 快照，把 `status` / `hasActiveRun` 一并写进 `last-report.json`；当 session 仍 `running` 或 `hasActiveRun=true` 时，会优先判成 `pendingProjection`（进行中），而不是急着报 anomaly。
- 同一轮里，Control UI 前台“进行中”状态补丁也继续收紧了一步：不再只看 `sending / stream / canAbort / queue.length > 0`，而是补成同时考虑 `loading / sending / stream / canAbort / queue.length > 0`；这样 tool-final 后 `chat.history` reload 的空窗也更容易被当成“还活着”。
- 随后又继续把这两边再往前推了一小步：
  - watcher 现在除 `session-active-run` / `session-running` 外，也会把“session 刚结束不久但 history 还没完全追上”的窗口判成 `pendingProjection(session-recently-ended)`，避免 tool-final / history reload 后立刻误报
  - Control UI 的正式补丁链路则继续把 `session.hasActiveRun / session.status=running` 也纳入聊天页“进行中”判断，进一步减少“后台仍在跑，但前台没有任何活着信号”的空窗
- 用户在 2026-05-14 10:01 又把修改类任务的默认工作流进一步明确成长期规则：当他已经明确提出诉求时，第一步先查之前有没有类似功能、入口或已有补丁；第二步检查与现有功能、旧逻辑、自动链路是否冲突，如有问题要及时回报并给出以当前诉求为目标的解决方案；第三步修改完成后默认接成正式补丁或自动链路，保证刷新、换主会话、换模型、重启、常规更新后仍尽量继续生效，并把落点记录清楚，方便后续续改。
- 在继续把 broker 第二阶段收口到“完全稳定”时，又定位并修补了一个更细的 recovered 一致性问题：`scripts/openclaw-frontstage-recovery-watch.py` 之前的 recovered 判定主要依赖 `previous_report`，没把 `notify-state.json` 中上一条已发 anomaly 当成强依据；现已改成同时参考 `previous_report + previous_notify + current_report`。这样当上一条已发状态还是 anomaly，但当前已不再 anomaly / pending 时，会优先生成 `recovered|<previous_notify.eventKey>`，避免出现“报告已恢复、notify-state 却还停在 anomaly”的半收口状态。当前 live 会话若仍 `hasActiveRun=true`，则继续保持 `pendingProjection(session-active-run)` 而不提前发 recovered，这是新的预期行为。
- 用户随后提出：考虑到 OpenClaw 大版本升级后，现有正式补丁可能因上游结构变化而失效，需要额外做一份“以结果为中心”的补丁注册表与重建清单；当前已新增：`docs/通用-OpenClaw-补丁注册表.md` 与 `docs/通用-OpenClaw-补丁重建清单.md`，用于记录每条正式补丁的目标结果、当前实现、自动触发链路、升级风险点、最小验收与维护落点。与此同时，为了让 broker 第二阶段后续能持续回归验证，还新增了 `scripts/test-frontstage-recovery-watch.py` 作为最小稳定性测试入口。
- 用户在 2026-05-14 11:24 进一步把分层方向正式收敛为：长期目标仍是工作层 / 数据层 / 渲染层分层，但当前阶段**先只升级 broker 为数据源**，其他层先做规划、不先落地；整条路线必须以 OpenClaw 主体稳定运行为最根本要求。
- 据此已开始实际落地 broker 的第一步 sidecar 数据层升级：`scripts/openclaw-frontstage-broker.py` 在保留现有前台投递行为不变的前提下，新增 `~/.local/state/openclaw/broker/events.jsonl` 与 `views/frontstage.json / health.json / tasks.json / recovery.json`；旧 `~/.local/state/openclaw/frontstage/broker-state.json` 继续保留以兼容现有链路。同时新增 `scripts/test-frontstage-broker.py` 做最小回归，当前已验证 `ALL PASS`。
- 用户在当天对当前主会话前端媒体链路做了真实验收，并明确反馈：图片、音频、视频当前展示/播放都“完全没问题”。这条结果可作为后续继续调整 broker / 渲染方向时的现状基线：当前 Control UI 至少已经通过最小媒体显示链路验收，不需要再把“基础媒体能不能显示”当成未知问题。
# 2026-05-14

- 用户把“监工”路线正式改成**可开关的监工服务**模型：简单工作默认不开，复杂工程默认开启，且用户可随时明确要求“开监工服务 / 关监工服务”。
- 监工服务控制语义已收敛为：`policyMode=auto|force_on|force_off` + `taskActive=true|false`；当前默认基线为 `auto + taskActive=false`，复杂工程默认切到 `auto + taskActive=true`。
- 已落地最小监工脚本：`scripts/openclaw-supervisor-status.py`，状态输出到 `~/.local/state/openclaw/supervisor/supervisor-status.json`，控制文件为 `~/.local/state/openclaw/supervisor/service-control.json`。
- 监工脚本当前可表达的主要状态：`service.state = disabled|armed|active|stopping`，`status = idle|running|stalled|done|failed`。
- 本地健康层已接入监工状态：`scripts/openclaw-local-health-diagnose.py` 现在会输出 supervisor 信息，并在本地健康页面里显示“监工”卡片与状态明细。
- 已补齐公司（Linux）机器上的监工 systemd 模板：`tools/openclaw-supervisor/openclaw-supervisor-watch.service` / `tools/openclaw-supervisor/openclaw-supervisor-watch.timer`；当前 timer 已安装到 `~/.config/systemd/user/` 并启用，默认开机约 1 分钟后首次运行、之后约每 30 秒刷新一次监工状态。
- 监工分身管理入口已落地：`scripts/openclaw-supervisor-subagent.py`，当前可通过 gateway RPC 间接做 `list / spawn / kill / resolve-frontstage / send-frontstage`。
- 当前验证得到的稳定通路：`tools.invoke -> subagents` 可做 `list / kill`；`chat.send -> /subagents spawn ...` 可做 `spawn`；`chat.inject` 可做前台状态回报；`tools.invoke -> sessions_spawn` 当前会被 gateway 拒绝为 `Tool not available: sessions_spawn`。
- `main-supervisor-lite` 不再按旧规则默认永远常驻；现在应作为监工服务开启后、且确实需要聊天插播/额外协作时才按需拉起的补充层。
- 用户又补充了一个关键前台绑定要求：若 WebChat / Control UI 用户点 `+` 切到新的 dashboard 会话，监工/后台回报的“当前会话”也应随之前移；默认不要继续打回旧 dashboard 页，而应优先解析到同一父会话下最新的 dashboard 会话。
- 用户进一步澄清：这里所说的“主会话”在 WebChat / Control UI 直聊里，默认应理解为用户当前正在看的前台 dashboard 页面，而不是内部共享 owner 会话 `agent:main:main`；后者只应作为共享父会话/中枢，不应直接作为默认前台回报目标。
- 前台投递这层也已补成固定入口：`send-frontstage` 会先解析同一父会话下最新的 dashboard，再通过 gateway `chat.inject` 把监工状态直接落到那个前台页面，不额外触发模型回复。
- 用户随后认可了“先做辅助消息 broker、暂不强制接管正常主回复”的第一阶段路线；当前已新增 `scripts/openclaw-frontstage-broker.py`，统一接管 `supervisor` / `local-health` 这两类辅助消息，按 `source + eventKey` 去重后再发回当前 dashboard 前台。
- 自动回报闭环也已接上：`scripts/openclaw-supervisor-status.py --notify-transitions` 会在 `stalled / failed / done` 这些状态变化时，自动调用统一前台 broker，再由 broker 走 `send-frontstage` / `chat.inject` 回到当前 dashboard，并把最近一次已发事件记到 `notify-state.json` 里做去重；当前已分别对 `done / failed / stalled` 三种状态做过闭环验证，且已额外验证同一任务可先触发 `stalled` 回报、恢复后再触发 `done` 回报，`notify_sent` 事件与前台 assistant note 都已出现。
# 2026-05-14

- 用户把“监工”路线正式改成**可开关的监工服务**模型：简单工作默认不开，复杂工程默认开启，且用户可随时明确要求“开监工服务 / 关监工服务”。
- 监工服务控制语义已收敛为：`policyMode=auto|force_on|force_off` + `taskActive=true|false`；当前默认基线为 `auto + taskActive=false`，复杂工程默认切到 `auto + taskActive=true`。
- 已落地最小监工脚本：`scripts/openclaw-supervisor-status.py`，状态输出到 `~/.local/state/openclaw/supervisor/supervisor-status.json`，控制文件为 `~/.local/state/openclaw/supervisor/service-control.json`。
- 监工脚本当前可表达的主要状态：`service.state = disabled|armed|active|stopping`，`status = idle|running|stalled|done|failed`。
- 本地健康层已接入监工状态：`scripts/openclaw-local-health-diagnose.py` 现在会输出 supervisor 信息，并在本地健康页面里显示“监工”卡片与状态明细。
- 已补齐公司（Linux）机器上的监工 systemd 模板：`tools/openclaw-supervisor/openclaw-supervisor-watch.service` / `tools/openclaw-supervisor/openclaw-supervisor-watch.timer`；当前 timer 已安装到 `~/.config/systemd/user/` 并启用，默认开机约 1 分钟后首次运行、之后约每 30 秒刷新一次监工状态。
- 监工分身管理入口已落地：`scripts/openclaw-supervisor-subagent.py`，当前可通过 gateway RPC 间接做 `list / spawn / kill / resolve-frontstage / send-frontstage`。
- 当前验证得到的稳定通路：`tools.invoke -> subagents` 可做 `list / kill`；`chat.send -> /subagents spawn ...` 可做 `spawn`；`chat.inject` 可做前台状态回报；`tools.invoke -> sessions_spawn` 当前会被 gateway 拒绝为 `Tool not available: sessions_spawn`。
- `main-supervisor-lite` 不再按旧规则默认永远常驻；现在应作为监工服务开启后、且确实需要聊天插播/额外协作时才按需拉起的补充层。
- 用户又补充了一个关键前台绑定要求：若 WebChat / Control UI 用户点 `+` 切到新的 dashboard 会话，监工/后台回报的“当前会话”也应随之前移；默认不要继续打回旧 dashboard 页，而应优先解析到同一父会话下最新的 dashboard 会话。
- 用户进一步澄清：这里所说的“主会话”在 WebChat / Control UI 直聊里，默认应理解为用户当前正在看的前台 dashboard 页面，而不是内部共享 owner 会话 `agent:main:main`；后者只应作为共享父会话/中枢，不应直接作为默认前台回报目标。
- 前台投递这层也已补成固定入口：`send-frontstage` 会先解析同一父会话下最新的 dashboard，再通过 gateway `chat.inject` 把监工状态直接落到那个前台页面，不额外触发模型回复。
- 用户随后认可了“先做辅助消息 broker、暂不强制接管正常主回复”的第一阶段路线；当前已新增 `scripts/openclaw-frontstage-broker.py`，统一接管 `supervisor` / `local-health` 这两类辅助消息，按 `source + eventKey` 去重后再发回当前 dashboard 前台。
- 自动回报闭环也已接上：`scripts/openclaw-supervisor-status.py --notify-transitions` 会在 `stalled / failed / done` 这些状态变化时，自动调用统一前台 broker，再由 broker 走 `send-frontstage` / `chat.inject` 回到当前 dashboard，并把最近一次已发事件记到 `notify-state.json` 里做去重；当前已分别对 `done / failed / stalled` 三种状态做过闭环验证，且已额外验证同一任务可先触发 `stalled` 回报、恢复后再触发 `done` 回报，`notify_sent` 事件与前台 assistant note 都已出现。
- 第一阶段后来又继续补齐了 `local-health` 的统一投递：`scripts/openclaw-local-health-diagnose.py` 新增 `--notify-frontstage`，只在 `warn / critical / recovered` 这些状态变化时通过 `scripts/openclaw-frontstage-broker.py` 发前台消息，不会周期性“报平安”；相关 user service 模板也已改为带上这个参数。
- 用户明确同意第二阶段先走**shadow watcher**，即先做“检测与补位观察”，不直接拦截正常主回复；目标是盯住“live 里出现过但最终没稳定留在前台/history”的回复，以及“最终可见文本异常、只剩英文控制片段/奇怪字符/空壳”的情况。
- 用户又进一步明确了一个新的前台体验硬要求：只要后台还没真正结束，前台至少也必须有一个“正在进行”的可见信号（例如转圈/进行中图标）；如果后台还在跑但前台完全没反馈，这本身就应视为失败，而不是可接受的偶发现象。
- 针对上面这条，当前已定位到一个 Control UI 前台状态洞：聊天页“进行中”原先主要只看 `sending` 和 `stream !== null`，会漏掉 tool-call 阶段、等待 toolResult、本地队列未清空、run 仍可 abort 但暂时没流字的情况；因此后来把“进行中”判断补成同时考虑 `sending / stream / canAbort / queue.length > 0`，用来减少“后台仍在工作但前台像死了”的无反馈窗口。
- 用户随后又明确补充了一条长期要求：像这种在他提前诉求后做的修补，不能只在当前会话里临时生效；默认应做成刷新/换会话/重启 gateway/切换模型后仍可继续生效的正式补丁，并且要有稳定记录，方便以后继续改动时能快速找到落点。
- 据此，前台“进行中”状态修补不再只允许停留在手工改一次当前 `dist/control-ui/assets/index-*.js` 的层面，而是应并入 `scripts/apply-openclaw-control-ui-branding.py` 这条已有的可重复应用补丁链路；这样公司 Linux 机上每次 gateway 启动前都会自动重打这条补丁，刷新/新会话/换模型不会把它丢掉。
- 用户随后要求继续把 `broker / watcher` 这条线也按同样的“持久生效”标准过一遍，并继续做 broker 第二阶段；当前审计结论是：`broker` 本体、`supervisor`、`local-health` 已经接入长期链路，但 `frontstage-recovery watcher` 之前还只是“脚本能跑”，没有稳定自动触发与去重状态机。
- 据此，第二阶段已继续补成正式链路：
  - `scripts/openclaw-frontstage-recovery-watch.py` 现在除了生成 `last-report.json`，还会维护 `notify-state.json` 与 `frontstage-recovery-events.log`
  - anomaly / recovered 会生成稳定 `eventKey`，不再按 `checkedAt` 每轮换 key，避免 timer 每次都重复刷同一类前台消息
  - 当前 watcher 会把 `pendingProjection`（history 追赶 transcript 的短窗口）排除出异常通知，减少误报
  - 当前 watcher 已接入 `scripts/openclaw-frontstage-broker.py`，来源名为 `frontstage-recovery`
- 为了让这条线在刷新 / 新会话 / 重启后继续有效，当前已新增并安装用户态 systemd：
  - 模板：`tools/openclaw-frontstage-recovery/openclaw-frontstage-recovery-watch.service` / `.timer`
  - 当前用户态：`~/.config/systemd/user/openclaw-frontstage-recovery-watch.service` / `.timer`
  - 默认频率：开机约 90 秒后首次运行，之后约每 45 秒运行一次
- 当前还已补齐维护落点：`tools/openclaw-frontstage-recovery/README.md`、`TOOLS.md`、`docs/公司-Linux-OpenClaw-维护说明.md`
- 在继续推进第二阶段时，又把 watcher 的判定再往前推了一步：`scripts/openclaw-frontstage-recovery-watch.py` 现在会额外读取目标 dashboard 的 `sessions.list` 快照，把 `status` / `hasActiveRun` 一并写进 `last-report.json`；当 session 仍 `running` 或 `hasActiveRun=true` 时，会优先判成 `pendingProjection`（进行中），而不是急着报 anomaly。
- 同一轮里，Control UI 前台“进行中”状态补丁也继续收紧了一步：不再只看 `sending / stream / canAbort / queue.length > 0`，而是补成同时考虑 `loading / sending / stream / canAbort / queue.length > 0`；这样 tool-final 后 `chat.history` reload 的空窗也更容易被当成“还活着”。
- 随后又继续把这两边再往前推了一小步：
  - watcher 现在除 `session-active-run` / `session-running` 外，也会把“session 刚结束不久但 history 还没完全追上”的窗口判成 `pendingProjection(session-recently-ended)`，避免 tool-final / history reload 后立刻误报
  - Control UI 的正式补丁链路则继续把 `session.hasActiveRun / session.status=running` 也纳入聊天页“进行中”判断，进一步减少“后台仍在跑，但前台没有任何活着信号”的空窗
- 用户在 2026-05-14 10:01 又把修改类任务的默认工作流进一步明确成长期规则：当他已经明确提出诉求时，第一步先查之前有没有类似功能、入口或已有补丁；第二步检查与现有功能、旧逻辑、自动链路是否冲突，如有问题要及时回报并给出以当前诉求为目标的解决方案；第三步修改完成后默认接成正式补丁或自动链路，保证刷新、换主会话、换模型、重启、常规更新后仍尽量继续生效，并把落点记录清楚，方便后续续改。
- 在继续把 broker 第二阶段收口到“完全稳定”时，又定位并修补了一个更细的 recovered 一致性问题：`scripts/openclaw-frontstage-recovery-watch.py` 之前的 recovered 判定主要依赖 `previous_report`，没把 `notify-state.json` 中上一条已发 anomaly 当成强依据；现已改成同时参考 `previous_report + previous_notify + current_report`。这样当上一条已发状态还是 anomaly，但当前已不再 anomaly / pending 时，会优先生成 `recovered|<previous_notify.eventKey>`，避免出现“报告已恢复、notify-state 却还停在 anomaly”的半收口状态。当前 live 会话若仍 `hasActiveRun=true`，则继续保持 `pendingProjection(session-active-run)` 而不提前发 recovered，这是新的预期行为。
- 用户随后提出：考虑到 OpenClaw 大版本升级后，现有正式补丁可能因上游结构变化而失效，需要额外做一份“以结果为中心”的补丁注册表与重建清单；当前已新增：`docs/通用-OpenClaw-补丁注册表.md` 与 `docs/通用-OpenClaw-补丁重建清单.md`，用于记录每条正式补丁的目标结果、当前实现、自动触发链路、升级风险点、最小验收与维护落点。与此同时，为了让 broker 第二阶段后续能持续回归验证，还新增了 `scripts/test-frontstage-recovery-watch.py` 作为最小稳定性测试入口。
- 用户在 2026-05-14 11:24 进一步把分层方向正式收敛为：长期目标仍是工作层 / 数据层 / 渲染层分层，但当前阶段**先只升级 broker 为数据源**，其他层先做规划、不先落地；整条路线必须以 OpenClaw 主体稳定运行为最根本要求。
- 据此已开始实际落地 broker 的第一步 sidecar 数据层升级：`scripts/openclaw-frontstage-broker.py` 在保留现有前台投递行为不变的前提下，新增 `~/.local/state/openclaw/broker/events.jsonl` 与 `views/frontstage.json / health.json / tasks.json / recovery.json`；旧 `~/.local/state/openclaw/frontstage/broker-state.json` 继续保留以兼容现有链路。同时新增 `scripts/test-frontstage-broker.py` 做最小回归，当前已验证 `ALL PASS`。
- 用户在当天对当前主会话前端媒体链路做了真实验收，并明确反馈：图片、音频、视频当前展示/播放都“完全没问题”。这条结果可作为后续继续调整 broker / 渲染方向时的现状基线：当前 Control UI 至少已经通过最小媒体显示链路验收，不需要再把“基础媒体能不能显示”当成未知问题。
- broker 数据层随后又继续收口到更接近正式 sidecar：已新增并接入周期重建 user systemd，模板为 `tools/openclaw-frontstage-broker/openclaw-frontstage-broker-rebuild.service` 与 `.timer`，当前用户态单元为 `~/.config/systemd/user/openclaw-frontstage-broker-rebuild.service` / `.timer`；默认开机约 75 秒后首次运行，之后约每 60 秒重建一次 broker 视图，避免长时间没有新辅助消息时视图慢慢变旧。
- `scripts/openclaw-frontstage-broker.py` 的 `rebuild-views` 已简化为真正的重建动作，不再需要伪造 `--source/--event-key/--message` 这类无意义参数；对应 apply / 重建文档也已同步按这条更自然的调用方式更新。
- broker 视图与 `manifest.json` 当前都已新增 `freshness` 语义：会记录本次 `rebuiltAt`，以及 `broker-state.json`、`local-health/last-report.json`、`supervisor-status.json`、`frontstage-recovery/last-report.json`、`frontstage-recovery/notify-state.json` 这些源文件的路径、存在性、文件 mtime 与报告时间戳，方便后续 renderer 或排查脚本区分“视图刚重建”与“底层 source 已陈旧”。
- 这轮里还顺手修掉了一个 broker 时间语义 bug：原先 `views/*.json` 与 `manifest.json` 的 `updatedAt` 可能沿用旧 `broker-state.json` 的时间，显得像老数据；现已改为反映真实 rebuild 时间，并与 `freshness.rebuiltAt` 保持一致。
- `scripts/test-frontstage-broker.py` 已扩充到覆盖上述 freshness / rebuild 时间语义，当前回归仍返回 `ALL PASS`；这意味着这些新增字段不再只是“当前机器碰巧有”，而是已进入最小测试保护范围。
- `scripts/apply-openclaw-frontstage-broker-data.py` 也已继续升级：除 `py_compile + broker 测试 + rebuild-views` 之外，当前还支持 `--install-user-systemd`，可把 broker rebuild 的 user systemd 单元复制到 `~/.config/systemd/user/`、`daemon-reload` 并启用 timer；这样以后升级后按表重建 broker，不必再靠记忆手工装那两个 unit 文件。
