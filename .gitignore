# Dependencies
node_modules/
**/node_modules/

# Temp / generated
tmp/
.openclaw/
*.tar.gz
*.log

# Credentials (never commit real secrets)
credentials/

# But DO track .config-backup/ (already whitelisted below)

# Embedded git repos
skills/self-improving-agent/

# Build artifacts
dist/
build/
.next/
**/.next/

# Python cache
__pycache__/
**/__pycache__/

# ClawHub / hook runtime state
.clawhub/
**/.clawhub/
.learnings/.hook-state.json

# Local-only research / installed tools / large assets
memory/research/
memory/????-??-??-????.md
model.bin
tools/Speech-AI-Forge/
tools/audio-sep/
skills/evolver-src/
skills/evolver/memory/
skills/ex-qianqian/memory/
skills/web-claude/
# Memory runtime artifacts
memory/.????-??-??-*.fallback.tmp
memory/daily/*-transcript.md
openclaw-workspace-state.json
.coverage

# Pre-push backup files (2026-07-14 added to prevent backup pollution)
*.bak-pre-*
*.bak-yyyymmdd*
# Backup files created during config-change operations (e.g. upgrade repair)
*.bak.2026*

# Backup directories (pre-upgrade systemd unit backups, etc.)
docs/backup/
media/Seedream-Images/

# Mark42 dev portal
# 【2026-08-05 修正】原规则整目录排除，连生成器脚本一起排在外面。
# 但生成器和产物性质不同：产物可重建，生成器丢了就重建不了。
# 现只排除产物，保留 generate*.py 入库。
mark42-pkg/docs/devportal/*.html
