==== 阶段 1 验证清单填回 ====

## 1.1 TTY openclaw status 可读: ✅
  → Gateway reachable 104ms · pid 12393 active · 3 agents · 23 sessions

## 1.2 TTY 直接执行命令 stdout / stderr 可读: ✅
  → 短输出 / 长输出(2000 A) / 多行(20行) / 含路径 / 含 JSON / 含 MEDIA: 前缀 / 含 code fence / 含 [[reply_to_*]] 都能落盘并可读

## 1.3 openclaw gateway stability --bundle latest 无异常: ✅
  → Events: 0/1000（无渲染层异常事件记录）

## 1.4 Gateway 日志在 TTY 可读: ✅
  → /tmp/openclaw/openclaw-2026-07-07.log，openclaw logs --limit 200 --no-color 可读

## 额外: TUI（TTY 端 chat 客户端） 不重现附件现象: ✅
  → 在 openclaw tui --local 下发消息，文本以普通字符显示，无附件包装

## 关键时序（与点点 16:03 反馈高度吻合）
  → 16:00:34 [compaction-diag] timeout durationMs=179958（3分钟压缩超时）
  → 16:00:36 compaction Full summarization failed: Request was aborted
  → 16:00:45+ 多次 webchat disconnected/connected（异常后浏览器重连）
  → 16:00:46~ 多个 conn 重连，chat.startup 重复触发

## 结论
  后端（Gateway / Provider / 工具链 / TTY / TUI）全部正常。
  渲染异常**只发生在 WebChat / Control UI 这条浏览器路径**。
  高度疑似 OpenClaw WebChat/Control UI 渲染层回归（Q1 + Q2 + Q3）。
  触发诱因疑似 16:00 的一次失败 compaction（Q3 命中）。
