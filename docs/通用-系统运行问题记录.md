# 系统运行问题记录

> 适用机器：公司（Linux）
> 用途：记录系统运行期间遇到的各类异常、崩溃、高负载、功能异常等问题，供后续排查参考。
> 每次遇到问题，追加新条目（时间倒序）。

---

## 2026-06-09 16:33 — CPU 负载过高警告

### 触发
用户提示"CPU 负载过高"，询问后台任务是否正常。

### 观测数据
- **CPU 负载**：1min 5.27 / 5min 5.20 / 15min 3.73（8核机器，>核数即过载）
- **内存**：7.7G 总 / 3.5G 已用 / 195M 空闲 / 4.4G buff/cache / swap 738M
- **系统已运行**：2小时43分钟

### top CPU 进程
| 进程 | PID | CPU% | 内存 | 说明 |
|------|-----|------|------|------|
| openclaw-gateway | 24133 | 93.1% | 223M | Gateway 子进程，刚启动瞬态，非持续高负载 |
| nautilus | 21436 | 58.6% | 172M | 文件管理器，可能因打开 analysis.md 等文件触发 |
| node (gateway主) | 1832 | 34.7% | 589M | Gateway 主进程，累计 56min CPU |
| firefox | 3572 | 29.6% | 581M | 浏览器 |
| gnome-shell | 2649 | 15.7% | 322M | GNOME 桌面 |
| firefox-content | 4180 | 9.9% | 274M | 浏览器内容进程 |

### 后台任务状态
- **backup_ufm_impl**：running（已运行 5 分钟 41 秒），正常推进中
- 监工服务：active（force_on），已就位

### 判断
- 主因是 nautilus（文件管理器）+ openclaw-gateway 瞬时峰值叠加，非持续性问题
- 内存尚有 4.3G 可用（含缓冲），swap 738M 在可控范围
- **后台分身正常运行，没有受到影响**
- 无需干预，观察即可

### 环境上下文
- 正在进行：会话备份机制修复 + Unity文件管理器 plan/apply 实施
- 后台分身模型：deepseek-v4-pro
- 会话压缩：约 16:12 发生（在此之前有一轮压缩）

---

## 模板（新问题按此格式追加）

```
## YYYY-MM-DD HH:MM — 问题简述

### 触发
（用户提示 / 自动告警 / 系统观测）

### 观测数据
（CPU / 内存 / 磁盘 / 进程 / 日志摘要）

### 根因分析
（追到哪一层）

### 处理结果
（做了什么 / 待处理）

### 环境上下文
（当时在做什么 / 什么模型 / 什么任务）
```
