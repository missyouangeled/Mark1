# Mark42 最终审查报告（2026-07-01）

> **审查方式**：基于真实代码、真实命令、真实测试、真实 systemd 验收，不按印象下结论。  
> **结论口径**：本机闭环真实成立；陌生机器从零安装仍未做完整实机验收。

---

## 一、最终结论

Mark42 当前已经是一个**可运行、可守护、可验收、可维护**的内部工程系统，不再只是概念项目。

但它还不是“所有模块同等成熟、所有设计文档完全追平、所有路径都已实机打透”的封箱状态。

### 综合评分

- **整体评分：7.8 / 10**
- **设计一致性：7.0 / 10**
- **运行可靠性：8.0 / 10**
- **测试成熟度：6.8 / 10**
- **内部可交付度：8.2 / 10**
- **商品化成熟度：4.5 / 10**

---

## 二、本次审查真实基线

> ⚠️ 本节保留的是 **2026-07-01 上午严格审查时** 的现场基线，用来说明这份报告当时依据什么下结论。  
> 如果你要看 **当前最新测试口径 / 当前接力顺位**，请改看：
> - `docs/design/mark42-测试覆盖接力开发方向-20260701.md`
> - `docs/design/mark42-更新日志.md`

### 1）代码与模块

- CLI 入口：`scripts/mark42.py`
- 实际主逻辑：`scripts/mark42_modules/cli.py`
- 当前模块文件数：**18**（`scripts/mark42_modules/*.py`）

### 2）测试基线（2026-07-01 实跑）

```text
316 passed, 8 skipped in 42.72s
TOTAL COVERAGE: 45.9%
```

> 后续同日补测已把正式 pytest 基线推进到：**456 passed, 1 skipped / 65.6%**。

覆盖率摘要：

- `heavy.py`：**90.6%**
- `engine.py`：**62.0%**
- `utils.py`：**67.3%**
- `armor.py`：**50.8%**
- `cli.py`：**46.9%**
- `compaction_diag.py`：**55.6%**
- `code_compressor.py`：**26.0%**
- `pii_redactor.py`：**13.5%**
- `log_deduplicator.py`：**8.0%**
- `perf_bench.py`：**0.0%**

### 3）运行基线（2026-07-01 10:18 实读）

`python3 scripts/mark42.py status --json`：

- `version=2.3.0`
- `armor.usagePercent=18.0`
- `armor.contextWindow=1000000`
- `engine.activeLoops=4`
- `heavy.activeTasks=[]`
- `logs.rotationCount=764`
- `broker.mark42Events=25435`

### 4）systemd 验收基线

`bash tools/mark42-systemd/verify.sh` 实跑通过：

- `mark42-bootstrap.service` active
- `mark42-engine-daemon.service` active
- `mark42-armor-guard.service` active
- `mark42-watchdog.timer` active
- `openclaw-gateway.service` active
- `pass>0 warn=0 fail=0`

---

## 三、设计 → 实现一致性判断

### 已一致的部分

1. **三大主模块真实存在且可运行**
   - Armor：上下文铠甲
   - Engine：循环引擎
   - Heavy：重型战甲
2. **统一 CLI 入口真实存在且可用**
3. **统一状态目录 / broker 事件 / actions 留痕真实存在**
4. **开发模式与生产托管模式已分离**
5. **systemd 工具链已形成 preflight / install / restore / verify / uninstall 闭环**

### 不一致 / 已漂移的部分

1. `docs/design/mark42-架构设计.md` 中旧资产统计（22 模块 / 111 测试 / 37.8% 覆盖）已过时
2. 文档中的“四守护服务”表述仍偏旧，当前真实托管单元已演化为：
   - `mark42-bootstrap.service`
   - `mark42-engine-daemon.service`
   - `mark42-armor-guard.service`
   - `mark42-watchdog.service`
   - `mark42-watchdog.timer`
3. `contextWindow` 的显示口径与默认配置口径仍存在漂移：
   - 默认常量：`131072`
   - 运行时实读：`1000000`

---

## 四、功能实现结论

### 已明确实现并可运行

- `armor --check`
- `armor --guard`
- `armor --compress`
- `engine --templates`
- `engine --list / --start / --kill / --daemon / --watch-task`
- `heavy --detect`
- `heavy --preflight`
- `heavy --start / --execute / --execute-all / --finish / --cleanup`
- `logs --status / --rotate`
- `status --json`
- `compaction` 诊断链路
- systemd 安装/回退/验收/卸载工具链

### 已实现但成熟度不均

- `algo_scheduler.py`
- `code_compressor.py`
- `llm_text_compressor.py`
- `pii_redactor.py`
- `log_deduplicator.py`
- `perf_bench.py`

这些模块不能算“没实现”，但也不能诚实地说“都已充分验证收口”。

---

## 五、关键运行证据：Armor 真压缩链路已现场验收成功

这是本次审查最关键的新结论。

### 本次实机取证结果

通过**临时降低阈值**进入真实压缩分支（未改常驻配置），执行：

```bash
MARK42_CTX_WARN_PCT=0 python3 scripts/mark42.py armor --compress
```

### 现场结果

```text
🧹 会话截短成功: 1252KB → 1138KB (节省 9.1%)
```

`memory-index.json` 实际记录：

```json
{
  "strategyUsed": "heuristic-classify",
  "compactTriggered": true,
  "compactMethod": "openclaw-sessions-compact",
  "preCompactBytes": 1282204,
  "postCompactBytes": 1166117,
  "bytesSaved": 116087,
  "compressionEffective": true,
  "compactError": null
}
```

`armor/actions.jsonl` 真实记录到最新成功样本：

```json
{
  "action": "compress",
  "preBytes": 1282204,
  "postBytes": 1166117,
  "bytesSaved": 116087,
  "bytesStatus": "captured",
  "compressionEffective": true,
  "compactTriggered": true,
  "compactMethod": "openclaw-sessions-compact",
  "compactError": null
}
```

### 结论

> 旧结论里“armor 压缩链路疑似断裂”的核心问题，至少在本机当前实现上，已经被新的真实证据推翻。  
> 现在可以明确说：**Armor 压缩闭环已经实现，且已现场验证有效。**

---

## 六、当前仍未闭环的地方

1. **陌生机器从零安装**未做完整实机验收
2. **文档与真实资产口径**仍需持续对齐
3. **低覆盖模块**仍影响整体可信度上限
4. **algo_scheduler 相关旧集成测试腐烂**，暂未重写收口
5. **context window 显示与运行口径不统一**，后续仍建议整理

---

## 七、P0 / P1 / P2 建议

### P0
1. 保留本次 Armor 真压缩验收结果，作为新的正式基线
2. 用本报告替代 2026-06-29 的旧整体结论口径
3. 修正文档中已过时的模块数、测试数、覆盖率、托管形态表述

### P1
1. **陌生机器静态验收 checklist 暂缓**：保留为后续事项，当前不作为主线推进
2. 优先补低覆盖模块测试：`pii_redactor.py` / `log_deduplicator.py` / `code_compressor.py`
3. 处理 `algo_scheduler.py` 的腐烂集成测试
4. 统一 `contextWindow` 配置/显示/运行口径

> 2026-07-01 10:21 决定：当前阶段先把 **Mark42 项目本体完善好**，不优先展开跨机器 checklist / 跨机器验收文档化。

### P2
1. 给 Heavy 再做一次完整生命周期现场演练
2. 给 `verify.sh` 增加更强的运行态判据
3. 明确 experimental/debug-only 模块标记

---

## 八、最终裁定

### 可以明确说成立的

- Mark42 核心主线已实现
- 本机主系统当前正常运行
- systemd 工具链本机闭环成立
- Armor 真压缩链路已实机验收成功

### 不能夸大说成立的

- 陌生机器一键安装已完全闭环
- 所有模块都达到同等成熟度
- 所有边缘/实验能力都已完整实机打透

---

## 九、一句话总结

Mark42 现在已经从“有想法、有代码”走到了“**核心闭环真实可用**”。  
接下来缺的不是大功能，而是：**证据收口、文档收口、低成熟模块补强。**
