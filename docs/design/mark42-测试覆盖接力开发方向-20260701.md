# Mark42 测试覆盖接力开发方向（2026-07-01）

> **用途**：给下一个模型 / 新会话 / 被自动压缩后的自己，提供一份可以直接接着干的测试推进说明。  
> **范围**：只覆盖当前这条主线——**继续提升 `scripts/mark42_modules/` 的正式 pytest 覆盖率**。  
> **接续基线**：以 2026-07-01 13:15 左右真实跑测结果为准。  
> **配套文档**：
> - 最近变更：`docs/design/mark42-更新日志.md`
> - 测试写法/陷阱：`docs/design/mark42-测试手册.md`
> - 阶段背景：`docs/design/mark42-Phase3路线-20260630.md`
> - 项目现状结论：`docs/design/mark42-最终审查报告-20260701.md`

---

## 一、当前真实基线（已实跑）

### 1）最近一次全量回归

实际命令：

```bash
python3 -m pytest scripts/tests/ --cov=scripts/mark42_modules --cov-report=term-missing -q
```

实际结果（**2026-07-01 13:15**）：

```text
526 passed, 2 skipped in 47.88s
TOTAL 74.7%
```

> 本轮 13:15 实跳：perf_bench.py 补测落地 53 条 + 1 跳过。从 473/72.6% 推到 526/74.7%。
> 详细留痕见：
> - `docs/design/mark42-更新日志.md` #18（末段）
> - `memory/daily/2026-07-01.md` §19

### 2）当前关键模块覆盖率（按接力价值排序）

| 模块 | 当前覆盖 | 说明 |
|---|---:|---|
| `perf_bench.py` | **44.9%** | 本轮已落地 smoke 级别测试（53 + 1 skip）；bench_* / main() 因本赮不跳 |
| `compress_queue.py` | **98.0%** | 本轮刚收完，从 53.7% → 98.0%，可后放 |
| `compaction_diag.py` | **86.5%** | 上一刀刚收完，可后放 |
| `code_compressor.py` | **61.8%** | 可暂后放 |
| `cli.py` | **61.9%** | 已补强，可暂收口 |
| `text_compressor.py` | **98.6%** | 已收口 |
| `algo_scheduler.py` | **99.5%** | 几乎打穿 |
| `llm_text_compressor.py` | **95.9%** | 已收口 |
| `logs.py` | **97.2%** | 已收口 |
| `heavy.py` | **90.6%** | 已高覆盖 |

### 3）当前最推荐的开发顺序

1. **`code_compressor.py` 中后段缺口**（61.8% → 中后段可能还有整段黑洞）
2. **`config.py` 中段缺口**（几个路径派生 / 环境变量交互 / /mnt/data 检测）
3. **`engine.py` 中后段缺口**（上下文铠甲的 daemon tick / 4 个 loop 模板路径补全）
4. **`cli.py` 的 status 聚合 / health-watch / 一些难以进默认套件的分支**
5. **perf_bench 补剩下的 55.1%**：限定不跑 `bench_*` / `main()`，只补未被覆盖的 helper 函数。
   剩余应该是 `_measure_peak_kb` / `_warmup` / `_progress` / `_flush` 这类小 helper。
   剩余都包含在 §六 C 说明里，**不迫**补。

> 若 code_compressor.py 中后段缺口补完后 overall 已 ≥75%，建议不再追 perf_bench 剩下部分。 以后拿个位在手心推进集成 + smoke 为主。

---

## 二、`text_compressor.py` 已完成，这一刀做了什么

这一块已经在本轮被正式收口，不再是“下一刀候选”。

### 本轮实际处理结果

1. **确认最大缺口就是模块底部 `_run_tests()`**
   - 单文件覆盖实跑确认：`542-723` 基本就是自检逻辑黑洞
   - 这类缺口和此前 `algo_scheduler.py`、`llm_text_compressor.py` 一样，属于高价值整段缺口

2. **继续沿用“直接补现有测试文件”的策略**
   - 直接追加到：`scripts/tests/unit/test_text_compressor.py`
   - 没新建测试文件

3. **新增正式 pytest 覆盖**
   - `_run_tests()` 成功路径
   - `_run_tests()` 失败路径
   - `if __name__ == "__main__"` 的退出码分支（0 / 1）

4. **实跑结果**
   - `python3 -m pytest scripts/tests/unit/test_text_compressor.py -q`
     - **26 passed**
   - `python3 -m pytest scripts/tests/ --cov=scripts/mark42_modules --cov-report=term-missing -q`
     - **456 passed, 1 skipped**
     - `text_compressor.py`: **58.0% → 98.6%**
     - overall: **63.2% → 65.6%**

### 这一刀的意义

- 不只是把模块覆盖率从中位直接拉到接近收口
- 更重要的是：把作者原来写在 `_run_tests()` 里的隐含契约，变成了正式 pytest 回归保护
- 这意味着以后压缩链如果漂了，不用靠人记得手跑脚本

---

## 三、下一位接手时的推荐动作

### Step 1：把主线切到 `compaction_diag.py`

先读：

- `scripts/mark42_modules/compaction_diag.py`
- `scripts/tests/unit/test_compaction_diag.py`

目标不是立刻补小分支，而是先确认：

1. 当前未覆盖的大段主要在哪
2. 这些缺口是不是“诊断快照 / 汇总结构 / 输出组装 / 容错回退”这类高价值路径
3. 现有测试里哪些已经是稳定骨架，后续适合直接追加

### Step 2：第二顺位再回到 `compress_queue.py`

如果 `compaction_diag.py` 过重、短时间内难拿稳定收益，就切到：

- `scripts/mark42_modules/compress_queue.py`

它的中后段缺口仍然清晰，属于更稳的推进型目标。

### Step 3：继续沿用当前已验证有效的写法

#### 推荐策略
- **优先直接编辑现有测试文件**
  - 例如：`scripts/tests/unit/test_text_compressor.py`
- **尽量不用新建测试文件**
  - 这轮曾出现过新文件写入不稳 / whole-file write 失败
- 如果整文件覆盖式 `write` 不稳：
  - 改用 `read` 对齐锚点
  - 再用 `edit` 做尾部追加或局部替换

#### 推荐节奏
1. 先补一小组高价值测试
2. 跑单文件：
   ```bash
   python3 -m pytest scripts/tests/unit/test_text_compressor.py -q
   ```
3. 修完失败后，再跑全量：
   ```bash
   python3 -m pytest scripts/tests/ --cov=scripts/mark42_modules --cov-report=term-missing -q
   ```
4. 只有真的跑过，才允许记录“通过”或“覆盖率提升”

---

## 四、这轮工作已经验证过的“靠谱策略”

以下经验不是猜的，是这轮实战已经证明有效的：

### 1）优先补现有测试文件，不新建

已验证有效：
- `scripts/tests/unit/test_llm_text_compressor.py`
- `scripts/tests/unit/test_cli.py`
- `scripts/tests/unit/test_algo_scheduler.py`

原因：
- 新建测试文件在当前环境里曾失败
- 直接补现有文件更稳，也更不容易把 pytest 收集路径搞乱

### 2）测试必须贴真实实现，不要为了测试改实现口径

典型例子：
- `llm_text_compressor.py` 的 async 空输入状态
- `_run_tests()` 自身允许的状态集合
- `cli.py` 分发到具体模块的真实参数透传
- `algo_scheduler.py` `_run_tests()` 中真实喂入的输入（包括 `{"a": 1, "b": 2}`）

### 3）大段 `_run_tests()` / self-check 很值钱

已经验证过的高收益打法：
- `llm_text_compressor.py`：把 `_run_tests()` 收编进 pytest，覆盖率拉到 **95.9%**
- `algo_scheduler.py`：把 `_run_tests()` 收编进 pytest，覆盖率拉到 **99.5%**

因此，后续读其他模块时，要优先警惕这种“覆盖率黑洞”。

### 4）遇到 whole-file `write` 失败，不要硬怼

实战里已经出现过：

- `Write: to ~/.openclaw/workspace/scripts/tests/unit/test_algo_scheduler.py (22429 chars) failed`

正确处理方式：
- 不再整文件覆盖
- 改成：
  1. 先 `read` 文件尾部
  2. 找稳定锚点
  3. 用 `edit` 精确追加

### 5）所有“自动 / 不可见”行为要留痕

本轮已经按这个原则做了：
- `docs/design/mark42-更新日志.md`
- `memory/daily/2026-07-01.md`

下一个模型也必须继续保持：
- 测试补强完成后更新项目文档
- 不要只把信息留在聊天上下文里

---

## 五、更新后的接力重点

### 1）`text_compressor.py` 已收口

当前状态：
- `text_compressor.py`: **98.6%**
- 仅余极少量小分支：
  - `ratio = 0.0` 的保护分支
  - `_convert_numbers()` 中 `<1000` 原样返回
  - `_run_tests()` 测试 14 的非回退 else 分支

这些都不是当前最高优先级，可以后补。

### 2）新的首选目标：`compaction_diag.py`

建议先核：
- 现有测试是否已经覆盖主数据结构
- 未覆盖大段是不是可通过稳定 fixture / 假数据直接补进去
- 是否存在类似 `_run_tests()` / 输出汇总块 / 主流程组装段 这种“整段黑洞”

### 3）第二目标：`compress_queue.py`

这是更稳的一刀：
- 中后段缺口边界清晰
- 更适合拿“稳定但不爆炸”的下一轮提升
   - 若没有：就找“整段 fallback/guard 逻辑”做成一批测试

---

## 六、后续模块的接力顺序与判断标准

### A. `compress_queue.py`

如果 `text_compressor.py` 做完，下一个优先建议是它。

原因：
- 当前 `53.7%`
- 已经做过一轮主链补测
- 中后段 `301-479` 仍可能有：
  - worker 细分路径
  - queue 满载边界
  - 错误恢复
  - 优先级/回退组合

判断标准：
- 如果还能找到一整段未被正式 pytest 吃掉的路径，就值得继续做
- 如果剩下的只是过碎的小边角，优先级可以下调

### B. `compaction_diag.py`

建议作为“更重的一刀”。

适合切入的时机：
- `text_compressor.py` 已做完
- `compress_queue.py` 也吃掉一批以后
- 需要再追一个明显的 overall 提升时

### C. `perf_bench.py`

不要机械地因为它是 `0.0%` 就立刻补测。

先判断它属于哪类：
1. **正式业务模块** → 值得补基础测试
2. **性能基准/脚手架工具** → 可能更适合：
   - 降低优先级
   - 标注 debug-only / experimental
   - 不把它当当前阶段主要覆盖目标

---

## 七、明确不要丢的事实基线

下一个模型不要再重复探索下面这些已经确认过的事实：

### 已完成并已验证通过
- `llm_text_compressor.py`：**95.9%**
- `cli.py`：**61.9%**
- `algo_scheduler.py`：**99.5%**
- `text_compressor.py`：**98.6%**
- `compaction_diag.py`：**86.5%**
- `compress_queue.py`：**98.0%**
- 全量：`473 passed, 1 skipped`，**72.6%** （2026-07-01 13:00）

### 已验证有效的命令

```bash
python3 -m pytest scripts/tests/unit/test_llm_text_compressor.py -q
python3 -m pytest scripts/tests/unit/test_cli.py -q
python3 -m pytest scripts/tests/unit/test_algo_scheduler.py -q
python3 -m pytest scripts/tests/unit/test_text_compressor.py -q
python3 -m pytest scripts/tests/unit/test_compaction_diag.py -q
python3 -m pytest scripts/tests/unit/test_compress_queue.py -q
python3 -m pytest scripts/tests/ --cov=scripts/mark42_modules --cov-report=term-missing -q
```

### 已验证有效的 mock / 测试策略
- `cli.py`：mock target 用完整模块路径
- `algo_scheduler.py`：`_run_tests()` 可以通过 mock `process()` 稳定收编
- `llm_text_compressor.py`：可通过“有模型字段但无 key”的方式让真实 LLM 分支自动 skip
- 写入不稳时：**优先 `edit`，少用整文件 `write`**

---

## 八、推荐的交接执行清单（给下一个模型）

### 交接后第 1 轮
1. 读本文件
2. 读：
   - `docs/design/mark42-更新日志.md`
   - `docs/design/mark42-测试手册.md`
3. 读目标模块与现有测试：
   - `scripts/mark42_modules/compaction_diag.py`
   - `scripts/tests/unit/test_compaction_diag.py`
4. 只做一件事：锁定 `compaction_diag.py` 的高价值缺口

### 交接后第 2 轮
1. 直接在现有 `test_compaction_diag.py` 中补测试
2. 先单跑
3. 失败就按真实实现修测试
4. 单跑通过后再跑全量覆盖

### 交接后第 3 轮
1. 更新：
   - `docs/design/mark42-更新日志.md`
   - `memory/daily/2026-07-01.md`（或当日 daily）
2. 再决定下一块：
   - `compress_queue.py`
   - `perf_bench.py`
   - `config.py` / `engine.py`

---

## 九、一句话结论

**当前最优接力方向不是再到处散打，而是顺着这条已验证有效的路径继续推进：先拿下 `compaction_diag.py`，再视收益转向 `compress_queue.py` 或 `perf_bench.py`。**

这条路线的优点是：
- 连续
- 稳
- 已经有实跑验证过的方法论
- 换模型后也不容易从头乱试
