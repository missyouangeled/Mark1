# 保命修复插件索引

## 当前已落地插件

> 说明：前 1-5 项属于轻修复；第 6 项属于只读审计，不改系统状态。

1. `repair_stale_watcher_alerts.py`
   - 处理 watcher `staleItems`
   - 类型：轻修复

2. `repair_archive_resolved_watcher_alerts.py`
   - 归档并清理已解决的 watcher 历史告警
   - 类型：轻修复

3. `repair_backup_kick_once.py`
   - backup 过旧时补跑一次备份
   - 类型：轻修复

4. `repair_health_collect_once.py`
   - health 过旧时补跑一次健康采集
   - 类型：轻修复

5. `repair_frontstage_guardian_collect_once.py`
   - frontstage 过旧时补跑一次前台保护检查
   - 类型：轻修复

6. `repair_audit_snapshot.py`
   - 将当前聚合状态追加写入审计轨迹
   - 类型：只读审计

## 总边界

- 不碰主 session 内容
- 不改 cron / systemd / gateway 配置
- 不做 restart
- 不做 cleanup --enforce
- 优先做只读 / 补采集 / 补检查 / 历史归档
- 删除 `skills/emergency-repair-pack/` 后，核心保命层仍需可独立运行

## 配套脚本 / 配置

- repair runner：`scripts/emergency0-repair-runner.py`
- 阈值配置：`scripts/emergency0-config.json`
- 删插件回归：`scripts/emergency0-delete-pack-regression.py`
- OpenClaw 故障回归：`scripts/openclaw-emergency-regression.py`
- Python 编排入口：`scripts/emergency1-orchestrator.py`

## 当前状态

- repair runner：`scripts/emergency0-repair-runner.py`
- plugin pack：`skills/emergency-repair-pack/`
- 通知当前仍走 `openclaw-proactive-inject.py`，但已由 `emergency0-notify.py` 统一承接 cooldown / 去重 / 前台事件语义
- 运行说明：`docs/runtime/保命体系运行说明-2026-07-06.md`
