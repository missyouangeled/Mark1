# trae-agent 环境变量模板
# 复制这份到 ~/trae-agent/.env 然后填真 key：
#   cp tools/trae-agent/.env.example ~/trae-agent/.env
#   chmod 600 ~/trae-agent/.env
#   # 编辑 .env 把占位符换成 OpenRouter 控制台的真 key

OPENROUTER_API_KEY=*** 去 https://openrouter.ai/settings/keys 拿真 key 填这
